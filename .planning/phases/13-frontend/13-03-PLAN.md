---
phase: 13-frontend
plan: 03
type: execute
wave: 3
depends_on:
  - "13-02"
files_modified:
  - apps/web/src/components/forms/VehicleForm.tsx
  - tests/e2e/specs/vehicle-form-vin.spec.ts
autonomous: true
requirements:
  - FE-01
  - FE-02
user_setup: []

must_haves:
  truths:
    - "VehicleForm submits to POST /api/v1/products with attributes.vin"
    - "VIN decode flow still works end-to-end (fields populate correctly)"
    - "Form submit creates product+vehicle records in database"
    - "Submit redirects to catalog after success"
  artifacts:
    - path: "apps/web/src/components/forms/VehicleForm.tsx"
      provides: "Vehicle form with product API integration"
      min_lines: 120
      contains: "createProductWithVehicle"
    - path: "tests/e2e/specs/vehicle-form-vin.spec.ts"
      provides: "E2E test for VIN decode → submit flow"
      min_lines: 100
  key_links:
    - from: "VehicleForm.tsx"
      to: "createProductWithVehicle"
      via: "onSubmit handler with mapped attributes"
      pattern: "createProductWithVehicle.*attributes.*vin"
    - from: "vehicle-form-vin.spec.ts"
      to: "/api/v1/products"
      via: "E2E test verifies POST request"
      pattern: "POST.*api/v1/products"
---

<objective>
Update VehicleForm submit handler to use product creation with auto-vehicle creation.

Purpose: Replace old vehicle creation endpoint with new products endpoint. Update form submission to use single-call product creation with auto-vehicle creation (Decision #1). Preserve existing VIN decode functionality. Verify end-to-end flow works.

Output: VehicleForm component that submits to POST /api/v1/products with attributes.vin for auto-vehicle creation, with passing E2E test.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-frontend/13-CONTEXT.md
@.planning/phases/13-frontend/13-RESEARCH.md
@.planning/phases/13-frontend/13-02-SUMMARY.md
@.planning/phases/12-backend-api/12-05-SUMMARY.md

# Existing VehicleForm Structure (from apps/web/src/components/forms/VehicleForm.tsx)

Current implementation after Plan 13-02:
- Uses React Hook Form + Zod validation
- Has VIN decode button that calls `useDecodeVin()` hook
- Category dropdown loads from API via `useCategories()`
- Form fields: year, make, model, trim, body_type, mileage, exterior_color, interior_color, etc.
- Submit handler still calls old vehicle creation endpoint

# Key Changes Required

1. Update submit handler:
   - OLD: POST to `/api/v1/vehicles`
   - NEW: `createProductWithVehicle()` with attributes.vin

2. Preserve VIN decode:
   - Keep existing `/api/v1/vehicles/decode-vin` call
   - Ensure decoded fields populate form correctly
   - No changes to decode logic

3. Preserve form validation:
   - Keep existing Zod schema validation
   - Keep existing error display logic
   - Keep existing disabled state during submission

# Brain #7 Condition #3: Field Mapping Table

| VehicleForm Field | CreateProductRequest Mapping | Notes |
|-------------------|------------------------------|-------|
| `vin` | `attributes.vin` | **REQUIRED** - triggers auto-vehicle creation |
| `year` | `attributes.year` | Number or undefined |
| `make` | `attributes.make` | String or undefined |
| `model` | `attributes.model` | String or undefined |
| `trim` | `attributes.trim` | String or undefined |
| `body_type` | `attributes.body_type` | String or undefined |
| `body_style` | `attributes.body_style` | String or undefined |
| `drivetrain` | `attributes.drivetrain` | String or undefined |
| `transmission` | `attributes.transmission` | String or undefined |
| `engine` | `attributes.engine` | String or undefined |
| `fuel_type` | `attributes.fuel_type` | String or undefined |
| `mpg_city` | `attributes.mpg_city` | Number or null |
| `mpg_highway` | `attributes.mpg_highway` | Number or null |
| `mpg_combined` | `attributes.mpg_combined` | Number or null |
| `mileage` | `attributes.mileage` | Number or undefined |
| `mileage_unit` | `attributes.mileage_unit` | "mi" or "km" |
| `exterior_color` | `attributes.exterior_color` | String or undefined |
| `interior_color` | `attributes.interior_color` | String or undefined |
| `has_sunroof` | `attributes.has_sunroof` | Boolean |
| `has_navigation` | `attributes.has_navigation` | Boolean |
| `has_leather` | `attributes.has_leather` | Boolean |
| `has_backup_camera` | `attributes.has_backup_camera` | Boolean |
| `has_bluetooth` | `attributes.has_bluetooth` | Boolean |
| `has_remote_start` | `attributes.has_remote_start` | Boolean |
| `seat_material` | `attributes.seat_material` | String or undefined |
| `stock_number` | `attributes.stock_number` | String or undefined |
| `description` | `attributes.description` | String or undefined |
| `category_id` | `category_id` | **Top-level field** (not in attributes) |
| `price` | `price_cents` | **Top-level field** (converted to cents, TODO: add to form) |
| `title` | `title` | **Top-level field** (constructed as "${year} ${make} ${model}") |

**Key Mapping Rules**:
- `attributes.vin` is **REQUIRED** for auto-vehicle creation
- `category_id` and `price_cents` are top-level fields, NOT in `attributes`
- `title` is constructed from year/make/model at submit time
- All vehicle-specific fields go into `attributes` object
- Backend auto-creates vehicle record when `attributes.vin` is present
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update VehicleForm submit handler for product creation</name>
  <files>apps/web/src/components/forms/VehicleForm.tsx</files>
  <behavior>
    - Test 1: Submit handler calls createProductWithVehicle
    - Test 2: Request includes attributes.vin
    - Test 3: Request includes title constructed from year/make/model
    - Test 4: Request includes price_cents converted from dollars
    - Test 5: Submit shows loading state during mutation
  </behavior>
  <action>
    Update VehicleForm submit handler to use product creation:

    1. Import `useCreateProduct` hook:
       ```typescript
       import { useCreateProduct } from '@/lib/api/products';
       const createProduct = useCreateProduct();
       ```

    2. Replace old submit logic:
       - Find current `onSubmit` or `handleSubmit` function
       - Replace old vehicle creation call with:
       ```typescript
       const onSubmit = async (data: VehicleFormValues) => {
         try {
           await createProduct.mutateAsync({
             title: `${data.year} ${data.make} ${data.model}`.trim(),
             price_cents: Math.round(Number(data.price) * 100),
             category_id: data.category_id,
             attributes: {
               vin: data.vin,  // REQUIRED - triggers auto-vehicle creation
               year: data.year,
               make: data.make,
               model: data.model,
               trim: data.trim,
               body_type: data.body_type,
               mileage: data.mileage,
               exterior_color: data.exterior_color,
               interior_color: data.interior_color,
               // ... map all other form fields to attributes
             },
           });

           toast.success("Vehicle created successfully");
           router.push("/catalog");
         } catch (error) {
           // Error handling already in useCreateProduct
         }
       };
       ```

    3. Key mapping rules:
       - `title`: Construct from year/make/model (or use form title field)
       - `price_cents`: Convert from dollars to cents (×100)
       - `category_id`: From category select field
       - `attributes.vin`: REQUIRED - backend auto-creates vehicle if present
       - `attributes.*`: Map all vehicle-specific fields to attributes object

    4. Preserve form validation:
       - Keep existing Zod schema validation
       - Keep existing error display logic
       - Keep existing disabled state during submission

    Test with valid VIN to verify auto-vehicle creation works.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- VehicleForm.test.tsx --grep submit</automated>
  </verify>
  <done>
    VehicleForm submits to POST /api/v1/products with attributes.vin for auto-vehicle creation. Form validation and error handling preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify VIN decode end-to-end flow</name>
  <what-built>VehicleForm with product API integration and VIN decode functionality</what-built>
  <how-to-verify>
    1. Run E2E test: `cd tests/e2e && pnpm test -- vehicle-form-vin.spec.ts --grep "VIN decode populates"`
    2. Expected: Test passes (VIN decode → populate fields → submit → product+vehicle created)
    3. Manual verification:
       - Open app in browser
       - Login as admin@prosell-demo.com / Admin123!
       - Navigate to /catalog/new
       - Enter VIN: 1HGCM82633A004352
       - Click "Decode VIN" → verify fields populate (year=2020, make=honda, model=civic)
       - Select category from dropdown
       - Fill price: 18500
       - Submit form
       - Verify redirect to /catalog
       - Check database → verify product created with linked vehicle record
  </how-to-verify>
  <resume-signal>Type "approved" if VIN decode → submit flow works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Run `cd apps/web && pnpm test -- VehicleForm.test.tsx` to verify component tests pass
2. Run E2E test: `cd tests/e2e && pnpm test -- vehicle-form-vin.spec.ts` to verify VIN decode → submit flow
3. Manual verification:
   - Open VehicleForm in browser
   - Enter valid VIN, click decode → fields populate
   - Submit form → check network tab for POST /api/v1/products
   - Verify product created in database with linked vehicle record
</verification>

<success_criteria>
1. Form submit calls POST /api/v1/products with attributes.vin
2. API response includes `has_vehicle: true` and `vehicle_id` populated (Brain #7 Condition #4)
3. VIN decode populates form fields correctly (existing functionality preserved)
4. Form validation works with new schema
5. Submit redirects to catalog after success
6. Component tests pass
7. E2E VIN decode + submit flow passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend/13-03-SUMMARY.md` with:
- VehicleForm changes summary (part 2 of 2 complete)
- Product API integration status
- VIN decode preservation verification
- Test results
- Any deviations from plan
</output>
