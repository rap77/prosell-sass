---
phase: 13-frontend
plan: 07
type: execute
wave: 5
depends_on:
  - "13-06"
files_modified:
  - tests/e2e/specs/vehicles.spec.ts
  - tests/e2e/specs/bulk-upload.spec.ts
  - tests/e2e/specs/categories.spec.ts
autonomous: true
requirements:
  - FE-01
  - FE-02
  - FE-03
  - FE-04
user_setup: []

must_haves:
  truths:
    - "DataGrid E2E tests pass with C3 join data"
    - "Bulk upload E2E tests pass with products schema"
    - "Category E2E tests pass with API integration"
    - "No regressions in existing E2E tests"
  artifacts:
    - path: "tests/e2e/specs/vehicles.spec.ts"
      provides: "DataGrid E2E tests for C3 join data"
      min_lines: 80
    - path: "tests/e2e/specs/bulk-upload.spec.ts"
      provides: "Bulk upload E2E tests for products schema"
      min_lines: 80
    - path: "tests/e2e/specs/categories.spec.ts"
      provides: "Category E2E tests for API integration"
      min_lines: 50
  key_links:
    - from: "vehicles.spec.ts"
      to: "VehicleDataGrid"
      via: "Component test for DataGrid with C3 data"
      pattern: "VehicleDataGrid.*test"
    - from: "bulk-upload.spec.ts"
      to: "BulkUploadForm"
      via: "Component test for CSV upload with products API"
      pattern: "BulkUploadForm.*test"
    - from: "categories.spec.ts"
      to: "CategorySelector"
      via: "Component test for category loading"
      pattern: "CategorySelector.*test"
---

<objective>
Update DataGrid, bulk upload, and category E2E tests for C3 schema integration.

Purpose: Complete E2E test coverage for remaining components affected by the products+vehicles schema change. Update DataGrid tests for C3 join data (title, price from product). Update bulk upload tests for products API with auto-vehicle creation. Update category tests for API integration.

Output: Updated E2E tests for DataGrid, bulk upload, and categories that work with new C3 schema.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-frontend/13-CONTEXT.md
@.planning/phases/13-frontend/13-RESEARCH.md
@.planning/phases/13-frontend/13-03-SUMMARY.md
@.planning/phases/13-frontend/13-04-SUMMARY.md
@.planning/phases/13-frontend/13-06-SUMMARY.md

# DataGrid C3 Join Data (from Phase 12)

Backend now returns `VehicleWithProduct` DTO:
```typescript
{
  id: string;           // vehicle.id
  product_id: string;
  title: string;        // from product.title
  price_cents: number;  // from product.price_cents
  status: string;       // from product.status
  year: number;
  make: string;
  model: string;
  // ... other vehicle fields
}
```

# Bulk Upload Products API (from Phase 12)

Backend now accepts:
```typescript
POST /api/v1/products/bulk
{
  products: Array<{
    title: string;
    price_cents: number;
    category_id: string;
    attributes: {
      vin: string;  // Auto-creates vehicle
      year: number;
      make: string;
      model: string;
      // ...
    };
  }>;
}
```

# Category API Integration (from Plan 13-01)

Categories now load from `GET /api/v1/categories` with:
- `name`: Human-readable display name
- `attribute_schema`: Conditional rendering rules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DataGrid E2E tests for C3 join data</name>
  <files>tests/e2e/specs/vehicles.spec.ts</files>
  <action>
    Update `tests/e2e/specs/vehicles.spec.ts` to work with C3 join data:

    1. Update vehicle data assertions:
       - Verify `title` field displays (from product.title)
       - Verify `price` field displays correctly (from product.price_cents / 100)
       - Verify `status` badge shows product.status

    2. Update infinite scroll tests:
       - Verify cursor pagination works
       - Verify next_cursor is passed correctly
       - Verify rows load without duplicates

    3. Add test for vehicle fields:
       - Verify year, make, model display from vehicle object
       - Verify data transformation works correctly

    4. Preserve existing tests:
       - Row selection
       - Status badges
       - Sorting/filtering

    Run tests to verify all pass.
  </action>
  <verify>
    <automated>cd tests/e2e && pnpm test -- vehicles.spec.ts</automated>
  </verify>
  <done>
    DataGrid E2E tests updated for C3 join data. All tests pass with new schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update bulk upload E2E tests for products schema</name>
  <files>tests/e2e/specs/bulk-upload.spec.ts</files>
  <action>
    Update `tests/e2e/specs/bulk-upload.spec.ts` to work with products schema:

    1. Update CSV parsing assertions:
       - Verify CSV maps to products array
       - Verify attributes.vin is included
       - Verify price converted to cents

    2. Update API call assertions:
       - Verify POST to `/api/v1/products/bulk` (not `/api/v1/vehicles/bulk-upload`)
       - Verify request body has products array

    3. Update response assertions:
       - Verify created_count/failed_count format
       - Verify error array structure (row_number, vin, error)

    4. Add test for auto-vehicle creation:
       - Upload CSV with valid VINs
       - Verify products created in database
       - Verify vehicles created and linked to products

    Run tests to verify all pass.
  </action>
  <verify>
    <automated>cd tests/e2e && pnpm test -- bulk-upload.spec.ts</automated>
  </verify>
  <done>
    Bulk upload E2E tests updated for products schema. All tests pass with new auto-vehicle creation flow.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update category E2E tests for API integration</name>
  <files>tests/e2e/specs/categories.spec.ts</files>
  <action>
    Update `tests/e2e/specs/categories.spec.ts` to work with API integration:

    1. Update category loading tests:
       - Verify categories load from `GET /api/v1/categories`
       - Verify categories display names (not UUIDs)
       - Verify attribute_schema is available

    2. Update category filtering tests:
       - Verify filter dropdown uses API data
       - Verify filter applies correctly
       - Verify filtered results match category

    3. Add test for conditional attributes:
       - Select category with year attribute
       - Verify year field displays
       - Select category without year attribute
       - Verify year field hidden

    Run tests to verify all pass.
  </action>
  <verify>
    <automated>cd tests/e2e && pnpm test -- categories.spec.ts</automated>
  </verify>
  <done>
    Category E2E tests updated for API integration. All tests pass with dynamic category loading.
  </done>
</task>

</tasks>

<verification>
1. Run updated E2E tests:
   - `cd tests/e2e && pnpm test -- vehicles.spec.ts`
   - `cd tests/e2e && pnpm test -- bulk-upload.spec.ts`
   - `cd tests/e2e && pnpm test -- categories.spec.ts`

2. Run full E2E suite:
   - `cd tests/e2e && pnpm test`
   - Check for regressions
   - Verify no new failures

3. Manual verification:
   - Open DataGrid, verify title and price display correctly
   - Test bulk upload with CSV, verify products created
   - Test category filtering, verify correct results
</verification>

<success_criteria>
1. DataGrid E2E tests pass with C3 join data
2. Bulk upload E2E tests pass with products schema
3. Category E2E tests pass with API integration
4. No regressions in previously passing tests
5. Manual testing confirms all components work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend/13-07-SUMMARY.md` with:
- DataGrid E2E test updates
- Bulk upload E2E test updates
- Category E2E test updates
- Test results summary
- Any remaining issues
</output>
