---
phase: 13-frontend
plan: 05
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/smoke.spec.ts
autonomous: true
requirements:
  - FE-01
  - FE-02
  - FE-03
  - FE-04
user_setup: []

must_haves:
  truths:
    - "Smoke test stub file exists with 20 test placeholders"
    - "Smoke tests can run without errors (test structure valid)"
    - "Smoke tests cover all critical paths (auth, form, grid, upload, categories, contracts)"
  artifacts:
    - path: "tests/e2e/smoke.spec.ts"
      provides: "Smoke test suite stub (20 test placeholders)"
      min_lines: 100
      contains: "test.describe('Smoke Tests')"
  key_links:
    - from: "smoke.spec.ts"
      to: "@playwright/test"
      via: "Import test framework"
      pattern: "import.*@playwright/test"
---

<objective>
Create Wave 0 smoke test stub file with 20 test placeholders.

Purpose: Satisfy Nyquist compliance by creating test stubs BEFORE plan execution begins. This ensures all `<automated>` verification commands have existing test files to run. The stubs will be implemented in Plan 13-06.

Output: Smoke test file with 20 placeholder tests (auth, VehicleForm, DataGrid, CSV upload, categories, API contracts).
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-frontend/13-CONTEXT.md
@.planning/phases/13-frontend/13-VALIDATION.md

# Wave 0 Requirement (from VALIDATION.md)

Nyquist compliance requires test files to exist BEFORE execution:

> - [ ] `tests/e2e/smoke.spec.ts` — 20 smoke tests covering critical paths

This is a Wave 0 task — create stubs that will be implemented in later plans.

# Smoke Test Distribution (from CONTEXT.md Decision #5)

1. **Auth Flow (3 tests)**
   - Login with valid credentials
   - Logout redirects to home
   - Protected route redirects to login

2. **Vehicle Form (5 tests)**
   - VIN decode populates fields
   - Category dropdown loads options
   - Form submit creates product+vehicle
   - Form validation shows errors
   - Submit redirects to catalog

3. **DataGrid (4 tests)**
   - DataGrid loads first page
   - Infinite scroll loads more rows
   - Row selection works
   - Status badge displays correctly

4. **CSV Upload (3 tests)**
   - CSV file uploads successfully
   - Invalid VIN shows error
   - Success toast displays count

5. **Categories (2 tests)**
   - Category list loads
   - Category filter works

6. **API Contracts (3 tests)**
   - GET /api/v1/categories returns valid schema
   - POST /api/v1/products creates product+vehicle
   - GET /api/v1/vehicles returns paginated results
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke test stub file with 20 placeholders</name>
  <files>tests/e2e/smoke.spec.ts</files>
  <action>
    Create `tests/e2e/smoke.spec.ts` with 20 placeholder tests:

    ```typescript
    import { test, expect } from '@playwright/test';

    // Auth Flow (3 tests)
    test.describe('Auth Flow', () => {
      test('login with valid credentials', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('logout redirects to home', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('protected route redirects to login', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });
    });

    // Vehicle Form (5 tests)
    test.describe('Vehicle Form', () => {
      test('VIN decode populates fields', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('category dropdown loads options', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('form submit creates product+vehicle', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('form validation shows errors', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('submit redirects to catalog', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });
    });

    // DataGrid (4 tests)
    test.describe('DataGrid', () => {
      test('datagrid loads first page', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('infinite scroll loads more rows', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('row selection works', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('status badge displays correctly', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });
    });

    // CSV Upload (3 tests)
    test.describe('CSV Upload', () => {
      test('CSV file uploads successfully', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('invalid VIN shows error', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('success toast displays count', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });
    });

    // Categories (2 tests)
    test.describe('Categories', () => {
      test('category list loads', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('category filter works', async ({ page }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });
    });

    // API Contracts (3 tests)
    test.describe('API Contracts', () => {
      test('GET /api/v1/categories returns valid schema', async ({ request }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('POST /api/v1/products creates product+vehicle', async ({ request }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });

      test('GET /api/v1/vehicles returns paginated results', async ({ request }) => {
        // TODO: Implement in Plan 13-06
        test.skip(true, 'Not implemented yet');
      });
    });
    ```

    Key points:
    - 20 test placeholders (all marked as skip)
    - Test structure is valid (can run without errors)
    - All tests marked TODO for implementation in Plan 13-06
    - Covers all FE requirements (FE-01 through FE-04)
  </action>
  <verify>
    <automated>cd tests/e2e && pnpm test -- smoke.spec.ts</automated>
  </verify>
  <done>
    Smoke test stub file created with 20 placeholder tests. File structure valid, tests marked skip until implementation in Plan 13-06.
  </done>
</task>

</tasks>

<verification>
1. Run `cd tests/e2e && pnpm test -- smoke.spec.ts` to verify file exists and structure is valid
2. Verify output shows 20 skipped tests (not errors)
3. Verify test file has correct imports and structure
</verification>

<success_criteria>
1. Smoke test file exists at `tests/e2e/smoke.spec.ts`
2. File contains 20 test placeholders (all marked skip)
3. Tests can run without syntax errors
4. Test distribution matches CONTEXT.md Decision #5
5. Nyquist compliance satisfied (Wave 0 stubs created before execution)
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend/13-05-SUMMARY.md` with:
- Smoke test stub file created
- Test structure validated
- Ready for implementation in Plan 13-06
</output>
