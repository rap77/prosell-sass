---
phase: 08-layout-shell-vehicle-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/tests/unit/components/datagrid/DataGrid.test.tsx
  - apps/web/tests/unit/components/datagrid/DataGridRow.test.tsx
  - apps/web/tests/unit/components/datagrid/StatusBadge.test.tsx
  - apps/web/tests/unit/components/datagrid/ActionMenu.test.tsx
  - apps/web/tests/unit/components/layout/Sidebar.test.tsx
  - apps/web/tests/unit/components/layout/Header.test.tsx
  - apps/web/tests/unit/components/layout/MobileNav.test.tsx
  - apps/web/tests/unit/components/layout/CommandPalette.test.tsx
  - apps/web/tests/unit/components/upload/BulkUpload.test.tsx
  - apps/web/tests/unit/components/upload/ImageDropzone.test.tsx
  - apps/web/tests/unit/components/upload/UploadProgress.test.tsx
  - apps/web/tests/unit/components/filters/FilterSidebar.test.tsx
  - apps/web/tests/unit/components/filters/FilterPills.test.tsx
  - apps/web/tests/unit/hooks/useDataGrid.test.ts
  - apps/web/tests/unit/hooks/useImageUpload.test.ts
  - apps/web/tests/e2e/upload/
  - apps/api/tests/integration/bulk_upload/
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 16 test stub files exist with placeholder tests"
    - "Vitest can run the test files without errors"
    - "Pytest can discover the integration test directory"
    - "E2E test directory structure exists"
  artifacts:
    - path: "apps/web/tests/unit/components/datagrid/DataGrid.test.tsx"
      provides: "DataGrid component test stub"
      contains: "describe block for DataGrid"
    - path: "apps/web/tests/unit/hooks/useDataGrid.test.ts"
      provides: "useDataGrid hook test stub"
      contains: "describe block for useDataGrid"
    - path: "apps/web/tests/e2e/upload/"
      provides: "E2E upload test directory"
      contains: "test spec files for upload flows"
    - path: "apps/api/tests/integration/bulk_upload/"
      provides: "Bulk upload integration tests"
      contains: "test_bulk_upload.py"
  key_links:
    - from: "apps/web/tests/unit/"
      to: "apps/web/src/components/"
      via: "test files mirror component structure"
      pattern: "tests/unit/components/.*\\.test\\.tsx"
    - from: "apps/web/tests/e2e/"
      to: "apps/web/src/app"
      via: "end-to-end flow testing"
      pattern: "tests/e2e/.*\\.spec\\.ts"
---

<objective>
Create 16 test stub files (13 component tests, 2 hook tests, 1 E2E directory) before Phase 8 execution begins. This Wave 0 plan ensures all Nyquist-compliant verification commands have test files to run, preventing "MISSING — Wave 0 must create" delays during execution.

Purpose: Test-driven development requires test files to exist before implementing features. Wave 0 creates all test scaffolds upfront so subsequent plans can immediately run `<automated>` verify commands without blocking.

Output: 16 test stub files with placeholder describe blocks, directory structure mirrors component organization, Vitest/Pytest can discover and run all tests.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-layout-shell-vehicle-management/08-CONTEXT.md
@.planning/phases/08-layout-shell-vehicle-management/08-VALIDATION.md
@apps/web/vitest.config.ts
@apps/api/pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataGrid component test stubs (4 files)</name>
  <files>
    apps/web/tests/unit/components/datagrid/DataGrid.test.tsx
    apps/web/tests/unit/components/datagrid/DataGridRow.test.tsx
    apps/web/tests/unit/components/datagrid/StatusBadge.test.tsx
    apps/web/tests/unit/components/datagrid/ActionMenu.test.tsx
  </files>
  <action>
    Create test stub files for DataGrid components:

    1. Create directory: `mkdir -p apps/web/tests/unit/components/datagrid`

    2. Create `DataGrid.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'
       import { render, screen } from '@testing-library/react'
       import { DataGrid } from '@/components/datagrid/DataGrid'

       describe('DataGrid', () => {
         it('renders without crashing', () => {
           const mockData = []
           render(<DataGrid data={mockData} />)
           // Placeholder: Add actual tests after implementation
         })

         it('displays vehicle data correctly', () => {
           // TODO: Implement after DataGrid component exists
         })

         it('virtualizes rows for performance', () => {
           // TODO: Verify only ~40 rows in DOM with 1000+ data
         })
       })
       ```

    3. Create `DataGridRow.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('DataGridRow', () => {
         it('renders memoized row', () => {
           // TODO: Test memoization prevents re-renders
         })

         it('displays cell data correctly', () => {
           // TODO: Test flexRender from TanStack Table
         })
       })
       ```

    4. Create `StatusBadge.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'
       import { render, screen } from '@testing-library/react'
       import { StatusBadge } from '@/components/datagrid/StatusBadge'

       describe('StatusBadge', () => {
         it('renders Published status with green color', () => {
           render(<StatusBadge status="published" />)
           // TODO: Verify color class and icon
         })

         it('renders all 6 status states correctly', () => {
           // TODO: Test published, pending, failed, draft, expired, online, sold
         })

         it('includes icon for accessibility', () => {
           // TODO: Verify icon + text for colorblind users
         })
       })
       ```

    5. Create `ActionMenu.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'
       import { render, screen } from '@testing-library/react'
       import { ActionMenu } from '@/components/datagrid/ActionMenu'

       describe('ActionMenu', () => {
         it('renders dropdown menu trigger', () => {
           // TODO: Test menu button renders
         })

         it('calls onPublish when Publish clicked', () => {
           // TODO: Test callback invocation
         })

         it('calls onDelete when Delete clicked', () => {
           // TODO: Test delete action
         })
       })
       ```

    All files follow Testing Library + Vitest patterns.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test tests/unit/components/datagrid/ --run</automated>
  </verify>
  <done>4 DataGrid test stub files exist and Vitest can run them</done>
</task>

<task type="auto">
  <name>Task 2: Create layout component test stubs (4 files)</name>
  <files>
    apps/web/tests/unit/components/layout/Sidebar.test.tsx
    apps/web/tests/unit/components/layout/Header.test.tsx
    apps/web/tests/unit/components/layout/MobileNav.test.tsx
    apps/web/tests/unit/components/layout/CommandPalette.test.tsx
  </files>
  <action>
    Create test stub files for layout components:

    1. Create directory: `mkdir -p apps/web/tests/unit/components/layout`

    2. Create `Sidebar.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'
       import { render, screen } from '@testing-library/react'
       import { Sidebar } from '@/components/layout/Sidebar'

       describe('Sidebar', () => {
         it('renders navigation groups', () => {
           // TODO: Test Inventario, Ventas, Configuración groups
         })

         it('filters navigation by role', () => {
           // TODO: Test seller doesn't see Configuración
         })

         it('toggles collapse state', () => {
           // TODO: Test Zustand store integration
         })

         it('uses corrected terminology (not Operations/Growth)', () => {
           // TODO: Verify Inventario/Ventas/Configuración labels
         })
       })
       ```

    3. Create `Header.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('Header', () => {
         it('renders search input', () => {
           // TODO: Test global search field
         })

         it('displays breadcrumbs', () => {
           // TODO: Test usePathname integration
         })

         it('shows user menu with role badge', () => {
           // TODO: Test user.role from AuthProvider
         })

         it('renders org switcher placeholder', () => {
           // TODO: Test org switcher (Phase 5 feature)
         })
       })
       ```

    4. Create `MobileNav.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('MobileNav', () => {
         it('renders 4 bottom navigation icons', () => {
           // TODO: Test Catálogo, Publicar, Leads, Más icons
         })

         it('has 44px touch targets (Fitts\'s Law)', () => {
           // TODO: Verify min-width and min-height
         })

         it('highlights active route', () => {
           // TODO: Test usePathname highlighting
         })

         it('opens drawer for Más icon', () => {
           // TODO: Test drawer trigger
         })
       })
       ```

    5. Create `CommandPalette.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('CommandPalette', () => {
         it('opens on Cmd+K keyboard shortcut', () => {
           // TODO: Test keyboard event listener
         })

         it('searches vehicles by VIN', () => {
           // TODO: Test search functionality
         })

         it('executes actions (publish, edit, delete)', () => {
           // TODO: Test action selection
         })

         it('closes on Escape key', () => {
           // TODO: Test keyboard dismissal
         })
       })
       ```

    All files test layout and navigation behavior.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test tests/unit/components/layout/ --run</automated>
  </verify>
  <done>4 layout test stub files exist and Vitest can run them</done>
</task>

<task type="auto">
  <name>Task 3: Create upload component test stubs (3 files)</name>
  <files>
    apps/web/tests/unit/components/upload/BulkUpload.test.tsx
    apps/web/tests/unit/components/upload/ImageDropzone.test.tsx
    apps/web/tests/unit/components/upload/UploadProgress.test.tsx
  </files>
  <action>
    Create test stub files for upload components:

    1. Create directory: `mkdir -p apps/web/tests/unit/components/upload`

    2. Create `BulkUpload.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('BulkUpload', () => {
         it('validates CSV with Zod schema', () => {
           // TODO: Test validation catches wrong columns
         })

         it('shows error for invalid file type', () => {
           // TODO: Test .txt rejection
         })

         it('displays progress bar during upload', () => {
           // TODO: Test chunk progress (0-100%)
         })

         it('shows ETA based on chunk speed', () => {
           // TODO: Test ETA calculation
         })

         it('downloads failed rows CSV on error', () => {
           // TODO: Test error report download
         })
       })
       ```

    3. Create `ImageDropzone.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('ImageDropzone', () => {
         it('accepts drag-and-drop images', () => {
           // TODO: Test drop event handling
         })

         it('shows immediate previews with URL.createObjectURL', () => {
           // TODO: Test instant thumbnail generation
         })

         it('allows drag-to-reorder images', () => {
           // TODO: Test first image = cover photo
         })

         it('limits to 10 images per vehicle', () => {
           // TODO: Test max file count
         })
       })
       ```

    4. Create `UploadProgress.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('UploadProgress', () => {
         it('displays per-image progress bars', () => {
           // TODO: Test 0-100% per file
         })

         it('shows processing status after upload', () => {
           // TODO: Test "Compressing..." state
         })

         it('allows individual image deletion', () => {
           // TODO: Test remove button
         })
       })
       ```

    All files test upload UX and error handling.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test tests/unit/components/upload/ --run</automated>
  </verify>
  <done>3 upload test stub files exist and Vitist can run them</done>
</task>

<task type="auto">
  <name>Task 4: Create filter component test stubs (2 files)</name>
  <files>
    apps/web/tests/unit/components/filters/FilterSidebar.test.tsx
    apps/web/tests/unit/components/filters/FilterPills.test.tsx
  </files>
  <action>
    Create test stub files for filter components:

    1. Create directory: `mkdir -p apps/web/tests/unit/components/filters`

    2. Create `FilterSidebar.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('FilterSidebar', () => {
         it('renders faceted filters (Brand, Price, Status)', () => {
           // TODO: Test filter options
         })

         it('collapses to maximize DataGrid width', () => {
           // TODO: Test toggle button
         })

         it('syncs with URL search params', () => {
           // TODO: Test useSearchParams integration
         })
       })
       ```

    3. Create `FilterPills.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest'

       describe('FilterPills', () => {
         it('displays active filters as removable pills', () => {
           // TODO: Test pill rendering
         })

         it('clears individual filter on pill click', () => {
           // TODO: Test remove action
         })

         it('clears all filters with "Clear all" button', () => {
           // TODO: Test reset action
         })
       })
       ```

    Both files test search filter UX from CONTEXT.md Section 6.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test tests/unit/components/filters/ --run</automated>
  </verify>
  <done>2 filter test stub files exist and Vitest can run them</done>
</task>

<task type="auto">
  <name>Task 5: Create hook test stubs (2 files)</name>
  <files>
    apps/web/tests/unit/hooks/useDataGrid.test.ts
    apps/web/tests/unit/hooks/useImageUpload.test.ts
  </files>
  <action>
    Create test stub files for custom hooks:

    1. Create directory: `mkdir -p apps/web/tests/unit/hooks`

    2. Create `useDataGrid.test.ts`:
       ```tsx
       import { describe, it, expect, hooks } from 'vitest'
       import { renderHook, act } from '@testing-library/react'
       import { useDataGrid } from '@/lib/hooks/useDataGrid'

       describe('useDataGrid', () => {
         it('initializes with empty state', () => {
           // TODO: Test initial sorting/selection state
         })

         it('updates sorting state', () => {
           // TODO: Test setSorting callback
         })

         it('manages row selection map', () => {
           // TODO: Test checkbox selection state
         })

         it('handles global filter input', () => {
           // TODO: Test text search state
         })
       })
       ```

    3. Create `useImageUpload.test.ts`:
       ```tsx
       import { describe, it, expect } from 'vitest'
       import { renderHook, act } from '@testing-library/react'
       import { useImageUpload } from '@/lib/hooks/useImageUpload'

       describe('useImageUpload', () => {
         it('tracks upload progress (0-100%)', () => {
           // TODO: Test Zustand progress updates
         })

         it('shows optimistic preview immediately', () => {
           // TODO: Test onMutate from TanStack Query
         })

         it('rolls back on upload error', () => {
           // TODO: Test onError handling
         })

         it('allows reordering images', () => {
           // TODO: Test drag-to-reorder state
         })
       })
       ```

    Both files test custom hook state management.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test tests/unit/hooks/ --run</automated>
  </verify>
  <done>2 hook test stub files exist and Vitest can run them</done>
</task>

<task type="auto">
  <name>Task 6: Create E2E upload test directory</name>
  <files>apps/web/tests/e2e/upload/</files>
  <action>
    Create E2E test directory structure for upload flows:

    1. Create directory: `mkdir -p apps/web/tests/e2e/upload`

    2. Create `upload-flow.spec.ts`:
       ```typescript
       import { test, expect } from '@playwright/test'

       test.describe('Bulk Upload Flow', () => {
         test('uploads CSV with 50 valid vehicles', async ({ page }) => {
           // TODO: Navigate to /bulk-upload
           // TODO: Drop CSV file
           // TODO: Verify progress bar reaches 100%
           // TODO: Verify success message
         })

         test('validates CSV with wrong format', async ({ page }) => {
           // TODO: Upload invalid CSV
           // TODO: Verify error message appears
         })

         test('processes chunks in parallel', async ({ page }) => {
           // TODO: Upload 100 vehicles
           // TODO: Verify 2 chunks process simultaneously
         })
       })

       test.describe('Image Upload Flow', () => {
         test('drags and drops 5 images', async ({ page }) => {
           // TODO: Drag images to dropzone
           // TODO: Verify instant previews appear
           // TODO: Verify progress bars reach 100%
         })

         test('reorders images by dragging', async ({ page }) => {
           // TODO: Drag image 2 to position 1
           // TODO: Verify order changes
         })

         test('deletes individual image during upload', async ({ page }) => {
           // TODO: Click remove on image 3
           // TODO: Verify image disappears
         })
       })
       ```

    This creates the E2E test structure for VALIDATION.md requirement.
  </action>
  <verify>
    <automated>test -d apps/web/tests/e2e/upload && test -f apps/web/tests/e2e/upload/upload-flow.spec.ts</automated>
  </verify>
  <done>E2E upload directory exists with test spec file</done>
</task>

<task type="auto">
  <name>Task 7: Create backend bulk upload integration tests</name>
  <files>apps/api/tests/integration/bulk_upload/</files>
  <action>
    Create integration test directory for bulk upload API:

    1. Create directory: `mkdir -p apps/api/tests/integration/bulk_upload`

    2. Create `test_bulk_upload.py`:
       ```python
       import pytest
       from httpx import AsyncClient

       @pytest.mark.asyncio
       class TestBulkUpload:
           async def test_upload_csv_with_50_valid_rows(self, client: AsyncClient, auth_token):
               # TODO: Upload CSV with 50 vehicles
               # TODO: Verify 201 Created response
               # TODO: Verify job_id returned
               pass

           async def test_upload_csv_with_validation_errors(self, client: AsyncClient):
               # TODO: Upload CSV with Zod validation errors
               # TODO: Verify 400 Bad Request with error details
               pass

           async def test_chunk_processing_in_parallel(self, client: AsyncClient):
               # TODO: Upload 100 vehicles (2 chunks)
               # TODO: Verify chunks process with 3-4 workers
               # TODO: Verify progress endpoint returns 50% then 100%
               pass

           async def test_idempotency_on_retry(self, client: AsyncClient):
               # TODO: Upload same CSV twice
               # TODO: Verify no duplicate vehicles created
               pass

           async def test_tenant_isolation(self, client: AsyncClient, tenant_a_token, tenant_b_token):
               # TODO: Upload vehicles as Tenant A
               # TODO: Verify Tenant B cannot see them
               pass
       ```

    This creates the integration test structure for VALIDATION.md requirement.
  </action>
  <verify>
    <automated>test -d apps/api/tests/integration/bulk_upload && test -f apps/api/tests/integration/bulk_upload/test_bulk_upload.py</automated>
  </verify>
  <done>Integration test directory exists with bulk upload test file</done>
</task>

</tasks>

<verification>
### Overall Verification Steps

1. **Frontend Test Structure:**
   - Run `cd apps/web && find tests/unit -name "*.test.tsx" | wc -l` → Should return 13
   - Run `cd apps/web && find tests/unit -name "*.test.ts" | wc -l` → Should return 2
   - Run `cd apps/web && pnpm test --run` → All tests should run (even if placeholder)

2. **Backend Test Structure:**
   - Run `test -d apps/api/tests/integration/bulk_upload` → Should succeed
   - Run `cd apps/api && uv run pytest tests/integration/bulk_upload/ --collect-only` → Should discover tests

3. **E2E Test Structure:**
   - Run `test -d apps/web/tests/e2e/upload` → Should succeed
   - Run `ls apps/web/tests/e2e/upload/*.spec.ts` → Should list at least 1 file

4. **Vitest Config Validation:**
   - Run `cd apps/web && pnpm test tests/unit/components/datagrid/DataGrid.test.tsx --run` → Should run without "file not found" error
   - Run `cd apps/web && pnpm test tests/unit/hooks/useDataGrid.test.ts --run` → Should run without error

5. **Test File Content:**
   - Run `grep -r "describe(" apps/web/tests/unit/` → Should find describe blocks in all files
   - Run `grep -r "TODO:" apps/web/tests/unit/ | wc -l` → Should show placeholder tests
</verification>

<success_criteria>
1. All 16 test stub files exist (13 component + 2 hook + 1 E2E directory)
2. Vitest can run all frontend test files without "file not found" errors
3. Pytest can discover integration test directory
4. E2E test directory structure exists with spec files
5. All test files have placeholder describe/it blocks
6. No Wave 0 tasks reference MISSING test files in later plans
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-shell-vehicle-management/08-00-SUMMARY.md`
</output>
