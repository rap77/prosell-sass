---
phase: 01-hybrid-publisher
task: OAuth Cookie Auth Issue
total_tasks: 3
completed_tasks: 1
status: blocked
last_updated: 2026-03-24T17:23:00Z
---

<current_state>
**Session 2026-03-24**: OAuth login funciona pero publisher endpoint devuelve 401 Unauthorized.

**Lo que funciona**:
- ✅ OAuth Google login: redirige correctamente, crea usuario y oauth_account en DB
- ✅ Cookies se setean correctamente: access_token y user_data con path=/ y SameSite=Lax
- ✅ Usuario puede navegar al dashboard y ver catálogo
- ✅ Tabla users con schema completo (18 columnas) tras migración Alembic

**El problema**:
- ❌ Publisher endpoint POST /api/v1/publisher/{product_id}/publish devuelve 401 Unauthorized
- ❌ Frontend tiene credentials: "include" configurado
- ❌ Cookies existen en navegador (Application → Cookies → localhost) pero NO se envían en request POST
- ❌ Modal muestra "Not authenticated" al intentar publicar

**Diagnóstico actual**:
El browser tiene los cookies pero no los envía al backend. El problema es probablemente CORS o configuración de cookies (domain, path, etc).
</current_state>

<completed_work>

### Session 2026-03-24

1. **Database Migration Fix** ✅
   - Problema: Tabla users tenía solo 5 columnas, el modelo esperaba 18
   - Solución: Creada migración Alembic `20260324_0828-20f24e79033e_recreate_users_table_complete`
   - Drop users table CASCADE, recreada con schema completo
   - Insertado usuario test: rafael.padron@gmail.com (id: e1871fb7-cf0e-4374-a4ff-89809adffc4e)
   - Ejecutado: `uv run alembic upgrade head`

2. **OAuth Cookie Path Fix** ✅
   - Problema: Cookies se seteaban sin path, solo disponibles para /api/auth/*
   - Solución: Agregado `path="/"` a ambos set_cookie (access_token y user_data)
   - Archivo: apps/api/src/prosell/infrastructure/api/routers/auth_router.py
   - Líneas 422 y 431 modificadas

3. **Contenedores Docker** ✅
   - Restart: docker start prosell-db prosell-redis
   - Estaban stopped (Exited 255), ahora corriendo

4. **OAuth Flow Test** ✅
   - Login con Google funciona
   - Usuario se crea correctamente en DB
   - OAuth account se crea con tokens de Google
   - Redirección al dashboard funciona

</completed_work>

<remaining_work>

### Bloqueo Actual: Publisher 401 Unauthorized

**Síntoma**:
- Modal muestra "Not authenticated" al hacer click en "Publicar en Facebook"
- Console: `POST http://localhost:8000/api/v1/publisher/veh-001/publish 401 (Unauthorized)`

**Verificado**:
- ✅ Cookies existen en navegador (localhost, path=/, SameSite=Lax)
- ✅ Frontend tiene `credentials: "include"` en publisherApi.ts
- ✅ Backend CORS tiene `allow_credentials=True` y allowed_origins incluye localhost:3000
- ✅ JWT token válido (exp: 2026-03-31, hoy: 2026-03-24)

**Por investigar**:
1. ¿El browser envía Cookie header en la request POST? (Network tab → Request Headers)
2. ¿El backend recibe el cookie? (logs del API)
3. ¿Hay algún middleware que esté bloqueando?
4. ¿Es un problema de domain (localhost vs 127.0.0.1)?

**Próximos pasos**:
1. Verificar en DevTools Network tab si Cookie se envía
2. Si NO se envía: problema de browser/CORS
3. Si SÍ se envía: problema de validación en backend
4. Revisar get_current_auth_user_from_cookie dependency

</remaining_work>

<decisions_made>

1. **Database**: Recrear schema vía SQL directo + Alembic tracking más limpio que intentar arreglar 8 migraciones rotas
2. **OAuth callbacks**: Sin versión (/api/auth/*) porque Google Cloud Console se configura una vez
3. **Auth API**: Con versión (/api/v1/auth/*) para evolución backward-compatible
4. **Development sobre producción**: En development usamos drop/recreate, en producción usaría migraciones incrementales

</decisions_made>

<blockers>

- **401 Unauthorized en publisher endpoint**: Cookies existen pero no se envían o no se validan correctamente
  - Estado: Investigando
  - Workaround: None - bloquea UAT completo

</blockers>

<context>

**El flujo completo hasta ahora**:
1. Usuario hace click en "Continue with Google" → redirige a Google OAuth
2. Usuario se autentica en Google → callback a /api/auth/oauth/google/callback
3. Backend crea/actualiza usuario y oauth_account, genera JWT
4. Backend setea cookies access_token y user_data con path=/
5. Usuario redirigido al dashboard (funciona ✅)
6. Usuario navega a /dashboard/catalog (funciona ✅)
7. Usuario hace click en "Preparar Publicación" (modal abre ✅)
8. Usuario llena formulario y hace click en "Publicar en Facebook"
9. Frontend llama POST /api/v1/publisher/veh-001/publish con credentials: include
10. Backend devuelve 401 Unauthorized ❌

**Hipótesis actual**: El browser tiene los cookies pero decide no enviarlos en el POST request. Puede ser:
- Problema de SameSite (aunque es Lax, que debería permitir)
- Problema de domain (localhost vs 127.0.0.1 vs IP)
- Problema de CORS preflight
- Problema de validación del JWT en backend

</context>

<next_action>

**Para desbloquear**:

1. **Verificar Request Headers**:
   - Abrir DevTools → Network tab
   - Buscar request `publish` que falló (401)
   - Verificar si `Cookie: access_token=...` está en Request Headers
   - Si NO está: problema del browser/CORS
   - Si SÍ está: problema del backend

2. **Si Cookie NO se envía**:
   - Verificar si es problema de domain (localhost vs 127.0.0.1)
   - Probar agregar `domain="localhost"` a set_cookie
   - Verificar configuración CORS en main.py

3. **Si Cookie SÍ se envía**:
   - Verificar logs del API para ver qué está pasando
   - Revisar get_current_auth_user_from_cookie dependency
   - Verificar si JWT se valida correctamente

**Para continuar sesión**:
```
/gsd:resume-work
```

</next_action>
