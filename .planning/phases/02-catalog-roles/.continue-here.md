---
phase: 02-catalog-roles
task: Maintenance
total_tasks: N/A
status: in_progress
last_updated: 2026-04-06T01:12:34.323Z
---

<current_state>
Finished fixing login E2E tests and created placeholder pages for terms/privacy to avoid 404 errors. Currently in maintenance mode, ready to continue with register E2E tests or other tasks.
</current_state>

<completed_work>
- ✅ Fixed login E2E tests: All 12 tests now passing (were 12 passing, 12 failing due to double execution)
  - Root cause: Tests were running in both `auth-tests` (clean) and `chromium` (with persistent cookies) projects
  - Solution: Modified playwright.config.ts to restrict auth tests to only run in `auth-tests` project
  - Changes: Updated testMatch/testIgnore in projects configuration

- ✅ Created placeholder pages for missing routes to prevent 404 errors:
  - `/apps/web/src/app/terms/page.tsx` - Terms of Service placeholder
  - `/apps/web/src/app/privacy/page.tsx` - Privacy Policy placeholder
  - These were referenced in the login page footer but didn't exist, causing 404s when clicked
</completed_work>

<remaining_work>
- ▢ Investigate and fix register E2E tests (likely same double execution issue)
- ▢ Investigate and fix middleware E2E tests (protected route tests)
- ▢ Continue with vehicle-form VIN select/MPG/image upload fixes verification
- ▢ Consider running full E2E test suite to see overall status
</remaining_work>

<decisions_made>
- Decided to fix the root cause (test configuration) rather than workaround individual test flakiness
  - Reason: Clean separation of concerns - auth tests should start fresh, other tests can use persisted state

- Chose to create simple placeholder pages for terms/privacy rather than implementing full pages
  - Reason: These are legal documents that require proper review; placeholders allow UI testing to proceed without blocking on legal completion
</decisions_made>

<blockers>
- None currently blocked. Waiting on next task assignment.
</blockers>

<context>
Just completed fixing the login test flakiness issue which was causing confusion in test results. The root cause was test configuration allowing auth tests to run in both clean and persistent cookie states. Fixed by properly segregating test projects. Also addressed immediate UI blockers (missing terms/privacy pages) that were causing 404s in the login flow.

Approach has been: identify root cause of test flakiness → fix configuration → verify tests pass → address any blocking UI issues → move to next task.
</context>

<next_action>
Start with: Investigate register E2E tests (task #3) - likely same issue as login tests (double execution due to test configuration)
</next_action>
