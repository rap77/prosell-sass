---
phase: 02-catalog-roles
plan: 04
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - apps/api/src/prosell/application/dto/user_dealer.py
  - apps/api/src/prosell/application/use_cases/user_dealer/assign_user_dealer.py
  - apps/api/src/prosell/infrastructure/api/routers/user_dealer_router.py
  - apps/api/src/prosell/infrastructure/api/main.py
  - apps/api/tests/integration/api/test_user_dealer_api.py
autonomous: true
requirements: [CATALOG-07]
must_haves:
  truths:
    - "POST /api/users/{id}/dealers assigns dealer to user with audit trail"
    - "POST /api/users/bulk-assign assigns multiple users to dealers"
    - "DELETE /api/users/{id}/dealers/{dealer_id} removes assignment"
    - "GET /api/users/{id}/dealers lists user's assigned dealers"
    - "Admin/Manager role required for assignment operations"
  artifacts:
    - path: "apps/api/src/prosell/application/dto/user_dealer.py"
      provides: "UserDealer DTOs (AssignDealerRequest, UserDealerResponse)"
      exports: ["AssignDealerRequest", "BulkAssignRequest", "UserDealerResponse"]
    - path: "apps/api/src/prosell/application/use_cases/user_dealer/assign_user_dealer.py"
      provides: "AssignUserDealerUseCase, BulkAssignUseCase"
      contains: "class AssignUserDealerUseCase"
    - path: "apps/api/src/prosell/infrastructure/api/routers/user_dealer_router.py"
      provides: "FastAPI router for user-dealer assignment"
      exports: ["POST /api/users/{id}/dealers", "POST /api/users/bulk-assign", "DELETE /api/users/{id}/dealers/{dealer_id}"]
  key_links:
    - from: "user_dealer_router"
      to: "AssignUserDealerUseCase"
      via: "Dependency injection"
      pattern: "Depends\(get_assign_user_dealer_usecase\)"
    - from: "AssignUserDealerUseCase"
      to: "AbstractUserDealerRepository"
      via: "Use case constructor"
      pattern: "def __init__\(self, user_dealer_repository: AbstractUserDealerRepository\)"
---

<objective>
Implement the UserDealer assignment API endpoints (assign, bulk assign, remove, list) with admin/manager-only access, audit trail tracking, and tenant isolation. This enables admin and manager users to manage which sellers work with which dealers.

Purpose: Provide REST API for M:N user-dealer assignment operations with proper authorization and audit logging.
Output: Working user-dealer assignment API endpoints with integration tests GREEN.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-roles/02-CONTEXT.md
@.planning/phases/02-catalog-roles/02-RESEARCH.md
@.planning/phases/02-catalog-roles/02-02-PLAN.md (depends on UserDealer entity and repository)

# Existing patterns from Phase 01 (FastAPI routers with DI)
```python
# Source: FastAPI router pattern
from fastapi import APIRouter, Depends, HTTPException

router = APIRouter(prefix="/api/users", tags=["users"])

@router.post("/{id}/dealers", response_model=UserDealerResponse, status_code=201)
async def assign_dealer(
    id: UUID,
    request: AssignDealerRequest,
    current_user: Annotated[User, Depends(get_current_user)],
    use_case: Annotated[AssignUserDealerUseCase, Depends(get_assign_user_dealer_use_case)],
) -> UserDealerResponse:
    # Role check: admin or manager only
    if not current_user.has_role(["admin", "manager"]):
        raise HTTPException(status_code=403, detail="Admin or manager role required")
    return await use_case.execute(id, request, current_user.id)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UserDealer DTOs</name>
  <files>apps/api/src/prosell/application/dto/user_dealer.py</files>
  <behavior>
    - Test 1: AssignDealerRequest validates dealer_id field
    - Test 2: BulkAssignRequest validates user_ids and dealer_ids arrays
    - Test 3: UserDealerResponse includes user_id, dealer_id, assigned_at, assigned_by
    - Test 4: UserDealerListResponse includes items array
  </behavior>
  <action>
    Create UserDealer DTOs in application/dto/user_dealer.py:
    - AssignDealerRequest (Pydantic): dealer_id (UUID)
    - BulkAssignRequest (Pydantic): user_ids (list[UUID], min 1), dealer_ids (list[UUID], min 1)
    - UserDealerResponse (Pydantic): id, user_id, dealer_id, tenant_id, assigned_at, assigned_by
    - UserDealerListResponse (Pydantic): items (list[UserDealerResponse]), total (int)

    Follow Dealer DTO pattern from Plan 02-03
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_user_dealer_api.py::test_user_dealer_dtos -xvs</automated>
  </verify>
  <done>UserDealer DTOs created with validation, 4 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AssignUserDealerUseCase</name>
  <files>apps/api/src/prosell/application/use_cases/user_dealer/assign_user_dealer.py</files>
  <behavior>
    - Test 1: execute() assigns dealer to user via repository
    - Test 2: Audit fields populated (assigned_at=now, assigned_by=current_user_id)
    - Test 3: Duplicate assignment raises error (unique constraint)
    - Test 4: Returns UserDealerResponse with all fields
  </behavior>
  <action>
    Create AssignUserDealerUseCase in application/use_cases/user_dealer/assign_user_dealer.py:
    - Constructor: Inject AbstractUserDealerRepository, AbstractDealerRepository
    - execute(user_id: UUID, request: AssignDealerRequest, tenant_id: UUID, assigned_by: UUID) -> UserDealerResponse
    - Validation: Check dealer exists via dealer_repository.get_by_id()
    - Creation: Call UserDealer.assign() with audit fields
    - Persistence: repository.assign()
    - Return: Map UserDealer entity to UserDealerResponse
    - Handle unique constraint violation (duplicate assignment)

    Follow CreateDealerUseCase pattern from Plan 02-03
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_user_dealer_api.py::test_assign_user_dealer_usecase -xvs</automated>
  </verify>
  <done>AssignUserDealerUseCase implemented with audit trail, 4 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement BulkAssignUseCase and RemoveUserDealerUseCase</name>
  <files>apps/api/src/prosell/application/use_cases/user_dealer/bulk_assign.py, apps/api/src/prosell/application/use_cases/user_dealer/remove_user_dealer.py</files>
  <behavior>
    - Test 1: BulkAssignUseCase assigns multiple users to multiple dealers
    - Test 2: BulkAssignUseCase returns count of successful assignments
    - Test 3: RemoveUserDealerUseCase deletes assignment
    - Test 4: RemoveUserDealerUseCase is idempotent (no error if already removed)
  </behavior>
  <action>
    Create BulkAssignUseCase in application/use_cases/user_dealer/bulk_assign.py:
    - execute(request: BulkAssignRequest, tenant_id: UUID, assigned_by: UUID) -> dict with "assigned_count"
    - Loop through user_ids × dealer_ids Cartesian product
    - Call AssignUserDealerUseCase for each combination
    - Skip duplicates (catch unique constraint errors)
    - Return summary with successful assignment count

    Create RemoveUserDealerUseCase in application/use_cases/user_dealer/remove_user_dealer.py:
    - execute(user_id: UUID, dealer_id: UUID, tenant_id: UUID) -> None
    - Call repository.remove(user_id, dealer_id, tenant_id)
    - No return value (204 No Content in API)

    Follow use case pattern from Plan 02-03
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_user_dealer_api.py::test_bulk_and_remove_usecases -xvs</automated>
  </verify>
  <done>BulkAssign and Remove use cases implemented, 4 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create user_dealer_router with assignment endpoints</name>
  <files>apps/api/src/prosell/infrastructure/api/routers/user_dealer_router.py</files>
  <behavior>
    - Test 1: POST /api/users/{id}/dealers assigns dealer (201)
    - Test 2: POST /api/users/bulk-assign assigns multiple (200)
    - Test 3: DELETE /api/users/{id}/dealers/{dealer_id} removes (204)
    - Test 4: GET /api/users/{id}/dealers lists assignments (200)
    - Test 5: Admin/Manager-only access enforced (403 for sellers)
  </behavior>
  <action>
    Create user_dealer_router in infrastructure/api/routers/user_dealer_router.py:
    - Router prefix: "/api/users", tag: "user-dealers"
    - POST /api/users/{id}/dealers: AssignUserDealerUseCase, response_model=UserDealerResponse, status_code=201
      - Role check: user.has_role(["admin", "manager"]) or 403
      - Pass assigned_by=current_user.id for audit trail
    - POST /api/users/bulk-assign: BulkAssignUseCase, response_model=dict (assigned_count)
      - Role check: admin or manager only
    - DELETE /api/users/{id}/dealers/{dealer_id}: RemoveUserDealerUseCase, status_code=204
      - Role check: admin or manager only
    - GET /api/users/{id}/dealers: ListUserDealersUseCase, response_model=UserDealerListResponse
      - No role check (users can see their own assignments)
      - Tenant isolation: pass user.tenant_id
    - Use Depends() for DI on all use cases
    - Use Annotated[User, Depends(get_current_user)] for auth

    Follow dealer_router pattern from Plan 02-03
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_user_dealer_api.py::test_user_dealer_router_endpoints -xvs</automated>
  </verify>
  <done>User-dealer router with 4 endpoints, admin/manager access, 5 tests GREEN</done>
</task>

<task type="auto">
  <name>Task 5: Wire user_dealer_router into main FastAPI app</name>
  <files>apps/api/src/prosell/infrastructure/api/main.py</files>
  <action>
    Import user_dealer_router and include in FastAPI app:
    - from prosell.infrastructure.api.routers.user_dealer_router import router as user_dealer_router
    - app.include_router(user_dealer_router)
    - Place after dealer_router include

    Verify with: curl http://localhost:8000/api/docs (shows /api/users endpoints)
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/api/docs | grep -o "/api/users" | wc -l</automated>
  </verify>
  <done>User-dealer router wired into main app, endpoints visible in /api/docs</done>
</task>

</tasks>

<verification>
- POST /api/users/{id}/dealers assigns dealer with audit trail
- POST /api/users/bulk-assign handles multiple assignments
- DELETE /api/users/{id}/dealers/{dealer_id} removes assignment
- GET /api/users/{id}/dealers lists user's assigned dealers
- Admin/Manager-only access enforced (403 for sellers)
- All integration tests GREEN (4 DTO + 8 use case + 5 router = 17 tests)
- Wave 0 xfail stubs now GREEN for Plan 02-04 tests
</verification>

<success_criteria>
- [ ] UserDealer DTOs in application/dto/user_dealer.py
- [ ] AssignUserDealerUseCase with audit trail
- [ ] BulkAssignUseCase and RemoveUserDealerUseCase
- [ ] user_dealer_router with 4 endpoints
- [ ] Admin/Manager-only access enforced
- [ ] Router wired into main FastAPI app
- [ ] 17 integration tests GREEN
- [ ] Plan 02-06 can proceed (Frontend UserDealerAssignment depends on these endpoints)
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-roles/02-04-SUMMARY.md`
</output>
