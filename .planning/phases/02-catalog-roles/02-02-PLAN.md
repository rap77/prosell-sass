---
phase: 02-catalog-roles
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - apps/api/src/prosell/domain/entities/user_dealer.py
  - apps/api/src/prosell/domain/repositories/user_dealer_repository.py
  - apps/api/src/prosell/infrastructure/models/user_dealer_model.py
  - apps/api/src/prosell/infrastructure/repositories/user_dealer_repository_impl.py
  - apps/api/alembic/versions/20260329_1500-add_user_dealers_table.py
  - apps/api/tests/unit/domain/test_user_dealer_entity.py
  - apps/api/tests/integration/repositories/test_user_dealer_repository.py
autonomous: true
requirements: [CATALOG-07]
must_haves:
  truths:
    - "UserDealer entity represents M:N relationship with audit fields"
    - "Composite unique constraint on (user_id, dealer_id) prevents duplicates"
    - "Repository provides assign(), remove(), get_user_dealer_ids() methods"
    - "Junction table model has FKs to users and dealers with CASCADE delete"
    - "Alembic migration creates user_dealers table with indexes"
  artifacts:
    - path: "apps/api/src/prosell/domain/entities/user_dealer.py"
      provides: "UserDealer domain entity (value object)"
      min_lines: 30
      exports: ["UserDealer"]
    - path: "apps/api/src/prosell/infrastructure/models/user_dealer_model.py"
      provides: "UserDealerModel junction table"
      contains: "class UserDealerModel"
    - path: "apps/api/src/prosell/domain/repositories/user_dealer_repository.py"
      provides: "AbstractUserDealerRepository interface"
      exports: ["assign", "remove", "get_user_dealer_ids"]
  key_links:
    - from: "UserDealer entity"
      to: "UserDealerModel"
      via: "Repository mapper"
      pattern: "def _to_entity\(self, model: UserDealerModel\)"
    - from: "UserDealerModel"
      to: "users and dealers tables"
      via: "Foreign keys with CASCADE delete"
      pattern: "ForeignKey.*users.id.*ondelete=CASCADE"
---

<objective>
Implement the UserDealer M:N relationship entity with junction table, repository for assignment/removal operations, and database migration. This enables ProSell users (sellers/managers) to be assigned to multiple dealers with audit trail.

Purpose: Support the business requirement that one seller can work for multiple organizations (dealers), with historical tracking of assignments.
Output: Working UserDealer entity with repository, tests GREEN, and user_dealers junction table in PostgreSQL.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-roles/02-CONTEXT.md
@.planning/phases/02-catalog-roles/02-RESEARCH.md
@.planning/phases/02-catalog-roles/02-01-PLAN.md (depends on Dealer entity)

# Existing patterns from Phase 01 (TeamMember reference)
```python
# Source: TeamMember M:N pattern
class UserDealerModel(Base):
    __tablename__ = "user_dealers"

    user_id: Mapped[UUID] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"))
    dealer_id: Mapped[UUID] = mapped_column(ForeignKey("dealers.id", ondelete="CASCADE"))
    assigned_at: Mapped[datetime] = mapped_column(server_default="now()")
    assigned_by: Mapped[UUID] = mapped_column(ForeignKey("users.id", ondelete="SET NULL"))
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UserDealer domain entity</name>
  <files>apps/api/src/prosell/domain/entities/user_dealer.py</files>
  <behavior>
    - Test 1: UserDealer entity created with user_id, dealer_id, tenant_id
    - Test 2: Audit fields assigned_at, assigned_by are populated
    - Test 3: Entity represents read-only value object (no update methods)
  </behavior>
  <action>
    Create UserDealer entity in domain/entities/user_dealer.py:
    - Identity fields: id (UUID), user_id (UUID), dealer_id (UUID), tenant_id (UUID)
    - Audit fields: assigned_at (datetime), assigned_by (UUID | None)
    - Read-only value object: No update methods (assignments create new records)
    - Inherit from DomainModel (Pydantic BaseModel)
    - Factory method: UserDealer.assign(user_id, dealer_id, tenant_id, assigned_by)

    Follow value object pattern (immutable, no state transitions)
  </action>
  <verify>
    <automated>pytest apps/api/tests/unit/domain/test_user_dealer_entity.py -xvs</automated>
  </verify>
  <done>UserDealer entity created with audit fields, 3 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AbstractUserDealerRepository interface</name>
  <files>apps/api/src/prosell/domain/repositories/user_dealer_repository.py</files>
  <behavior>
    - Test 1: Interface defines assign(), remove(), get_user_dealer_ids() methods
    - Test 2: Interface includes get_dealer_users() for reverse lookup
  </behavior>
  <action>
    Create AbstractUserDealerRepository in domain/repositories/user_dealer_repository.py:
    - Abstract methods: assign(), remove(), get_user_dealer_ids(), get_dealer_users()
    - Query methods: exists(), get_assignment()
    - All methods accept tenant_id for multi-tenancy
    - Return type hints: UserDealer for assign/get, list[UUID] for ID queries, bool for exists
    - Inherit from ABC

    Follow AbstractDealerRepository pattern from Plan 02-01
  </action>
  <verify>
    <automated>pytest apps/api/tests/unit/domain/test_user_dealer_entity.py::test_user_dealer_repository_interface -xvs</automated>
  </verify>
  <done>AbstractUserDealerRepository interface defined with 6 abstract methods</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create UserDealerModel SQLAlchemy ORM</name>
  <files>apps/api/src/prosell/infrastructure/models/user_dealer_model.py</files>
  <behavior>
    - Test 1: Model maps UserDealer entity to junction table
    - Test 2: Composite unique constraint on (user_id, dealer_id) prevents duplicates
    - Test 3: FKs to users and dealers with CASCADE delete
    - Test 4: Indexes on user_id, dealer_id for subquery performance
  </behavior>
  <action>
    Create UserDealerModel in infrastructure/models/user_dealer_model.py:
    - Table name: "user_dealers"
    - Mapped columns: id (UUID PK), user_id (UUID FK), dealer_id (UUID FK), tenant_id (UUID FK)
    - Audit columns: assigned_at (DateTime, server_default="now()"), assigned_by (UUID FK, nullable)
    - Foreign keys: users.id (CASCADE), dealers.id (CASCADE), users.id (SET NULL for assigned_by)
    - Indexes: ix_user_dealers_user_id, ix_user_dealers_dealer_id, ix_user_dealers_user_dealer (unique composite)
    - Use Mapped[] and mapped_column() pattern
    - Import in models/__init__.py for Alembic detection

    Follow TeamMember M:N pattern from existing codebase
  </action>
  <verify>
    <automated>pytest apps/api/tests/unit/domain/test_user_dealer_entity.py::test_user_dealer_model_columns -xvs</automated>
  </verify>
  <done>UserDealerModel created with FKs, indexes, and unique constraint</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement SqlAlchemyUserDealerRepository</name>
  <files>apps/api/src/prosell/infrastructure/repositories/user_dealer_repository_impl.py</files>
  <behavior>
    - Test 1: assign() creates UserDealer with audit fields
    - Test 2: remove() deletes assignment (preserves no history, keeps table clean)
    - Test 3: get_user_dealer_ids() returns list of dealer IDs for user
    - Test 4: exists() checks for existing assignment
    - Test 5: Mapper converts entity↔model bidirectionally
  </behavior>
  <action>
    Create SqlAlchemyUserDealerRepository in infrastructure/repositories/user_dealer_repository_impl.py:
    - Constructor accepts AsyncSession
    - assign(): Create UserDealerModel, session.add(), session.flush(), return entity
    - remove(): Delete by (user_id, dealer_id, tenant_id)
    - get_user_dealer_ids(): Select dealer_id WHERE user_id + tenant_id, return list of UUIDs
    - get_dealer_users(): Reverse lookup for dealer's assigned users
    - exists(): Use func.count() for efficient existence check
    - Mapper methods: _to_entity(), _to_model()
    - Use select() pattern with tenant isolation

    Follow SqlAlchemyDealerRepository pattern from Plan 02-01
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/repositories/test_user_dealer_repository.py -xvs</automated>
  </verify>
  <done>SqlAlchemyUserDealerRepository implements all 6 abstract methods, 5 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create Alembic migration for user_dealers table</name>
  <files>apps/api/alembic/versions/20260329_1500-add_user_dealers_table.py</files>
  <behavior>
    - Test 1: Migration creates user_dealers junction table
    - Test 2: FKs to users and dealers with CASCADE delete
    - Test 3: Unique constraint on (user_id, dealer_id) enforced
    - Test 4: Downgrade drops table cleanly
  </behavior>
  <action>
    Create Alembic migration 20260329_1500-add_user_dealers_table.py:
    - upgrade(): create_table('user_dealers') with id, user_id (FK users.id CASCADE), dealer_id (FK dealers.id CASCADE), tenant_id, assigned_at, assigned_by (FK users.id SET NULL)
    - Create unique constraint: uq_user_dealers_user_dealer on (user_id, dealer_id)
    - Create indexes: ix_user_dealers_user_id, ix_user_dealers_dealer_id
    - downgrade(): drop_table('user_dealers')
    - Apply migration: alembic upgrade head

    Verify in PostgreSQL: \d user_dealers (shows FK references)
  </action>
  <verify>
    <automated>psql -U prosell -d prosell -c "\d user_dealers" | grep -E "(user_id|dealer_id|foreign)" | wc -l</automated>
  </verify>
  <done>Migration applied, user_dealers table exists with FKs, 4 tests GREEN</done>
</task>

</tasks>

<verification>
- UserDealer entity represents M:N relationship with audit fields
- Composite unique constraint prevents duplicate assignments
- Repository provides assign/remove/get_user_dealer_ids operations
- Junction table model has CASCADE FKs to users and dealers
- Alembic migration creates user_dealers table
- All unit tests GREEN (3 entity + 5 repo = 8 tests)
- Wave 0 xfail stubs now GREEN for Plan 02-02 tests
</verification>

<success_criteria>
- [ ] UserDealer entity in domain/entities/user_dealer.py
- [ ] AbstractUserDealerRepository interface with 6 abstract methods
- [ ] UserDealerModel junction table with FKs and unique constraint
- [ ] SqlAlchemyUserDealerRepository implements interface
- [ ] Alembic migration creates user_dealers table
- [ ] 8 unit tests GREEN (3 entity + 5 repo)
- [ ] PostgreSQL user_dealers table verified with \d user_dealers
- [ ] Plan 02-03 can proceed (API endpoints depend on UserDealer)
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-roles/02-02-SUMMARY.md`
</output>
