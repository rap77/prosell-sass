---
phase: 02-catalog-roles
plan: 01
type: execute
wave: 1
depends_on: [02-00]
files_modified:
  - apps/api/src/prosell/domain/entities/dealer.py
  - apps/api/src/prosell/domain/repositories/dealer_repository.py
  - apps/api/src/prosell/domain/exceptions/dealer_exceptions.py
  - apps/api/src/prosell/infrastructure/models/dealer_model.py
  - apps/api/src/prosell/infrastructure/repositories/dealer_repository_impl.py
  - apps/api/alembic/versions/20260329_1400-add_dealers_table.py
  - apps/api/tests/unit/domain/test_dealer_entity.py
autonomous: true
requirements: [CATALOG-05]
must_haves:
  truths:
    - "Dealer entity exists with all production-ready fields (location, timezone, settings)"
    - "Dealer.create() factory method generates slug from name (editable)"
    - "Slug is unique per tenant_id (validated in repository)"
    - "DealerModel SQLAlchemy ORM maps all entity fields with indexes"
    - "Alembic migration creates dealers table with unique constraint on (tenant_id, slug)"
  artifacts:
    - path: "apps/api/src/prosell/domain/entities/dealer.py"
      provides: "Dealer domain entity"
      min_lines: 50
      exports: ["Dealer", "Dealer.create"]
    - path: "apps/api/src/prosell/domain/repositories/dealer_repository.py"
      provides: "AbstractDealerRepository interface"
      exports: ["AbstractDealerRepository"]
    - path: "apps/api/src/prosell/infrastructure/models/dealer_model.py"
      provides: "DealerModel SQLAlchemy ORM"
      contains: "class DealerModel"
  key_links:
    - from: "Dealer entity"
      to: "DealerModel"
      via: "Repository mapper methods (_to_entity, _to_model)"
      pattern: "def _to_entity\(self, model: DealerModel\)"
    - from: "DealerModel"
      to: "PostgreSQL dealers table"
      via: "Alembic migration"
      pattern: "def upgrade\(\):.*create_table.*dealers"
---

<objective>
Implement the Dealer (Organization) domain entity with full production-ready fields, repository interface, SQLAlchemy ORM model, and database migration. This establishes the core entity for managing multi-tenant dealers with role-based catalog access.

Purpose: Enable admin users to create dealer organizations without user accounts, providing the foundation for M:N seller-dealer assignments.
Output: Working Dealer entity with repository, tests GREEN, and dealers table in PostgreSQL.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-roles/02-CONTEXT.md
@.planning/phases/02-catalog-roles/02-RESEARCH.md
@.planning/phases/01-hybrid-publisher/01-01-publication-entity-PLAN.md (TDD pattern reference)

# Existing patterns from Phase 01
```python
# Source: Publication entity pattern
from prosell.domain.base import DomainModel
from uuid import UUID, uuid4
from datetime import UTC, datetime

class Publication(DomainModel):
    id: UUID
    # ... fields

    @classmethod
    def create(cls, ...) -> "Publication":
        return cls(id=uuid4(), ...)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Dealer domain entity and exceptions</name>
  <files>apps/api/src/prosell/domain/entities/dealer.py, apps/api/src/prosell/domain/exceptions/dealer_exceptions.py</files>
  <behavior>
    - Test 1: Dealer.create() generates valid entity with UUID, timestamp, slug from name
    - Test 2: Slug validation fails if empty or invalid format
    - Test 3: Timezone defaults to "America/Montevideo"
    - Test 4: Location coordinates (lat/lng) accept None or valid float values
    - Test 5: Settings field accepts dict for flexible configuration
  </behavior>
  <action>
    Create Dealer entity in domain/entities/dealer.py:
    - Identity fields: id (UUID), tenant_id (UUID), name (str), slug (str)
    - Location fields: location_address, location_city, location_state, location_zip, location_lat, location_lng (all optional)
    - Business fields: timezone (str, default "America/Montevideo"), settings (dict, default {})
    - Audit fields: created_at, updated_at (datetime, UTC)
    - Factory method: Dealer.create(name, tenant_id, slug=None) with auto-slug generation
    - Slug generation: lowercase, replace spaces with hyphens, remove special chars
    - Inherit from DomainModel (Pydantic BaseModel)

    Create domain exceptions:
    - DealerNotFound (raised when get_by_id returns None)
    - SlugNotUnique (raised when slug already exists in tenant)

    Follow Phase 01 Publication entity pattern (DomainModel inheritance, factory method)
  </action>
  <verify>
    <automated>pytest apps/api/tests/unit/domain/test_dealer_entity.py -xvs</automated>
  </verify>
  <done>Dealer entity created with all production fields, 5 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AbstractDealerRepository interface</name>
  <files>apps/api/src/prosell/domain/repositories/dealer_repository.py</files>
  <behavior>
    - Test 1: Interface defines abstract methods for CRUD operations
    - Test 2: Interface includes slug uniqueness check method
  </behavior>
  <action>
    Create AbstractDealerRepository in domain/repositories/dealer_repository.py:
    - Abstract methods: create(), get_by_id(), get_by_slug(), update(), delete()
    - Query methods: exists_by_slug(), list_by_tenant()
    - All methods accept tenant_id for multi-tenancy
    - Inherit from ABC (abstract base class)
    - Return type hints: Dealer entity for get methods, bool for exists_by_slug

    Follow IPublicationRepository pattern from Phase 01
  </action>
  <verify>
    <automated>pytest apps/api/tests/unit/domain/test_dealer_entity.py::test_dealer_repository_interface -xvs</automated>
  </verify>
  <done>AbstractDealerRepository interface defined with 7 abstract methods</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create DealerModel SQLAlchemy ORM</name>
  <files>apps/api/src/prosell/infrastructure/models/dealer_model.py</files>
  <behavior>
    - Test 1: Model maps all Dealer entity fields to PostgreSQL columns
    - Test 2: Indexes on tenant_id, slug for query performance
    - Test 3: Unique constraint on (tenant_id, slug) composite
  </behavior>
  <action>
    Create DealerModel in infrastructure/models/dealer_model.py:
    - Table name: "dealers"
    - Mapped columns: id (UUID PK), tenant_id (UUID, indexed), name (String), slug (String, indexed)
    - Location columns: location_address, location_city, location_state, location_zip, location_lat, location_lng (all nullable)
    - Business columns: timezone (String, default "America/Montevideo"), settings (JSONB, default {})
    - Audit columns: created_at, updated_at (DateTime, timezone=True)
    - Indexes: ix_dealers_tenant_id, ix_dealers_slug, ix_dealers_tenant_slug (unique composite)
    - Use Mapped[] and mapped_column() pattern from Phase 01
    - Import in models/__init__.py for Alembic autogenerate detection

    Follow PublicationModel pattern from Phase 01
  </action>
  <verify>
    <automated>pytest apps/api/tests/unit/domain/test_dealer_entity.py::test_dealer_model_columns -xvs</automated>
  </verify>
  <done>DealerModel created with all fields, indexes, and unique constraint</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement SqlAlchemyDealerRepository</name>
  <files>apps/api/src/prosell/infrastructure/repositories/dealer_repository_impl.py</files>
  <behavior>
    - Test 1: create() persists Dealer and returns entity with generated id
    - Test 2: get_by_slug() returns Dealer or None
    - Test 3: exists_by_slug() returns True/False for slug uniqueness
    - Test 4: list_by_tenant() returns all dealers for tenant
    - Test 5: Mapper methods convert entity↔model bidirectionally
  </behavior>
  <action>
    Create SqlAlchemyDealerRepository in infrastructure/repositories/dealer_repository_impl.py:
    - Constructor accepts AsyncSession (not session_factory)
    - Implement all abstract methods from AbstractDealerRepository
    - create(): Map entity to model, session.add(), session.flush(), return mapped entity
    - get_by_id(): Query by id + tenant_id, return None if not found
    - get_by_slug(): Query by slug + tenant_id with unique constraint
    - exists_by_slug(): Use func.count() for efficient existence check
    - update(): Fetch existing, update fields, flush, return entity
    - delete(): Delete by id + tenant_id
    - list_by_tenant(): Select all dealers by tenant_id
    - Mapper methods: _to_entity() (model_validate), _to_model() (manual mapping)
    - Use select() pattern from Phase 01 repositories

    Follow SqlAlchemyPublicationRepository pattern from Phase 01
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/repositories/test_dealer_repository.py -xvs</automated>
  </verify>
  <done>SqlAlchemyDealerRepository implements all 7 abstract methods, 5 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create Alembic migration for dealers table</name>
  <files>apps/api/alembic/versions/20260329_1400-add_dealers_table.py</files>
  <behavior>
    - Test 1: Migration creates dealers table with all columns
    - Test 2: Unique constraint on (tenant_id, slug) is enforced
    - Test 3: Indexes are created for tenant_id, slug, and composite
    - Test 4: Downgrade drops table cleanly
  </behavior>
  <action>
    Create Alembic migration 20260329_1400-add_dealers_table.py:
    - upgrade(): create_table('dealers') with all columns (UUID PK, tenant_id, name, slug, location fields, timezone, settings JSONB, created_at, updated_at)
    - Create unique constraint: uq_dealers_tenant_slug on (tenant_id, slug)
    - Create indexes: ix_dealers_tenant_id, ix_dealers_slug
    - downgrade(): drop_table('dealers')
    - Use op.create_table(), op.create_index(), op.drop_table()
    - Follow manual migration pattern from Phase 01 (no autogenerate)

    Apply migration: alembic upgrade head

    Verify in PostgreSQL: \d dealers (shows table schema)
  </action>
  <verify>
    <automated>psql -U prosell -d prosell -c "\d dealers" | grep -E "(tenant_id|slug|unique)" | wc -l</automated>
  </verify>
  <done>Migration applied, dealers table exists with unique constraint, 4 tests GREEN</done>
</task>

</tasks>

<verification>
- Dealer entity exists with all production fields (location, timezone, settings)
- Dealer.create() factory method auto-generates slug from name
- Slug uniqueness enforced at DB level (tenant_id + slug unique constraint)
- SqlAlchemyDealerRepository implements all abstract methods
- Alembic migration creates dealers table in PostgreSQL
- All unit tests GREEN (5 entity + 5 repo = 10 tests)
- Wave 0 xfail stubs now GREEN for Plan 02-01 tests
</verification>

<success_criteria>
- [ ] Dealer entity in domain/entities/dealer.py with all fields
- [ ] AbstractDealerRepository interface with 7 abstract methods
- [ ] DealerModel in infrastructure/models with Mapped[] pattern
- [ ] SqlAlchemyDealerRepository implements interface
- [ ] Alembic migration creates dealers table with unique (tenant_id, slug)
- [ ] 10 unit tests GREEN (5 entity + 5 repo)
- [ ] PostgreSQL dealers table verified with \d dealers
- [ ] Plan 02-02 can proceed (UserDealer depends on Dealer)
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-roles/02-01-SUMMARY.md`
</output>
