---
phase: 02-catalog-roles
plan: 06
type: execute
wave: 3
depends_on: [02-05]
files_modified:
  - apps/api/src/prosell/application/dto/vehicle.py
  - apps/api/src/prosell/infrastructure/repositories/vehicle_repository_impl.py
  - apps/api/tests/integration/api/test_vehicle_pagination.py
autonomous: true
requirements: [CATALOG-01, CATALOG-02, CATALOG-03]
must_haves:
  truths:
    - "Cursor-based pagination returns next_cursor and has_more flags"
    - "Pagination consistent across requests (no duplicates/skips)"
    - "Cursor encoding uses base64 for URL-safe transport"
    - "Pagination works with role-based filters applied"
    - "Order by dealer_id, then created_at DESC (CONTEXT decision)"
  artifacts:
    - path: "apps/api/src/prosell/application/dto/vehicle.py"
      provides: "CatalogResponse with pagination metadata"
      exports: ["CatalogResponse"]
    - path: "apps/api/src/prosell/infrastructure/repositories/vehicle_repository_impl.py"
      provides: "Cursor pagination logic"
      contains: "encode_cursor, decode_cursor"
  key_links:
    - from: "vehicle_repository"
      to: "CatalogResponse"
      via: "get_catalog_for_user returns (items, next_cursor, has_more)"
      pattern: "return vehicles, next_cursor, has_more"
---

<objective>
Implement cursor-based pagination for the vehicle catalog API, replacing any offset-based pagination with consistent, high-performance pagination that works correctly with role-based filtering and dynamic ordering.

Purpose: Provide O(1) performance for deep pages, consistent results (no duplicates/skips), and support for the CONTEXT decision to order by dealer_id, then created_at DESC.
Output: Working cursor pagination with integration tests GREEN.
</object>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-roles/02-CONTEXT.md
@.planning/phases/02-catalog-roles/02-RESEARCH.md
@.planning/phases/02-catalog-roles/02-05-PLAN.md (extends vehicle repository)

# Existing patterns from Phase 08 Brain #5 (cursor-based pagination)
```python
# Source: RESEARCH.md cursor pagination pattern
from base64 import urlsafe_b64encode, urlsafe_b64decode
from uuid import UUID

def encode_cursor(vehicle_id: UUID) -> str:
    """Encode vehicle ID as base64 cursor."""
    return urlsafe_b64encode(str(vehicle_id).encode()).decode().rstrip("=")

def decode_cursor(cursor: str) -> UUID:
    """Decode base64 cursor to vehicle ID."""
    padded = cursor + "=" * (-len(cursor) % 4)
    return UUID(urlsafe_b64decode(padded).decode())

class CatalogResponse(BaseModel):
    items: list[VehicleDTO]
    next_cursor: str | None
    has_more: bool
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cursor encoding/decoding utilities</name>
  <files>apps/api/src/prosell/infrastructure/repositories/vehicle_repository_impl.py</files>
  <behavior>
    - Test 1: encode_cursor() converts UUID to base64 string
    - Test 2: decode_cursor() converts base64 back to UUID
    - Test 3: Roundtrip encode/decode is lossless
    - Test 4: decode_cursor() handles padding correctly
  </behavior>
  <action>
    Add cursor utility functions in infrastructure/repositories/vehicle_repository_impl.py:
    - encode_cursor(vehicle_id: UUID) -> str
      - Convert UUID to string, encode to bytes, base64 urlsafe encode, decode to string, strip padding
      - Return URL-safe base64 string
    - decode_cursor(cursor: str) -> UUID
      - Add padding (=) to make length multiple of 4
      - base64 urlsafe decode, convert to string, parse as UUID
      - Return UUID object
    - Add error handling for invalid cursor format

    Follow RESEARCH.md "Cursor Pagination Helper" pattern
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_vehicle_pagination.py::test_cursor_encoding_decoding -xvs</automated>
  </verify>
  <done>Cursor encoding/decoding utilities, 4 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cursor pagination in get_catalog_for_user</name>
  <files>apps/api/src/prosell/infrastructure/repositories/vehicle_repository_impl.py</files>
  <behavior>
    - Test 1: First page request (no cursor) returns first N items
    - Test 2: Second page request (with cursor) returns next N items
    - Test 3: next_cursor is None when no more items
    - Test 4: has_more is True when more items exist, False on last page
    - Test 5: No duplicates across pages
    - Test 6: No skipped items across pages
  </behavior>
  <action>
    Modify get_catalog_for_user() in infrastructure/repositories/vehicle_repository_impl.py:
    - Add cursor parameter: cursor: str | None = None
    - Apply cursor filter: if cursor: stmt = stmt.where(VehicleModel.id > decode_cursor(cursor))
    - Ordering: ORDER BY ProductModel.organization_id, VehicleModel.created_at DESC, VehicleModel.id (CONTEXT decision)
    - Limit: stmt.limit(limit + 1) to fetch +1 for has_more check
    - Execute query: result = await self.session.execute(stmt)
    - Extract models: models = result.scalars().all()
    - Check has_more: has_more = len(models) > limit
    - Trim to limit: if has_more: models = models[:limit]
    - Generate next_cursor: next_cursor = encode_cursor(models[-1].id) if models else None
    - Return: (vehicles list, next_cursor, has_more)

    Follow RESEARCH.md "Pattern 3: Role-Based Query Filtering" with cursor addition
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_vehicle_pagination.py::test_cursor_pagination_consistency -xvs</automated>
  </verify>
  <done>Cursor pagination in repository, 6 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update CatalogResponse DTO with pagination metadata</name>
  <files>apps/api/src/prosell/application/dto/vehicle.py</files>
  <behavior>
    - Test 1: CatalogResponse includes items array
    - Test 2: CatalogResponse includes next_cursor field
    - Test 3: CatalogResponse includes has_more field
    - Test 4: Serialization produces valid JSON
  </behavior>
  <action>
    Create or update CatalogResponse in application/dto/vehicle.py:
    - items: list[VehicleDTO] (vehicles with publications array)
    - next_cursor: str | None (base64 cursor for next page)
    - has_more: bool (whether more pages exist)
    - Optional: total_count: int (for UI display, not required for pagination)

    If VehicleDTO doesn't exist with publications array, create it:
    - All vehicle fields: id, title, price_cents, make, model, year, condition
    - publications: list[PublicationDTO] (status, platform, published_at, expires_at)

    Follow DealerResponse pattern from Plan 02-03
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_vehicle_pagination.py::test_catalog_response_dto -xvs</automated>
  </verify>
  <done>CatalogResponse DTO with pagination metadata, 4 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test cursor pagination with role-based filters</name>
  <files>apps/api/tests/integration/api/test_vehicle_pagination.py</files>
  <behavior>
    - Test 1: Admin pagination with cursor works correctly
    - Test 2: Seller pagination (with dealer filter) works with cursor
    - Test 3: Dealer pagination (own vehicles only) works with cursor
    - Test 4: Pagination is consistent across different roles
    - Test 5: Cursor works with dynamic filters applied (make, status)
  </behavior>
  <action>
    Add integration tests in test_vehicle_pagination.py:
    - test_admin_cursor_pagination: Create 60 vehicles, request limit=20, verify 3 pages, no duplicates
    - test_seller_cursor_pagination: Assign seller to 2 dealers (30 vehicles each), verify cursor returns only assigned vehicles
    - test_dealer_cursor_pagination: Dealer owns 25 vehicles, verify cursor returns only own vehicles
    - test_cursor_with_filters: Apply make=Toyota filter, verify cursor works with filtered subset
    - test_cursor_ordering: Verify ORDER BY dealer_id, created_at DESC is respected

    Use pytest fixtures for test data (vehicles, dealers, users)
    Assert no duplicates: len(set(all_ids)) == total_count
    Assert no skips: all expected IDs present

    Follow integration test pattern from Plan 02-05
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_vehicle_pagination.py::test_cursor_with_role_filters -xvs</automated>
  </verify>
  <done>Cursor pagination with role filters, 5 tests GREEN</done>
</task>

</tasks>

<verification>
- Cursor encoding/decoding utilities work correctly
- Pagination returns next_cursor and has_more flags
- No duplicates or skips across pages
- Pagination works with role-based filters (admin, seller, dealer)
- Pagination works with dynamic filters (make, status)
- Ordering by dealer_id, then created_at DESC enforced
- All integration tests GREEN (4 encode + 6 pagination + 4 DTO + 5 filters = 19 tests)
- Wave 0 xfail stubs now GREEN for Plan 02-06 tests
</verification>

<success_criteria>
- [ ] encode_cursor() and decode_cursor() utilities in repository
- [ ] get_catalog_for_user() implements cursor pagination
- [ ] CatalogResponse DTO with next_cursor, has_more fields
- [ ] Ordering: dealer_id, created_at DESC
- [ ] Pagination tested with role-based filters
- [ ] Pagination tested with dynamic filters
- [ ] 19 integration tests GREEN
- [ ] Plan 02-07 can proceed (Dynamic filters build on this)
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-roles/02-06-SUMMARY.md`
</output>
