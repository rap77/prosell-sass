---
phase: 02-catalog-roles
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/tests/unit/domain/test_dealer_entity.py
  - apps/api/tests/unit/domain/test_user_dealer_entity.py
  - apps/api/tests/integration/repositories/test_dealer_repository.py
  - apps/api/tests/integration/repositories/test_user_dealer_repository.py
  - apps/api/tests/integration/api/test_dealer_endpoints.py
  - apps/api/tests/integration/api/test_vehicle_filtering.py
  - apps/api/tests/integration/api/test_vehicle_pagination.py
  - apps/api/tests/integration/api/test_dynamic_filters.py
  - apps/api/tests/integration/api/test_user_dealer_api.py
  - apps/web/tests/unit/hooks/useDealerFilters.test.ts
  - apps/web/tests/components/DealerForm.test.tsx
  - apps/web/tests/components/UserDealerAssignment.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All test stub files exist with pytest/vitest imports"
    - "Wave 0 stubs import only test framework (zero domain deps)"
    - "Test files marked with xfail reason='Not implemented yet — Plan 02-XX'"
  artifacts:
    - path: "apps/api/tests/unit/domain/test_dealer_entity.py"
      provides: "Dealer entity test stubs"
      min_lines: 15
    - path: "apps/api/tests/unit/domain/test_user_dealer_entity.py"
      provides: "UserDealer entity test stubs"
      min_lines: 15
    - path: "apps/api/tests/integration/api/test_vehicle_filtering.py"
      provides: "Role-based filtering test stubs"
      min_lines: 20
    - path: "apps/web/tests/components/DealerForm.test.tsx"
      provides: "DealerForm component stubs"
      min_lines: 15
  key_links:
    - from: "Wave 0 test stubs"
      to: "All subsequent plans"
      via: "xfail marks become GREEN as implementation completes"
      pattern: "@pytest.mark.xfail.*reason='Not implemented yet — Plan 02"
---

<objective>
Create all test stubs for Phase 02 (Catalog & Roles) following the TDD workflow established in Phase 01. These stubs serve as both test placeholders and executable documentation of the phase requirements.

Purpose: Enable TDD workflow where stubs start as xfail and turn GREEN as implementation plans execute.
Output: 12 test stub files (7 backend + 3 frontend + 2 integration) with pytest/vitest imports only.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-roles/02-CONTEXT.md
@.planning/phases/02-catalog-roles/02-RESEARCH.md
@.planning/phases/01-hybrid-publisher/01-00-wave0-infra-PLAN.md (Wave 0 pattern reference)

# Reusable patterns from Phase 01 Wave 0
```python
# Test stub pattern from 01-00
import pytest

@pytest.mark.xfail(reason="Not implemented yet — Plan 01-01")
def test_publication_entity_create():
    """Publication entity creates valid instance with all fields."""
    pytest.fail("stub")
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend domain entity test stubs</name>
  <files>apps/api/tests/unit/domain/test_dealer_entity.py, apps/api/tests/unit/domain/test_user_dealer_entity.py</files>
  <action>
    Create test_dealer_entity.py with xfail stubs for:
    - test_dealer_create_factory (Dealer.create() factory method)
    - test_dealer_slug_validation (slug unique per tenant)
    - test_dealer_location_update (lat/lng validation)
    - test_dealer_timezone_default (America/Montevideo default)

    Create test_user_dealer_entity.py with xfail stubs for:
    - test_assign_user_to_dealer (UserDealer creation)
    - test_user_dealer_unique_constraint (duplicate prevention)
    - test_remove_user_from_dealer (historical preservation)

    Pattern: Import only pytest (zero domain deps), use @pytest.mark.xfail with reason='Not implemented yet — Plan 02-01'
  </action>
  <verify>
    <automated>pytest apps/api/tests/unit/domain/test_dealer_entity.py apps/api/tests/unit/domain/test_user_dealer_entity.py -v --collect-only</automated>
  </verify>
  <done>Both test files created with 7 total xfail stubs importing only pytest</done>
</task>

<task type="auto">
  <name>Task 2: Create backend repository test stubs</name>
  <files>apps/api/tests/integration/repositories/test_dealer_repository.py, apps/api/tests/integration/repositories/test_user_dealer_repository.py</files>
  <action>
    Create test_dealer_repository.py with xfail stubs for:
    - test_dealer_repository_create (repository creates Dealer)
    - test_dealer_repository_get_by_slug (slug lookup)
    - test_dealer_repository_slug_unique (uniqueness validation)

    Create test_user_dealer_repository.py with xfail stubs for:
    - test_assign_user_dealer (M:N assignment)
    - test_get_user_dealer_ids (subquery for dealer IDs)
    - test_remove_user_dealer (preserves audit trail)

    Pattern: Import pytest + fixtures from conftest.py, use @pytest.mark.xfail with reason='Not implemented yet — Plan 02-02'
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/repositories/test_dealer_repository.py apps/api/tests/integration/repositories/test_user_dealer_repository.py -v --collect-only</automated>
  </verify>
  <done>Both test files created with 6 total xfail stubs</done>
</task>

<task type="auto">
  <name>Task 3: Create backend API endpoint test stubs</name>
  <files>apps/api/tests/integration/api/test_dealer_endpoints.py, apps/api/tests/integration/api/test_vehicle_filtering.py, apps/api/tests/integration/api/test_vehicle_pagination.py, apps/api/tests/integration/api/test_dynamic_filters.py, apps/api/tests/integration/api/test_user_dealer_api.py</files>
  <action>
    Create 5 API test files with xfail stubs:

    test_dealer_endpoints.py:
    - test_create_dealer_admin (POST /api/dealers)
    - test_create_dealer_slug_unique (slug uniqueness check)
    - test_get_dealer_by_id (GET /api/dealers/{id})

    test_vehicle_filtering.py:
    - test_admin_sees_all_vehicles (no dealer filter)
    - test_seller_sees_assigned_dealers (IN subquery)
    - test_dealer_sees_own_inventory (organization_id filter)
    - test_unauthorized_empty_assignments (401 on no dealers)

    test_vehicle_pagination.py:
    - test_cursor_pagination_consistency (no duplicates/skips)
    - test_cursor_encoding_decoding (base64 roundtrip)
    - test_has_more_flag (last page detection)
    - test_cursor_with_filters (pagination + filters)

    test_dynamic_filters.py:
    - test_filter_by_make_model (field_config filters)
    - test_filter_price_range (numeric ranges)
    - test_filter_with_invalid_field (rejection)

    test_user_dealer_api.py:
    - test_assign_seller_to_dealer (POST /api/users/{id}/dealers)
    - test_bulk_assign_sellers (POST /api/users/bulk-assign)
    - test_remove_seller_from_dealer (DELETE endpoint)

    Pattern: Import pytest + httpx + fixtures, use @pytest.mark.xfail with specific plan numbers
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/ -v --collect-only 2>&1 | grep -E "(test_dealer|test_vehicle|test_user_dealer)" | wc -l</automated>
  </verify>
  <done>5 test files created with 16 total xfail stubs for API endpoints</done>
</task>

<task type="auto">
  <name>Task 4: Create frontend test stubs</name>
  <files>apps/web/tests/unit/hooks/useDealerFilters.test.ts, apps/web/tests/components/DealerForm.test.tsx, apps/web/tests/components/UserDealerAssignment.test.tsx</files>
  <action>
    Create 3 frontend test files with xfail stubs:

    useDealerFilters.test.ts:
    - test_dealer_filters_initial_state (empty filters)
    - test_dealer_filters_set_make (make filter)
    - test_dealer_filters_set_status (status filter)

    DealerForm.test.tsx:
    - test_dealer_form_render (form fields display)
    - test_dealer_form_validation (required fields)
    - test_dealer_form_submit (POST on submit)
    - test_slug_auto_generation (name → slug transformation)

    UserDealerAssignment.test.tsx:
    - test_multi_select_dealers (dropdown selection)
    - test_assign_multiple_dealers (multi-select)
    - test_remove_dealer_assignment (removal)

    Pattern: Import vitest + testing-library, use test.skip() or describe.skip() with TODO comments
  </action>
  <verify>
    <automated>cd apps/web && vitest run --reporter=verbose 2>&1 | grep -E "(test_dealer|useDealerFilters)" | wc -l</automated>
  </verify>
  <done>3 test files created with 10 total xfail stubs for frontend</done>
</task>

</tasks>

<verification>
- All test stub files created with correct imports only (pytest/vitest)
- Every stub marked with xfail/skip and reason='Not implemented yet — Plan 02-XX'
- pytest --collect-only shows all expected tests
- Total stub count: 7 backend domain + 6 backend repo + 16 backend API + 10 frontend = 39 stubs
</verification>

<success_criteria>
- [ ] 12 test stub files exist (7 backend + 3 frontend + 2 integration)
- [ ] All stubs import only test framework (zero domain deps)
- [ ] All stubs marked with xfail/skip and plan number reference
- [ ] pytest --collect-only runs without errors
- [ ] vitest run --reporter=verbose shows all frontend stubs
- [ ] Wave 0 complete — ready for Plan 02-01 implementation
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-roles/02-00-SUMMARY.md`
</output>
