---
phase: 02-catalog-roles
plan: 05
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - apps/api/src/prosell/application/use_cases/vehicle/get_vehicle_catalog.py
  - apps/api/src/prosell/infrastructure/repositories/vehicle_repository_impl.py
  - apps/api/src/prosell/infrastructure/api/routers/vehicle_router.py
  - apps/api/tests/integration/api/test_vehicle_filtering.py
autonomous: true
requirements: [CATALOG-01, CATALOG-02, CATALOG-03]
must_haves:
  truths:
    - "Admin users see all vehicles across all dealers (no dealer filter)"
    - "Seller/Manager users see vehicles from assigned dealers only (IN subquery)"
    - "Dealer users see only their own vehicles (organization_id filter)"
    - "Unauthorized users (no dealers assigned) get 401 error"
    - "Tenant isolation enforced in all queries"
  artifacts:
    - path: "apps/api/src/prosell/application/use_cases/vehicle/get_vehicle_catalog.py"
      provides: "GetVehicleCatalogUseCase with role-based filtering"
      contains: "class GetVehicleCatalogUseCase"
    - path: "apps/api/src/prosell/infrastructure/repositories/vehicle_repository_impl.py"
      provides: "get_catalog_for_user() with role-based SQL"
      contains: "async def get_catalog_for_user"
    - path: "apps/api/src/prosell/infrastructure/api/routers/vehicle_router.py"
      provides: "GET /api/vehicles endpoint with role filtering"
      exports: ["GET /api/vehicles"]
  key_links:
    - from: "vehicle_router"
      to: "GetVehicleCatalogUseCase"
      via: "Dependency injection"
      pattern: "Depends\(get_vehicle_catalog_usecase\)"
    - from: "GetVehicleCatalogUseCase"
      to: "AbstractVehicleRepository"
      via: "Use case calls repository.get_catalog_for_user(user)"
      pattern: "return await vehicle_repository.get_catalog_for_user\(user\)"
---

<objective>
Implement role-based vehicle catalog filtering in the repository layer and API endpoint. Admin sees all vehicles, sellers/managers see assigned dealers' vehicles, dealers see only their own inventory, with proper tenant isolation and unauthorized handling.

Purpose: Enforce the business requirement that every role sees exactly the inventory they own, at the right scope.
Output: Working role-based vehicle filtering with integration tests GREEN.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-roles/02-CONTEXT.md
@.planning/phases/02-catalog-roles/02-RESEARCH.md
@.planning/phases/02-catalog-roles/02-02-PLAN.md (depends on UserDealer for M:N subquery)

# Existing patterns from Phase 01 (Vehicle repository)
```python
# Source: Vehicle repository pattern (from existing codebase)
class SqlAlchemyVehicleRepository(AbstractVehicleRepository):
    async def get_catalog_for_user(self, user: User, limit: int = 50) -> list[Vehicle]:
        stmt = select(VehicleModel).join(ProductModel).where(
            ProductModel.tenant_id == user.tenant_id,
        )

        # Role-based filtering
        if user.has_role("admin"):
            pass  # No dealer filter
        elif user.has_role("dealer"):
            stmt = stmt.where(ProductModel.organization_id == user.dealer_id)
        else:  # seller, manager
            user_dealer_ids = await self._get_user_dealer_ids(user.id, user.tenant_id)
            if not user_dealer_ids:
                raise Unauthorized("No dealers assigned to user")
            stmt = stmt.where(ProductModel.organization_id.in_(user_dealer_ids))
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend VehicleRepository with role-based filtering</name>
  <files>apps/api/src/prosell/infrastructure/repositories/vehicle_repository_impl.py</files>
  <behavior>
    - Test 1: get_catalog_for_user() returns all vehicles for admin
    - Test 2: get_catalog_for_user() returns assigned dealers' vehicles for seller
    - Test 3: get_catalog_for_user() returns own vehicles for dealer
    - Test 4: get_catalog_for_user() raises 401 for seller with no dealers
    - Test 5: _get_user_dealer_ids() subquery returns correct dealer IDs
  </behavior>
  <action>
    Extend SqlAlchemyVehicleRepository in infrastructure/repositories/vehicle_repository_impl.py:
    - Add method: get_catalog_for_user(user: User, limit: int = 50, cursor: str | None = None) -> tuple[list[Vehicle], str | None, bool]
    - Base query: select(VehicleModel).join(ProductModel).where(ProductModel.tenant_id == user.tenant_id)
    - Role filtering:
      - admin: No dealer_id filter (see all vehicles)
      - dealer: stmt.where(ProductModel.organization_id == user.dealer_id)
      - seller/manager: Call _get_user_dealer_ids(), then stmt.where(ProductModel.organization_id.in_(dealer_ids))
    - Unauthorized handling: Raise Unauthorized if dealer_ids is empty for seller/manager
    - Cursor pagination: WHERE id > decode_cursor(cursor), ORDER BY created_at DESC, LIMIT +1
    - Return: (vehicles list, next_cursor, has_more)

    Add private method: _get_user_dealer_ids(user_id: UUID, tenant_id: UUID) -> list[UUID]
    - Subquery: select(UserDealerModel.dealer_id).where(user_id == user_id, tenant_id == tenant_id)
    - Return list of dealer IDs

    Follow repository pattern from RESEARCH.md section "Pattern 3: Role-Based Query Filtering"
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_vehicle_filtering.py::test_vehicle_repository_role_filtering -xvs</automated>
  </verify>
  <done>VehicleRepository extended with role-based filtering, 5 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GetVehicleCatalogUseCase</name>
  <files>apps/api/src/prosell/application/use_cases/vehicle/get_vehicle_catalog.py</files>
  <behavior>
    - Test 1: execute() calls repository.get_catalog_for_user()
    - Test 2: execute() passes user object for role-based filtering
    - Test 3: execute() returns vehicles with publication state array
  </behavior>
  <action>
    Create GetVehicleCatalogUseCase in application/use_cases/vehicle/get_vehicle_catalog.py:
    - Constructor: Inject AbstractVehicleRepository, AbstractPublicationRepository
    - execute(user: User, limit: int = 50, cursor: str | None = None) -> CatalogResponse
    - Call vehicle_repository.get_catalog_for_user(user, limit, cursor)
    - For each vehicle, fetch publications via publication_repository.get_by_vehicle_id()
    - Build response: VehicleDTO[] with nested publications array
    - Return CatalogResponse with items, next_cursor, has_more

    Follow use case pattern from Plan 02-03
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_vehicle_filtering.py::test_get_vehicle_catalog_usecase -xvs</automated>
  </verify>
  <done>GetVehicleCatalogUseCase implemented with publication state, 3 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create or extend vehicle_router with catalog endpoint</name>
  <files>apps/api/src/prosell/infrastructure/api/routers/vehicle_router.py</files>
  <behavior>
    - Test 1: GET /api/vehicles returns vehicles based on user role
    - Test 2: Query params: limit, cursor for pagination
    - Test 3: Admin sees all vehicles in tenant
    - Test 4: Seller sees assigned dealers' vehicles
    - Test 5: Dealer sees only own vehicles
    - Test 6: Unauthorized seller (no dealers) gets 401
  </behavior>
  <action>
    Create or extend vehicle_router in infrastructure/api/routers/vehicle_router.py:
    - Router prefix: "/api/vehicles", tag: "vehicles"
    - GET /api/vehicles: GetVehicleCatalogUseCase, response_model=CatalogResponse
      - Query params: limit (int, default 50), cursor (str | None)
      - Auth: Depends(get_current_user)
      - Pass current_user to use case for role-based filtering
      - Return CatalogResponse with items, next_cursor, has_more
    - If vehicle_router doesn't exist, create new file
    - If exists, add GET /api/vehicles endpoint
    - Use Depends(get_vehicle_catalog_use_case) for DI
    - Use Annotated[User, Depends(get_current_user)] for auth

    Follow router pattern from Plan 02-03
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_vehicle_filtering.py::test_vehicle_router_catalog_endpoint -xvs</automated>
  </verify>
  <done>Vehicle router with GET /api/vehicles endpoint, 6 tests GREEN</done>
</task>

</tasks>

<verification>
- Admin users see all vehicles across all dealers
- Seller/Manager users see assigned dealers' vehicles (IN subquery)
- Dealer users see only their own vehicles
- Unauthorized users (no dealers) get 401 error
- Tenant isolation enforced in all queries
- All integration tests GREEN (5 repo + 3 use case + 6 router = 14 tests)
- Wave 0 xfail stubs now GREEN for Plan 02-05 tests
</verification>

<success_criteria>
- [ ] VehicleRepository.get_catalog_for_user() with role-based SQL
- [ ] GetVehicleCatalogUseCase with publication state array
- [ ] GET /api/vehicles endpoint with role filtering
- [ ] Admin sees all, seller sees assigned, dealer sees own
- [ ] 401 for unauthorized sellers (no dealers)
- [ ] 14 integration tests GREEN
- [ ] Plan 02-07 can proceed (Dynamic filters extend this)
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-roles/02-05-SUMMARY.md`
</output>
