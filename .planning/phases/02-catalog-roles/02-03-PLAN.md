---
phase: 02-catalog-roles
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/api/src/prosell/application/dto/dealer.py
  - apps/api/src/prosell/application/use_cases/dealer/create_dealer.py
  - apps/api/src/prosell/infrastructure/api/routers/dealer_router.py
  - apps/api/src/prosell/infrastructure/api/main.py
  - apps/api/tests/integration/api/test_dealer_endpoints.py
autonomous: true
requirements: [CATALOG-05]
must_haves:
  truths:
    - "POST /api/dealers creates dealer with slug auto-generation"
    - "Slug uniqueness validated per tenant (409 Conflict on duplicate)"
    - "GET /api/dealers/{id} returns dealer with tenant isolation"
    - "GET /api/dealers lists all dealers for tenant with pagination"
    - "Admin-only access enforced via dependency injection"
  artifacts:
    - path: "apps/api/src/prosell/application/dto/dealer.py"
      provides: "DealerDTOs (CreateDealerRequest, DealerResponse)"
      exports: ["CreateDealerRequest", "DealerResponse"]
    - path: "apps/api/src/prosell/application/use_cases/dealer/create_dealer.py"
      provides: "CreateDealerUseCase"
      contains: "class CreateDealerUseCase"
    - path: "apps/api/src/prosell/infrastructure/api/routers/dealer_router.py"
      provides: "FastAPI router for dealer endpoints"
      exports: ["POST /api/dealers", "GET /api/dealers/{id}", "GET /api/dealers"]
  key_links:
    - from: "dealer_router"
      to: "CreateDealerUseCase"
      via: "Dependency injection in FastAPI"
      pattern: "Depends\(get_create_dealer_usecase\)"
    - from: "DealerRouter"
      to: "AbstractDealerRepository"
      via: "Use case constructor"
      pattern: "def __init__\(self, dealer_repository: AbstractDealerRepository\)"
---

<objective>
Implement the Dealer CRUD API endpoints (POST, GET by ID, GET list) with admin-only access, slug uniqueness validation, and tenant isolation. This enables admin users to create and manage dealer organizations from the frontend UI.

Purpose: Provide REST API for dealer management operations with proper authentication, authorization, and validation.
Output: Working dealer API endpoints with integration tests GREEN.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-roles/02-CONTEXT.md
@.planning/phases/02-catalog-roles/02-RESEARCH.md
@.planning/phases/02-catalog-roles/02-01-PLAN.md (depends on Dealer entity and repository)
@.planning/phases/01-hybrid-publisher/01-06-graph-api-router-PLAN.md (FastAPI router pattern reference)

# Existing patterns from Phase 01 (FastAPI routers)
```python
# Source: Publication router pattern
from fastapi import APIRouter, Depends, HTTPException
from prosell.application.use_cases.publisher.publish_vehicle import PublishVehicleUseCase

router = APIRouter(prefix="/api/publisher", tags=["publisher"])

@router.post("/publish", response_model=PublicationResponse, status_code=201)
async def publish_vehicle(
    request: PublishVehicleRequest,
    use_case: Annotated[PublishVehicleUseCase, Depends(get_publish_vehicle_use_case)],
) -> PublicationResponse:
    return await use_case.execute(request)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Dealer DTOs</name>
  <files>apps/api/src/prosell/application/dto/dealer.py</files>
  <behavior>
    - Test 1: CreateDealerRequest validates required fields (name, tenant_id)
    - Test 2: CreateDealerRequest accepts optional slug, location, timezone, settings
    - Test 3: DealerResponse includes all dealer fields + computed URLs
    - Test 4: DealerListResponse includes items array + pagination metadata
  </behavior>
  <action>
    Create Dealer DTOs in application/dto/dealer.py:
    - CreateDealerRequest (Pydantic): name (str, min 1 char), slug (str | None), location fields (all optional), timezone (str, default "America/Montevideo"), settings (dict, default {})
    - DealerResponse (Pydantic): All dealer fields + id, created_at, updated_at
    - DealerListResponse (Pydantic): items (list[DealerResponse]), total (int), page (int), limit (int)
    - Use Field() for validation (min_length, max_length)
    - Include example values for documentation

    Follow PublishVehicleRequest pattern from Phase 01
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_dealer_endpoints.py::test_dealer_dtos -xvs</automated>
  </verify>
  <done>Dealer DTOs created with validation, 4 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CreateDealerUseCase</name>
  <files>apps/api/src/prosell/application/use_cases/dealer/create_dealer.py</files>
  <behavior>
    - Test 1: execute() creates dealer via repository
    - Test 2: Auto-generates slug from name if not provided
    - Test 3: Validates slug uniqueness (raises SlugNotUnique if exists)
    - Test 4: Returns DealerResponse with all fields
  </behavior>
  <action>
    Create CreateDealerUseCase in application/use_cases/dealer/create_dealer.py:
    - Constructor: Inject AbstractDealerRepository
    - execute(request: CreateDealerRequest, tenant_id: UUID) -> DealerResponse
    - Slug logic: Use request.slug if provided, else generate from request.name
    - Validation: Call repository.exists_by_slug() before create
    - Creation: Call Dealer.create() factory, repository.create()
    - Return: Map Dealer entity to DealerResponse
    - Raise SlugNotUnique if slug already exists in tenant

    Follow PublishVehicleUseCase pattern from Phase 01
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_dealer_endpoints.py::test_create_dealer_usecase -xvs</automated>
  </verify>
  <done>CreateDealerUseCase implemented with slug validation, 4 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement GetDealerUseCase and ListDealersUseCase</name>
  <files>apps/api/src/prosell/application/use_cases/dealer/get_dealer.py, apps/api/src/prosell/application/use_cases/dealer/list_dealers.py</files>
  <behavior>
    - Test 1: GetDealerUseCase returns dealer by id or raises DealerNotFound
    - Test 2: ListDealersUseCase returns paginated dealer list for tenant
    - Test 3: Pagination supports limit/offset parameters
  </behavior>
  <action>
    Create GetDealerUseCase in application/use_cases/dealer/get_dealer.py:
    - execute(dealer_id: UUID, tenant_id: UUID) -> DealerResponse
    - Call repository.get_by_id(), raise DealerNotFound if None
    - Return mapped DealerResponse

    Create ListDealersUseCase in application/use_cases/dealer/list_dealers.py:
    - execute(tenant_id: UUID, limit: int = 50, offset: int = 0) -> DealerListResponse
    - Call repository.list_by_tenant() with pagination
    - Return DealerListResponse with items + total count

    Follow use case pattern from Phase 01
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_dealer_endpoints.py::test_get_dealer_usecases -xvs</automated>
  </verify>
  <done>GetDealer and ListDealers use cases implemented, 3 tests GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create dealer_router with CRUD endpoints</name>
  <files>apps/api/src/prosell/infrastructure/api/routers/dealer_router.py</files>
  <behavior>
    - Test 1: POST /api/dealers creates dealer and returns 201
    - Test 2: GET /api/dealers/{id} returns dealer or 404
    - Test 3: GET /api/dealers returns paginated list
    - Test 4: Admin-only access enforced (403 for non-admin)
    - Test 5: Slug conflict returns 409 Conflict
  </behavior>
  <action>
    Create dealer_router in infrastructure/api/routers/dealer_router.py:
    - Router prefix: "/api/dealers", tag: "dealers"
    - POST /api/dealers: CreateDealerUseCase, response_model=DealerResponse, status_code=201
      - Dependency: get_current_user() for auth
      - Admin check: user.has_role("admin") or HTTPException 403
      - Extract tenant_id from user.tenant_id
      - Catch SlugNotUnique → HTTPException 409
    - GET /api/dealers/{id}: GetDealerUseCase, response_model=DealerResponse
      - Tenant isolation: pass user.tenant_id
      - Catch DealerNotFound → HTTPException 404
    - GET /api/dealers: ListDealersUseCase, response_model=DealerListResponse
      - Query params: limit (default 50), offset (default 0)
      - Tenant isolation: pass user.tenant_id
    - Use Depends(get_create_dealer_use_case) for DI
    - Use Annotated[User, Depends(get_current_user)] for auth

    Follow publication router pattern from Phase 01
  </action>
  <verify>
    <automated>pytest apps/api/tests/integration/api/test_dealer_endpoints.py::test_dealer_router_endpoints -xvs</automated>
  </verify>
  <done>Dealer router with 3 endpoints, admin-only access, 5 tests GREEN</done>
</task>

<task type="auto">
  <name>Task 5: Wire dealer_router into main FastAPI app</name>
  <files>apps/api/src/prosell/infrastructure/api/main.py</files>
  <action>
    Import dealer_router and include in FastAPI app:
    - from prosell.infrastructure.api.routers.dealer_router import router as dealer_router
    - app.include_router(dealer_router)
    - Place after existing router includes

    Verify with: curl http://localhost:8000/api/docs (shows /api/dealers endpoints)
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/api/docs | grep -o "/api/dealers" | wc -l</automated>
  </verify>
  <done>Dealer router wired into main app, endpoints visible in /api/docs</done>
</task>

</tasks>

<verification>
- POST /api/dealers creates dealer with slug auto-generation
- Slug uniqueness returns 409 Conflict on duplicate
- GET /api/dealers/{id} returns dealer with tenant isolation
- GET /api/dealers returns paginated list for tenant
- Admin-only access enforced (403 for non-admin users)
- All integration tests GREEN (4 DTO + 4 use case + 5 router = 13 tests)
- Wave 0 xfail stubs now GREEN for Plan 02-03 tests
</verification>

<success_criteria>
- [ ] Dealer DTOs in application/dto/dealer.py
- [ ] CreateDealerUseCase with slug validation
- [ ] GetDealerUseCase and ListDealersUseCase
- [ ] dealer_router with 3 endpoints (POST, GET by ID, GET list)
- [ ] Admin-only access enforced via role check
- [ ] Router wired into main FastAPI app
- [ ] 13 integration tests GREEN
- [ ] Plan 02-05 can proceed (Frontend DealerForm depends on these endpoints)
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-roles/02-03-SUMMARY.md`
</output>
