---
phase: 10-contract-testing-skill
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-04-05T01:18:29.830Z
---

<current_state>

**Phase 10 (Contract Testing Skill)** code is 100% complete (7/7 tasks, 7 contract tests passing).

**BUG ACTIVO**: VehicleForm Select fields no se actualizan visualmente ni con VIN decode ni con selección manual. Root cause CONFIRMADO y documentado.

</current_state>

<completed_work>

**Esta sesión — Systematic Debugging Investigation**:
- ✅ Phase 1 (Root Cause Investigation) — COMPLETA
  - Leído VehicleForm.tsx, select.tsx wrapper, fbVehicleOptions.ts, nhtsa_normalizer.py, decode_vin.py
  - Consultado context7 docs para Radix UI Select (controlled value API) y React Hook Form (Controller + setValue)
  - Verificado versiones exactas: @radix-ui/react-select@2.2.6, react@19.2.4, react-hook-form@7.71.1
  - Leído source code de Radix Select en node_modules (líneas 70-135 y 1069-1105 de index.mjs)
  - Encontrado bugs confirmados: radix-ui/primitives#3381 y #3693
- ✅ Phase 2 (Pattern Analysis) — COMPLETA
  - mileage_unit funciona (siempre tiene valor "mi", no cambia via setValue)
  - make/body_type/drivetrain/transmission/fuel_type fallan (empiezan undefined, cambian via setValue)
- ✅ Phase 3 (Hypothesis) — FORMULADA Y VALIDADA con evidencia
  - Root cause: Radix SelectBubbleInput dentro de <form> en React 19 dispara onValueChange("") espurio
  - Mecanismo: nativeOptionsSet vacío en primer render → native <select> sin options → change event con "" → reset

**Sesiones anteriores**:
- ✅ Phase 10 Contract Testing Skill: 7/7 tareas, 7 contract tests passing
- ✅ DB Migration Fix: tablas faltantes creadas, merge migration
- ❌ Múltiples intentos de fix que NO funcionaron (remover ?? "", dynamic keys, etc.)

</completed_work>

<remaining_work>

**Phase 4 (Implementation) del Systematic Debugging**:

1. **Opción B (preferida)**: `pnpm patch @radix-ui/react-select`
   - Cambio: `isFormControl ?` → `(isFormControl && nativeOptionsSet.size > 0) ?`
   - En ambos archivos: `dist/index.mjs` y `dist/index.js`
   - Línea ~115 en index.mjs
   - Workaround exacto propuesto en radix-ui/primitives#3693

2. **Verificación**: Probar VIN decode + selección manual en browser

3. **Plan B (si patch no funciona)**: Reemplazar Radix Select por `<select>` nativo con Tailwind
   - Elimina Controller (usa register() directo)
   - Elimina BubbleInput completamente
   - Styling con clases de Tailwind que matcheen el design system

4. **Limpieza**: Remover logs de debug del VehicleForm.tsx

5. **Commit**: Con GGA aprobado

</remaining_work>

<decisions_made>

- **Root cause confirmado**: Radix Select BubbleInput + React 19 + <form> = onValueChange("") espurio
- **Fix elegido**: pnpm patch (Opción B) sobre guard en onValueChange (Opción A)
  - Razón: fix de raíz, protege todos los Select del proyecto, no solo VehicleForm
- **Plan B acordado**: Si patch falla, reemplazar con <select> nativo + Tailwind
  - Razón: elimina toda la complejidad de Radix Select, funciona con register() directo

</decisions_made>

<blockers>

**Blocker 1**: Radix UI Select BubbleInput bug con React 19 — fix planificado (pnpm patch)
- Issues: radix-ui/primitives#3381, radix-ui/primitives#3693
- React 19 issue: facebook/react#30580

</blockers>

<context>

**Estado mental**: El debugging sistemático reveló que NO era un problema de nuestro código. Es un bug CONOCIDO de Radix UI con React 19 cuando el Select está dentro de un <form>. Múltiples sesiones anteriores intentaron fixes en VehicleForm.tsx sin éxito porque atacaban síntomas, no la causa raíz.

**Archivos sin commitear** (del trabajo previo):
- `apps/web/src/components/forms/VehicleForm.tsx` - Select fixes anteriores + debug logs
- `apps/api/alembic/versions/...merge_migration.py` - New merge migration
- `apps/api/src/prosell/infrastructure/api/middleware/exception_handlers.py` - minor fix
- `apps/api/src/prosell/infrastructure/api/routers/auth_router.py` - minor fix
- `apps/web/package.json` - deps
- `tests/e2e/specs/vehicles.spec.ts` - 73 lines added

**Documentación clave leída**:
- Radix Select controlled API (context7): value + onValueChange
- RHF Controller + setValue (context7): shouldValidate/shouldDirty/shouldTouch
- Radix Select source (node_modules): BubbleInput useEffect + isFormControl logic

</context>

<next_action>

1. `pnpm patch @radix-ui/react-select` — editar línea ~115 en dist/index.mjs y dist/index.js
2. Cambiar `isFormControl ?` → `(isFormControl && nativeOptionsSet.size > 0) ?`
3. `pnpm patch-commit <path>`
4. Levantar dev server y verificar en browser (VIN decode + manual select)
5. Si funciona → limpiar debug logs → commit con GGA
6. Si no funciona → Plan B: reemplazar con <select> nativo

</next_action>

---

*Updated: 2026-04-05*
*Status: PAUSED — Root cause found, fix ready to implement*
