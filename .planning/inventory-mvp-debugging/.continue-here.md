---
phase: inventory-mvp-debugging
task: 4
total_tasks: 5
status: awaiting_user_decision
last_updated: 2026-04-01T12:33:48.036Z
---

<current_state>
OAuth buttons issue evolved into architectural bug discovery. OAuth infrastructure is now fixed (Redis running, imports corrected, AnimatedSvgWrapper restored). Real issue: `user_dealers` table has FK to non-existent `dealers.id`. Platform should use `organizations` (multi-niche) not vehicle-specific `dealers`.
</current_state>

<completed_work>

**Session 2026-04-01: OAuth Debug → Architecture Bug**

- ✅ Task 1: Investigated OAuth buttons not responding
  - Agent 1 misdiagnosed AnimatedSvgWrapper (removed forwardRef - WRONG)
  - Agent 2 corrected: restored React.forwardRef pattern (CORRECT)

- ✅ Task 2: Fixed OAuth infrastructure
  - Started Redis (Docker): `docker compose up -d redis`
  - Fixed missing imports in 5 routers:
    - product_router.py - Added get_async_session
    - category_router.py - Removed duplicate
    - vehicle_router.py - Fixed broken import block
    - wallet_router.py - Removed duplicate
    - user_dealer_router.py - Added get_current_auth_user_from_cookie

- ✅ Task 3: Discovered architectural bug
  - User: "ahora es error 500" - Backend logs show: `relation "user_dealers" does not exist`
  - User insight: "por qué hay user_dealer si se debe usar organization" (KEY INSIGHT)
  - Agent 3 confirmed: Migration creates FK to `dealers.id` (doesn't exist)
  - Should use `organizations.id` (exists, multi-niche)
  - Platform is multi-niche: vehicles, real estate, electronics, etc.
  - Memory confirms: "Dealer = Organization"

**Key Finding**:
```python
# Migration 20260329_1500-add_user_dealers_table.py line 48-49
sa.ForeignKeyConstraint(
    ["dealer_id"],
    ["dealers.id"],  # ← ❌ This table doesn't exist!
    name="fk_user_dealers_dealer_id_dealers",
)
```

**Files with user_dealers references**:
1. alembic/versions/20260329_1500-add_user_dealers_table.py - Invalid FK
2. routers/user_dealer_router.py - Vehicle-specific router
3. repositories/user_dealer_repository_impl.py - Queries user_dealers
4. domain/entities/user_dealer.py - Entity
5. models/user_dealer_model.py - ORM model
6. tests/integration/api/test_user_dealer_api.py - Tests
7. repositories/vehicle_repository_impl.py - Calls _get_user_dealer_ids()
</completed_work>

<remaining_work>

- ⏸️ **Task 4: AWAITING USER DECISION**
  - Option A (Recommended): Full refactor to `organization_members`
  - Option B (Alternative): Fix FK only, keep vehicle-specific names

- ⏳ **Task 5: Implement chosen option**
  - Create migration
  - Update all affected code
  - Test /api/v1/vehicles endpoint
  - Verify OAuth buttons work end-to-end
</remaining_work>

<decisions_made>

- **AnimatedSvgWrapper**: Needs React.forwardRef in React 19 (Agent 1 was wrong)
- **Redis**: Required for OAuth state tokens (line 161 oauth_service_impl.py)
- **Architecture**: Platform is multi-niche → Dealer = Organization
- **Bug root cause**: user_dealers has FK to non-existent dealers.id
- **Memory**: "Dealer = Organization" confirmed for multi-niche platform
</decisions_made>

<blockers>

- 🔴 **BLOCKED**: User must choose Option A vs Option B
- 🔴 **DB Error**: user_dealers table cannot be queried
- 🔴 **500 Error**: GET /api/v1/vehicles fails with `relation "user_dealers" does not exist`
</blockers>

<context>

**Session Flow**:
1. User: "OAuth buttons no funcionan - arreglalo con un agente"
2. Agent 1 (a04beef9): Fixed AnimatedSvgWrapper - removed forwardRef ❌
3. User: "sigue igual no reaccionan los botones"
4. Agent 2 (ab4967464): Restored forwardRef ✅, found Redis not running
5. User: "ahora es error 500... relation user_dealers does not exist"
6. User: "por qué hay user_dealer si se debe usar organization" 🎯
7. Agent 3 (afdc48699ed00805d): Started Redis, fixed imports
8. Agent 4 (a43022a95e3ee3ff4): Confirmed bug - FK to non-existent dealers.id
9. **NOW**: Awaiting user decision

**Technical State**:
- Backend: localhost:8000 ✅ running
- Frontend: localhost:3000 ✅ running
- Redis: ✅ running (Docker)
- PostgreSQL: ✅ running (missing user_dealers table)
- Tests: 1027/1027 passing (before this issue)

**Architecture Context**:
- Multi-niche platform: vehicles, real estate, electronics, etc.
- Generic entities: Organization, Product, Category
- Vehicle-specific: Vehicle (inherits from Product)
- Dealer = Organization (multi-niche design)
- user_dealers table breaks this principle
</context>

<next_action>

**WAIT for user decision**:

**Option A (Recommended)**: Full refactor to generic `organization_members`
- Rename: `user_dealers` → `organization_members`
- Rename: `dealer_id` → `organization_id`
- Fix FK: `organizations.id` (not `dealers.id`)
- Update: All 6+ files
- Pros: Proper multi-niche architecture ✅
- Cons: Large refactor, more work

**Option B**: Fix FK only
- Change FK: `dealers.id` → `organizations.id`
- Keep: Vehicle-specific naming (`user_dealers`, `dealer_id`)
- Pros: Quick fix
- Cons: Breaks multi-niche principle ❌

**Once decided**:
1. Create new Alembic migration
2. Run: `alembic upgrade head`
3. Restart backend
4. Test: `GET /api/v1/vehicles?limit=50`
5. Test: OAuth buttons (Google + Facebook)
</next_action>

---

**Agent Session IDs**:
- a04beef9f19bd1871 - AnimatedSvgWrapper (wrong)
- ab4967464c4038b49 - OAuth routers + Redis fix
- afdc48699ed00805d - Backend/frontend logs check
- a43022a95e3ee3ff4 - user_dealers→organizations diagnosis

**Debug Sessions**:
- .planning/debug/oauth-buttons-not-working.md
- .planning/debug/user-dealers-organization-refactor.md
