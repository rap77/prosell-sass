---
phase: inventory-mvp-debugging
task: oauth-cookie-fix
total_tasks: 1
status: in_progress
last_updated: 2026-04-01T00:36:43.399Z
---

<current_state>
Debugging 401 Unauthorized error on GET /api/v1/vehicles?limit=50

**Root Cause Identified:** NOT a technical bug - it's a user flow issue. The user needs to login first.

**Evidence gathered:**
- Next.js proxy routes ARE working correctly
- Backend IS working correctly (returns 401 when no auth - expected behavior)
- Database IS working (PostgreSQL running, has 2 users)
- Cookies with domain="localhost" ARE set correctly in backend

**The actual issue:** User is not logged in, so no cookies exist, therefore 401 is the CORRECT response from backend.
</current_state>

<completed_work>
- Created Next.js API routes for /api/v1/vehicles to forward cookies
- Modified next.config.ts to use fallback type for rewrites
- Verified backend is responding correctly
- Verified database is running and has 2 users
- Verified auth flow endpoints exist
</completed_work>

<remaining_work>
- User needs to login at http://localhost:3000/auth/login
- Test with existing users: rafael.padron@gmail.com or prosellweb@gmail.com
- OR create a test user with known password for easier testing
</remaining_work>

<decisions_made>
- Used Next.js API routes instead of direct client-side fetch
- Used fallback type in rewrites so API routes are processed BEFORE rewrites
- Did NOT modify backend auth logic (it's working correctly)
</decisions_made>

<blockers>
- User doesn't have password for existing accounts
- Need to either: reset password, create test user, or login with existing account
</blockers>

<context>
The 401 is NOT a bug - it's the backend correctly rejecting an unauthenticated request.
The Next.js proxy is working (forwards cookies when present).
The user just needs to login first.

Files Modified:
- apps/web/src/app/api/v1/vehicles/route.ts (created)
- apps/web/src/app/api/v1/vehicles/[...path]/route.ts (created)
- apps/web/next.config.ts (modified to use fallback)
</context>

<next_action>
Ask user:
1. Can you login with existing account?
2. Or create test user with known credentials?
3. Or test "Forgot Password" flow?
</next_action>
