import { describe, it, expect } from 'vitest'

describe('UploadProgress', () => {
  it('displays per-image progress bars', () => {
    // TODO: Test 0-100% per file
  })

  it('shows processing status after upload', () => {
    // TODO: Test "Compressing..." state
  })

  it('allows individual image deletion', () => {
    // TODO: Test remove button
  })
})
