Do not commit real keys to this directory.

Generate local development keys with:

```bash
./scripts/generate-jwt-keys.sh
```

Provide runtime paths with:

- `JWT_PRIVATE_KEY_PATH`
- `JWT_PUBLIC_KEY_PATH`
