# Business Overview — ProSell SaaS

## Dominio de negocio

ProSell SaaS es una **plataforma de venta y análisis de mercado de vehículos**: un marketplace propio (catálogo, fichas de producto, leads, citas) combinado con herramientas de gestión de inventario multi-tenant para concesionarios (dealers), y publicación automatizada de listados hacia Facebook Marketplace.

## Corrección respecto a `CLAUDE.md` (brecha visión vs. implementación)

`CLAUDE.md` (raíz del repo) describe el proyecto con cuatro pilares: "Public Marketplace", "SaaS Analytics", **"Automated Scraping: Multi-marketplace scraping (Facebook Marketplace primary)"** y **"ML Predictions: Price prediction and recommendation models"**. Este rescan completo verificó el código real contra esa descripción y encontró una brecha real entre visión documentada e implementación:

- **No existe scraping genérico multi-marketplace.** Lo que existe es **publicación** (no extracción) hacia Facebook, vía dos estrategias intercambiables detrás de `publisher_strategy.py`: `graph_api_publisher.py` (Facebook Graph API oficial, requiere cuenta de negocio conectada) y `playwright_publisher.py` (automatización de navegador vía Playwright como estrategia alternativa/fallback cuando no hay credenciales de Graph API). Playwright automatiza la **publicación** del propio inventario, no la extracción de datos de terceros — no hay scraper de "qué se está vendiendo en la competencia" en ningún marketplace, ni un módulo dedicado a scraping multi-sitio.
- **No existe código de Machine Learning.** Se buscó explícitamente cualquier módulo de predicción de precio o motor de recomendación (`sklearn`, `torch`, `tensorflow`, nombres de archivo tipo `price_prediction`/`ml_model`/`recommendation_engine`) — cero resultados. La dependencia `anthropic>=0.40.0` está declarada en `apps/api/pyproject.toml` pero **no tiene ningún uso en el código fuente** (cero imports de `anthropic` en `apps/api/src`) — es una dependencia instalada y no utilizada, no evidencia de un pipeline de ML/IA en producción.

**Este documento describe lo efectivamente implementado**, no la aspiración de `CLAUDE.md`. La brecha queda registrada aquí y en `code-quality-assessment.md` § Technical Debt Signals como deuda de documentación (visión desactualizada o nunca ejecutada), no como funcionalidad entregada.

## Funcionalidad clave (verificada este pase)

- **Gestión de inventario/catálogo**: alta, edición, publicación y ciclo de vida de estado de un producto (`Borrador → Pendiente revisión → Aprobado/Rechazado → Publicado → Vendido/Archivado`, con reversiones auditadas — ver `architecture.md` § Interaction Diagrams).
- **Cola de revisión (review queue)**: flujo de moderación donde un rol interno aprueba o rechaza publicaciones antes de que lleguen al marketplace público.
- **Publicación a Facebook Marketplace**: `publisher_router.py` + `PublisherStrategy` (Graph API oficial o Playwright como alternativa), orquestado vía tareas asíncronas Taskiq/Redis (`publish_product_task.py`, `auto_republish_task.py`, `delete_listing_task.py`, `update_listing_task.py`, `poll_facebook_leads_task.py`, `refresh_facebook_tokens.py`) — publicación y sincronización de estado, no scraping de terceros.
- **Leads y citas**: captura de interés de compradores (incluye leads sincronizados desde Facebook vía `poll_facebook_leads_task.py`) y agendamiento de citas con el vendedor/dealer, con calendario (`FullCalendar` en dependencias frontend).
- **Multi-tenant**: cada organización (dealer) opera con su propio espacio de datos; todo agregado de dominio lleva `tenant_id`, resuelto siempre desde el JWT (nunca del body de la petición — mitigación IDOR).
- **Onboarding e invitaciones**: alta de organizaciones nuevas y alta de usuarios vía invitación por token (`organization_invitation.py`, `team_invitation.py`).
- **Autenticación**: JWT + OAuth2 (Google, Microsoft) + 2FA (TOTP), cookies httpOnly, con auditoría multi-nivel (`auth_middleware.py`, `rbac_middleware.py`, `rate_limit_middleware.py`). Del lado frontend, la navegación auth vive en `apps/web/src/proxy.ts` (middleware) + `apps/web/src/stores/authStore.ts` (Zustand con persist) + `apps/web/src/lib/auth/deriveRole.ts` (single source of truth de rol, compartida entre ambos) — ver `architecture.md` § Interaction Diagrams para el flujo OAuth completo.
- **Categorización dinámica**: taxonomía de categorías con `attribute_schema` configurable por categoría vía `category_inference_router.py` y `org_verticals_router.py` — el modelo de producto es genérico, con vehículos como vertical principal (VIN decode vía NHTSA, `nhtsa_vin_service.py`/`nhtsa_normalizer.py`, y consumo de tabla de eficiencia de combustible vía `fueleconomy_service.py`).
  - **Catálogo editorial de valores del schema, alineado a Facebook Marketplace (confirmado en el scan enfocado del intent `260915-vehicle-catalog`)**: el admin de la plataforma puede poblar las `options` de un campo `select` del `attribute_schema` de una categoría con un botón "Load from Facebook catalog" (`category-schema-editor.tsx`), que trae los strings oficiales en **español** que Facebook Marketplace acepta (`"SUV"`, `"Gasolina"`, etc. — catálogo `apps/web/src/lib/i18n/facebook-values/index.ts`, agregado esta semana). Es un catálogo EDITORIAL/de referencia para que el admin arme el schema — no es el mismo catálogo de normalización que usa el flujo de decodificación de VIN (ver Nota de riesgo abajo y `code-quality-assessment.md`).
  - **Nota de negocio sobre la taxonomía de vehículos (confirmado en el scan enfocado del intent `260830-ci-seed-data`)**: la jerarquía de categorías de la vertical "Vehículos y Transporte" fue **aplanada deliberadamente** el 6-ago (commit `2166f142`, "fix(seed): flatten Carros y Camionetas category"). Antes, `carros-y-camionetas` tenía 5 subcategorías hoja de nivel 3 (`sedan`, `hatchback`, `suvs`, `pick-ups`, `coupe`); ahora `carros-y-camionetas` ES la hoja (nivel 2), y el tipo de carrocería (body type) pasó a ser un **atributo del producto** (vía `attribute_schema`), no una subcategoría separada. Es una decisión de modelado de dominio válida y ya aplicada en la data de seed real — el problema encontrado no es de negocio sino de sincronización de tests (ver `code-quality-assessment.md`).
- **Wallet**: entidad de dominio `wallet.py` + `wallet_router.py` para saldo/transacciones por organización. **Nota**: `stripe>=11.0.0` está declarado en dependencias backend pero, igual que `anthropic`, **no tiene ningún import en el código fuente** — no hay integración de cobro real verificada este pase; el wallet parece un libro de saldo interno, no un flujo de pago con proveedor externo conectado.
- **Carga masiva de inventario por CSV (bulk upload)** (confirmado en el scan enfocado del intent `260830-ci-fixes-round2`): importación de vehículos vía archivo CSV del cliente (23 columnas, delimitado por `;`), mapeado por `csv_field_mapper.py` a `MappedCSVRow`, con resolución de organización por código (`cod_organization`, con fallback deliberado a `title` cuando no hay código explícito — comentario in-line "ponytail"). Dos modos: **preview/dry-run** (`/bulk-upload/preview`, no escribe en DB, solo reporta un resumen incluyendo `missing_org_codes`) y **ejecución con imágenes** (`/bulk-upload/with-images`, sube CSV + ZIP de fotos, hace upsert por VIN). El fallback de resolución de organización interactúa mal con el chequeo de "unknown codes" del modo de ejecución — ver `code-quality-assessment.md` para el bug de diseño confirmado.
- **Citas (appointments)**: 5 endpoints CRUD + cambio de estado (`appointment_router.py`), registrados en producción (`main.py:389-393`) — confirmado este pase que están activos pese a que la documentación de test interna (`test_appointment_api.py`) tenía una nota desactualizada sugiriendo lo contrario.

## Actores / usuarios

- **Comprador (público)**: navega el marketplace, contacta vendedores, agenda citas. No requiere cuenta necesariamente (`public_product_router.py`).
- **Vendedor/dealer (seller)**: publica inventario, gestiona leads, ve analytics de su organización, publica a Facebook Marketplace.
- **Revisor/moderador (admin interno)**: aprueba/rechaza publicaciones en la cola de revisión.
- **Super admin / plataforma**: gestiona categorías, esquemas de atributos, y operaciones sensibles (`admin_router.py`, `admin_organizations_router.py`), con permiso `ORG_ADMIN_VIEW_ALL`/rol `super_admin` que le da acceso cross-org en la mayoría de los endpoints de `product_router.py` (`list_products`, cola de revisión, auditoría/reverse-transition, y — desde el intent `260910-export-cross-org` — también `export-client-format.zip`, que ya acepta `organization_id` y respeta el permiso). El intent `260911-export-org-selector` ya le dio a este actor un selector de UI (`OrganizationPicker`, cableado a `viewingOrgId`) para elegir una organización ajena puntual al exportar. **Gaps vigentes confirmados por el intent `260911-cross-org-export-ux`**: no puede exportar TODAS las organizaciones de una sola vez (solo una a la vez, o la propia); elegir una organización en el picker no filtra todavía lo que ve en la grilla de `/catalog` (solo afecta el export); y el catálogo exportado en formato cliente tiene columnas (`VIN`, tipo de carrocería, título limpio, grupos de Facebook, categoría/tipo, ubicación) vacías o con el valor crudo equivocado — ver § Funcionalidad clave arriba. Deuda conocida: verificar el TODO histórico de chequeo de rol admin en `marketplace_access_router.py` (ver `code-quality-assessment.md`).

## Alcance de este pase de reverse engineering

**Rescan completo** (full rescan, human-approved), disparado por el intent `260826-prod-bugfixes-batch`, que reemplaza íntegramente el store de codekb previo (incluyendo el scan enfocado de auth-navigation del intent `260829-auth-navigation-refactor`, cuyo contenido sustantivo sobre navegación OAuth/`proxy.ts`/`fetchWithAuth.ts` fue preservado e integrado en este documento y en `architecture.md`). Cobertura: dominio, aplicación (18 subdominios de use cases), 30 routers FastAPI, middleware backend, servicios de infraestructura (email, Facebook, publishers, tareas Taskiq), cliente API y stores del frontend, 31 rutas BFF de Next.js, configuración de build/CI/pre-commit. Ver `reverse-engineering-timestamp.md` § Scope of Analysis para el detalle exacto de rutas analizadas en profundidad vs. solo relevadas.

**Scan enfocado adicional** (intent `260830-ci-seed-data`, aditivo sobre el rescan completo anterior — no lo reemplaza): fue a profundidad sobre el área de seed data/schema de test de CI (`apps/api/scripts/create_test_schema.py`, `apps/api/src/prosell/infrastructure/database/{base,session,seed_categories}.py`, `apps/api/tests/conftest.py` + jerarquía de `tests/integration/conftest.py`), que el rescan previo solo había relevado a nivel de directorio. Ver `architecture.md` § Interaction Diagrams (nuevo diagrama de bootstrap de schema de test) y `code-quality-assessment.md` para los hallazgos.

**Scan enfocado adicional** (intent `260830-ci-fixes-round2`, aditivo sobre los dos pases anteriores — no los reemplaza): fue a profundidad sobre las áreas de batch review (`test_batch_review_api.py`), bulk upload CSV (`bulk_upload_vehicles.py`, `bulk_upload_preview.py`, `csv_field_mapper.py`), appointments (`appointment_router.py`, `test_appointment_api.py`) y fb-sync (`fb_sync_router.py::unpublish_callback`) — áreas que los pases anteriores solo habían relevado a nivel de directorio o no habían tocado. `fb_credential_migration_router.py` y su test quedaron BLOQUEADOS por una política de permisos local (`.claude/settings.local.json` deniega Read/Bash sobre rutas con "credential") — documentado como gap de cobertura, no como bug. Ver `architecture.md` § Interaction Diagrams (nuevos diagramas de bulk upload y fb-sync) y `code-quality-assessment.md` para los hallazgos completos.

**Scan enfocado adicional** (intent `260831-invalid-tailwind-classes`, aditivo sobre los tres pases anteriores — no los reemplaza): re-verificación de la deuda de clases Tailwind CSS inválidas ya catalogada, tras el fix de escala de spacing (`624819e3`) ya mergeado a `main` antes de este intent. Confirma que la deuda `h-9.5`/`px-4.5`/`h-8.5` está resuelta en las 4 rutas donde se había detectado, y aísla el único residuo genuinamente vigente hoy (`(seller)/publications/page.tsx`, familia `.25`/`.75`, 5 clases exactas con línea). No es una funcionalidad de negocio nueva — deuda técnica de UI, sin impacto funcional. Ver `code-quality-assessment.md` y `component-inventory.md` para el detalle.

**Scan enfocado adicional** (intent `260828-useeffect-to-react-query`, aditivo sobre los cuatro pases anteriores — no los reemplaza, foco en un área distinta a Tailwind): profundiza en los dos flujos de negocio afectados por la deuda ya catalogada de `useEffect` para fetch/mutación (Signal #6 de `code-quality-assessment.md`) — **alta de organización nueva** (`onboarding/page.tsx`, wizard multi-paso que consulta `orgApi.getMyOrganization()` en un `useEffect` de mount, con `orgApi.update()`/`orgApi.completeSetup()` como llamadas imperativas disparadas por botón) y **alta de usuario vía invitación por token** (`invite/[token]/page.tsx`, que dispara la mutación `teamApi.acceptInvitation()` en el mount con 5 estados de UI). Ambos son flujos de negocio sensibles con cero tests hoy. El scan confirma además el único precedente en el repo de hooks `useQuery`/`useMutation` colocados en un módulo de API (`notificationsApi.ts`) y triangula tres formas de manejo de error incompatibles en esta misma área (`ApiError`, `Error` genérico, matching de string) — ver `architecture.md` § Interaction Diagrams (nuevos diagramas 8 y 9) y `code-quality-assessment.md` para el detalle completo. No es funcionalidad de negocio nueva — es deuda técnica de patrón de data-fetching sobre dos flujos ya documentados arriba (Onboarding e invitaciones).

**Scan enfocado adicional** (intent `260901-frontend-test-debt`, aditivo sobre los cinco pases anteriores — no los reemplaza, foco distinto: deuda de tests unitarios frontend, no funcionalidad de negocio nueva): repara `apps/web/tests/unit/api/products.test.tsx` y `apps/web/tests/unit/lib/api/reverseTransitions.test.tsx`, ambos con mocks de `Product` desactualizados desde que el commit `7315fdf2` (2026-08-22) volvió requerido el campo `published_to_marketplace` de `productSchema` (ya reflejado en `business-overview.md` § Gestión de inventario/catálogo arriba — el ciclo de vida de estado de producto y sus 4 transiciones de "deshacer" `reverse`/`resubmit`/`restore`/`revert-sale` son exactamente lo que estos dos archivos ejercitan). No introduce ni cambia ninguna regla de negocio — es backfill mecánico de fixtures de test contra un contrato de dominio ya vigente. Ver `code-quality-assessment.md` y `architecture.md` § Interaction Diagrams (diagrama 10) para el detalle completo.

**Scan enfocado adicional** (intent `260902-teamapi-create-param`, aditivo sobre los seis pases anteriores — no los reemplaza, foco distinto: bug de mismatch de contrato en creación de equipos, no funcionalidad de negocio nueva): confirma un bug de nombre de parámetro entre `teamApi.create()` del frontend (envía `organization_id`) y `CreateTeamRequest` del backend (espera `org_id`) al dar de alta un equipo dentro de una organización — funcionalidad ya descrita arriba bajo "Onboarding e invitaciones". El bug tiene un lado simétrico no nombrado en la descripción original del intent: la respuesta también difiere (`TeamResponse.org_id` vs. `TeamSchema.organization_id`). Ninguno de los dos lados se manifestó nunca como falla visible porque `POST /api/v1/teams` (y las dos lecturas GET relacionadas) están hoy shadow-implementadas por una ruta BFF mock in-memory de Next.js que nunca reenvía al backend real — ver `architecture.md` § Interaction Diagrams (diagrama 11) para el detalle completo del mecanismo de shadowing. No introduce ni cambia ninguna regla de negocio — es un fix de contrato de wire, sin ambigüedad de diseño de dominio.

**Scan enfocado adicional** (intent `260828-zod-3-to-4-migration`, aditivo sobre los siete pases anteriores — no los reemplaza, foco distinto: sintaxis de validación Zod, no funcionalidad de negocio nueva): confirma que el paquete `zod` está en `^4.4.0` desde el commit `d1af1858`/`6042236b` (2026-07-20) — no es una instalación nueva de este intent — pero el código de `apps/web/src/lib/api/schemas/` (17 archivos) sigue escrito con sintaxis Zod 3 (`.passthrough()`, `z.nativeEnum()`). El scan confirma además, vía `gh issue view 74`, que el issue #74 ("chore(deps): migrate Zod 3 → Zod 4 for performance + unified error API") está **CERRADO** desde 2026-07-20 y su alcance propio nunca incluyó `.passthrough()`/`z.nativeEnum()` — solo el patrón de mensaje de error (`.min(n, "msg")` → `{ error: ... }`), ya migrado al 100% y verificado en 0 ocurrencias remanentes. Es decir: esta limpieza de idioma Zod 3 es alcance genuinamente nuevo, no la finalización de un trabajo abandonado. Deuda puramente técnica (sintaxis de validación de datos), sin impacto en reglas de negocio ni en el contrato de wire entre frontend y backend. Ver `code-quality-assessment.md` y `dependencies.md` para la corrección del framing "bloqueado por #74" y el detalle línea por línea.

**Scan enfocado adicional** (intent `260903-catalog-client-export`, aditivo sobre los ocho pases anteriores — no los reemplaza, foco distinto: exportación de catálogo en el mismo formato CSV que el cliente usa para importar, más un ZIP de imágenes por vehículo, no funcionalidad de negocio nueva ya existente): confirma que **ya existe** un export de catálogo (`GET /api/v1/products/export.csv`, FEAT-1 del intent `260826-prod-bugfixes-batch`), pero en formato **genérico** (columnas `UNIVERSAL_COLUMNS_ORDERED` + `attribute_schema` dinámico), no en las 24 columnas exactas que el cliente usa para importar (`docs/data39.csv`: `id;cod_dealer;price;category;type;location;year;make;model;mileage;body_style;exterior_color;interior_color;clean_title;state;fuel_type;transmission;option;description;path;groups;label;publicado;VIN`) — se necesita un pipeline de export distinto (endpoint nuevo o extensión del existente, decisión de diseño pendiente para Requirements Analysis), no una simple reutilización. El scan también confirma que `csv_export.py` ya implementa **casi** el patrón de carpeta de imágenes pedido por el intent (`build_image_folder_name()`: `{AÑO}-{MARCA}-{MODELO}-{MILLAS}K-{COLOR}-{CÓDIGO_ORG}`), pero con un bug real de mapeo de campo (lee `attrs.get("color")` en vez de `attributes["exterior_color"]`, que es donde el color del vehículo se guarda de verdad) — el segmento COLOR de la carpeta se pierde silenciosamente hoy para vehículos reales. No introduce funcionalidad de negocio nueva de cero — extiende/corrige un export ya existente y cierra un gap de UX ya conocido (selección de carpeta destino sin valor por defecto, FR8.3). Ver `architecture.md` § Interaction Diagrams (diagrama 12) y `code-quality-assessment.md` para el detalle completo.

**Scan enfocado adicional** (intent `260910-export-cross-org`, aditivo sobre los nueve pases anteriores — no los reemplaza, foco distinto: permiso cross-org del endpoint de export de catálogo, no funcionalidad de negocio nueva): confirma que `GET /api/v1/products/export-client-format.zip` (agregado por el intent `260903-catalog-client-export`, ver arriba) resuelve la organización exclusivamente del JWT del usuario que llama, sin respetar el permiso `ORG_ADMIN_VIEW_ALL` ni el rol `super_admin` que sí usan otros endpoints del mismo router (`list_products`, cola de revisión, auditoría/reverse-transition) — un usuario **Super admin / plataforma** (ver § Actores arriba) hoy solo puede exportar el catálogo de su propia organización, pese a que ya puede ver/gestionar el resto de las organizaciones en toda la app. El scan confirma, vía los propios artefactos de diseño del intent `260903-catalog-client-export` (`personas.md`), que esta exclusión fue una decisión de scope explícita del diseño original, no un descuido de implementación — el objetivo de este intent es ampliar ese scope para que el endpoint de export honre el mismo modelo de permisos cross-org que el resto del router. Ver `architecture.md` § Interaction Diagrams (diagrama 13) y `code-quality-assessment.md` para el detalle completo.

**Scan enfocado adicional** (intent `260911-export-org-selector`, aditivo sobre los diez pases anteriores — no los reemplaza, foco distinto: cómo un `super_admin`/`ORG_ADMIN_VIEW_ALL` elige QUÉ organización exportar desde la UI, no funcionalidad de negocio nueva): el intent `260910-export-cross-org` (ver arriba) cerró el gap de **backend** — el endpoint ya acepta `organization_id` y respeta `ORG_ADMIN_VIEW_ALL`/`super_admin` — pero el botón "Exportar catálogo (formato cliente)" de `/catalog` sigue sin ningún mecanismo de UI para que un **Super admin / plataforma** elija la organización ajena a exportar: `exportCatalogClientFormat()` no manda `organization_id`, y `catalog/page.tsx` no importa ni `useAuth` ni `organizationStore`. El scan confirma un hallazgo central: la app YA TIENE un selector de organización completo, testeado y en producción — `OrganizationPicker.tsx` (renderizado en el `Header` global, gateado por `isAdmin` + defensa en profundidad vía `Permission.ORG_ADMIN_VIEW_ALL`) respaldado por `organizationStore.viewingOrgId` — pero ese estado global está **dormido**: ningún hook de datos de la app (ni catálogo, ni cola de revisión) lo consume hoy para filtrar nada; el selector solo lee/escribe su propio estado. Queda una bifurcación de diseño explícita para Requirements/Functional Design, no resuelta en este scan: (a) cablear el export al `viewingOrgId`/`OrganizationPicker` global existente, convirtiéndolo en su primer consumidor real; o (b) un selector local independiente, acotado solo al flujo de export (p. ej. dentro de `ExportSummaryBanner`), sin tocar el estado global del header. Ver `architecture.md` § Interaction Diagrams y `code-quality-assessment.md` para el detalle completo.

**Scan enfocado adicional** (intent `260911-cross-org-export-ux`, aditivo sobre los once pases anteriores — no los reemplaza, foco distinto: cerrar los gaps de negocio que quedan tras conectar el selector de organización al export en `260911-export-org-selector`, no funcionalidad de negocio nueva de cero): profundiza en cuatro necesidades reales de negocio para el **Super admin / plataforma** (ver § Actores arriba) y para cualquier organización que exporte su propio catálogo: (1) poder exportar el catálogo de TODAS las organizaciones en una sola corrida, no solo una a la vez — la capacidad de traer datos de todas las organizaciones ya existe en la capa de datos del backend, falta exponerla a través del flujo de export; (2) que elegir una organización en el selector realmente filtre lo que se ve en pantalla en `/catalog`, no solo lo que se exporta — hoy son dos cosas independientes; (3) que el selector solo muestre organizaciones que efectivamente tienen productos, en vez de la lista completa (~29, la mayoría vacías) — el dato para filtrar ya está disponible, falta aplicarlo; (4) que el CSV exportado en formato cliente tenga los valores reales de VIN, tipo de carrocería, estado del título, grupos de Facebook, categoría/tipo y ubicación — hoy varias de estas columnas salen vacías o con el formato crudo interno en vez del formato esperado por el cliente, porque el export nunca aplicó las mismas conversiones de valor que sí se usan al importar ese mismo CSV. Se agrega también la posibilidad de indicar una carpeta base para las imágenes y una lista de grupos de Facebook al momento de exportar, mediante el mismo tipo de ventana de confirmación (con un valor sugerido por defecto) que el flujo de export ya usa hoy para pedir el nombre del archivo. Ver `architecture.md` § Interaction Diagrams (diagrama 15) y `code-quality-assessment.md` para el detalle completo.

**Scan enfocado adicional** (intent `260915-vehicle-catalog`, aditivo sobre los doce pases anteriores — no los reemplaza, foco distinto: consistencia del catálogo editorial de valores de campos del schema de vehículos frente al flujo real de autocompletado por VIN, no funcionalidad de negocio nueva): profundiza en un riesgo de negocio real, no antes catalogado — **existen DOS catálogos independientes de "qué valores acepta Facebook" con formatos incompatibles entre sí**. (1) El catálogo histórico `nhtsa_normalizer.py` (`NHTSA_TO_FACEBOOK`), que traduce lo que el VIN decodificado por NHTSA trae, a tokens en **inglés/minúscula** (`"suv"`, `"gasoline"`, `"FWD"`) — es el que efectivamente autocompleta un campo del formulario cuando el vendedor decodifica un VIN (`VinDecodeField.tsx`). (2) El catálogo nuevo `facebook-values/index.ts` (agregado esta semana, commits `b0015223`/`c1c86138`), que trae los strings oficiales en **español** (`"SUV"`, `"Gasolina"`) que el admin usa para poblar manualmente las `options` de un campo del schema vía el botón "Load from Facebook catalog" del editor de schema. Si un admin puebla las `options` de un campo con el catálogo nuevo (español) y luego un vendedor decodifica un VIN, el autocompletado seguirá llegando en el formato del catálogo viejo (inglés/minúscula) — un valor que no calza con ninguna de las `options` configuradas, sin que ningún chequeo en tiempo real lo detecte (la validación de `options` corre solo del lado del backend al guardar, no en el momento del autocompletado). Es un riesgo de UX/calidad de dato silencioso para el flujo central del negocio (publicar un vehículo con datos completos y consistentes), no un bug de un módulo aislado. Confirma además que el mecanismo de publicación a Facebook (`IPublisherService`/`PublisherStrategySelector`, ver § Funcionalidad clave arriba) ya existe y está en producción — la pregunta real para Requirements/Functional Design es si necesita extenderse para absorber esta desalineación, no diseñarlo de cero. Ver `code-quality-assessment.md` (hallazgo central de este pase) y `architecture.md` § Interaction Diagrams para el detalle completo.

## Fuera de alcance (según memoria del proyecto y este rescan)

- Marketplace público completo / SEO — parcialmente implementado, no es el foco de MVP.
- E-commerce completo (pagos de compra directa al comprador) — sin integración de cobro verificada (ver nota sobre `stripe` sin uso arriba).
- App móvil nativa.
- Scraping genérico multi-marketplace y pricing/recomendación por IA — no implementados; ver corrección de brecha arriba.
