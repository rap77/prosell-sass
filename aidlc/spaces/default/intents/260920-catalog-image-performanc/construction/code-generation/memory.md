<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->

> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations

<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->

- 2026-09-23T22:34:00Z — chose to add a separate nullable `thumbnail_image_key` column rather than repurposing the existing `cover_image_key`. The plan's "preserve `cover_image_key` as the gallery-cover selection" guard rail binds: gallery-cover stays a first-class field, the new thumbnail is a separate first-class field, neither mutates the other.
- 2026-09-23T22:34:00Z — chose to put the CDN purge compensation logic inside `PurgeProductImageUseCase` (application layer) rather than in the handler. The use case owns the policy (sync invalidate, fallback to retry, audit outcome) and the handler just builds the 2xx reply with the outcome label. Keeps the handler thin and the policy unit-testable without the FastAPI machinery.
- 2026-09-23T22:34:00Z — chose to wire `cdn_invalidator` and `task_dispatcher` via FastAPI `Depends()` (not `__import__` with `fromlist`) after the first attempt hung in tests. The router module is re-exported via `routers/__init__.py` (`from prosell.infrastructure.api.routers.product_router import router as product_router`), so `__import__("prosell.infrastructure.api.dependencies", fromlist=["get_cdn_invalidator"])` resolves to a non-API entry — the handler returned the dependency value correctly, but FastAPI's response-model serialization against an `AsyncMock` somewhere downstream caused a hang. Switching to proper `Annotated[..., Depends(...)]` made the dependency chain explicit and removed the ambiguity.

## Tradeoffs

<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->

- 2026-09-23T22:34:00Z — picked a single shared `PurgeProductImageUseCase` over a per-handler retry-loop. Both the DELETE endpoint AND any future PATCH / replace-image flow can call the same use case; the retry contract lives in one place. Trade-off: the use case depends on three ports (invalidator, dispatcher, storage) instead of two, but that's the minimum for sync + retry + housekeeping.
- 2026-09-23T22:34:00Z — picked `httpx.AsyncClient` injection in `DigitalOceanCdnInvalidator.__init__` over a module-level singleton. Tests pass a `MagicMock()` with `client.post = AsyncMock(...)` to assert on the request shape; production gets a default-constructed `AsyncClient()` shared across calls. Same pattern as `DOSpacesService`.
- 2026-09-23T22:34:00Z — picked 100 as the OQ3 cap (not the 50 from A4). A4 says "≤50 typical"; 100 leaves headroom for filter views without enabling unbounded fan-out. If the catalog grows beyond that, the cap is a single constant.

## Deviations

<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->

- 2026-09-23T22:34:00Z — Steps 2-7 (thumbnail persistence, pipeline, CDN config, signed URL routing) were already implemented in commit `23870cd2` (feat(images)) and merged into main before this Code Generation session started. The plan fingerprint approved the surface decisions in that commit; this run verified the baseline (Step 1) and continued from Step 8 (batch endpoint + invalidation + frontend). No production code rewound or duplicated.
- 2026-09-23T22:34:00Z — Two pre-existing tests (`test_image_router_signed_url.py`, `test_image_upload_cross_org.py`) had mocks that did not cover `spaces.generate_cdn_download_url` because the upload route switched to that signer in the merged commit. Fixed by adding `AsyncMock(return_value=...)` for the CDN signer in both `_make_spaces()` fixtures. The fix is a direct consequence of the merged commit's surface (no plan step added those mocks earlier).
- 2026-09-23T22:34:00Z — The original plan referenced the new column as `cover_image_key` in some FRs (e.g. FR2.3). The actual implementation added `thumbnail_image_key` to avoid repurposing the existing `cover_image_key`. The plan's "preserve cover_image_key" guard rail was honored (the existing selection is unchanged); only the new column's name differed from the FR wording.
- 2026-09-24T03:30:00Z — `aidlc engine review-brief` (all three verbs: `review`, `context`, `summary`) fails with `{"error":"aidlc-review-brief.ts does not export main(argv)"}`, even though the source file (`.claude/tools/aidlc-review-brief.ts`) does export `main(argv)` at line 934 — looks like the shipped `aidlc` binary is built from a stale compiled copy of that tool, not a usage error. Worked around by reading the review record's markdown copy directly (`<stage dir>/reviews/review-NN.md`) and presenting its findings table to the human by hand instead of via the tool's rendered brief.
- 2026-09-24T02:50:00Z — `traceability.json` was originally written with a non-conforming shape (`entries`/`summary` instead of the sensor-required `upstream_ids`/`coverage`); fixed to the documented schema, which resolved the JSON-shape finding. The `traceability` sensor's unit-context resolver then reported a second, expected finding: it derives the construction unit from the record path via a `/construction/<unit>/<stage>/traceability.json` regex, which requires two path segments after `construction/`; this stage's real `produces[]` path (`construction/code-generation/traceability.json`) has only one, so unit resolution always fails here — the same zero-Unit path-doubling engine quirk already documented in `project.md` for the plan-approval-guard and testing-posture fingerprint tools. Verified via the documented workaround (temporary doubled-path copy, sensor re-run, copy discarded): with unit resolution unblocked, the sensor's fallback ID resolver (no `stories.md` present) additionally treats the bare FR/NFR _group headers_ in `requirements.md` (`FR1`..`FR6`, `NFR1`..`NFR5`) as expected upstream IDs, distinct from the granular `FRx.y`/`NFRx.y` items already covered — a regex artifact of the resolver, not a real gap in this Unit's traceability. `traceability` is `default_severity: advisory` and fires on write (advisory in this release per stage-protocol.md §14), so this does not block the gate; left unresolved rather than adding synthetic header-level coverage rows for section headings that aren't real requirement IDs.

## Open questions

<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->

- 2026-09-23T22:34:00Z — NFR1.2 (p95 ≤ 300 ms for K≤50 batch signing) and NFR1.3 (p95 ≤ 200 ms for thumbnail CDN fetch with warm cache) require measured evidence against a real CDN endpoint. Unit tests assert behavioral correctness only. Performance validation belongs to Stage 4.6 (`performance-validation`), not Code Generation. NOT claimed as satisfied in `traceability.json` (marked `Deferred` with the relevant stage target).
- 2026-09-23T22:34:00Z — If legacy products (no `thumbnail_image_key`) end up dominating the catalog grid in production, the runtime fallback may cause perceptible 404→placeholder flashes on first load. A5 estimated it would be a small subset; if observability proves otherwise, a one-shot Taskiq backfill job is the natural follow-up (out of scope for this bugfix).
