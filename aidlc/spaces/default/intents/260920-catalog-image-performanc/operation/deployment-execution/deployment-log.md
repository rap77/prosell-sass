# Deployment Log — Catalog Image Load Optimization

## Pre-deployment state

`main` was diverged from `origin/main`: 4 local commits ahead (including
this intent's implementation), 2 origin-only commits behind (one a
duplicate of a local commit — same message/author, different session; one
an unrelated real fix, `fix(catalog): default clean_title...`, touching
files this intent never touches). Confirmed via `git diff --name-only` that
neither origin-only commit overlapped with this intent's changed files
before proceeding.

## Commit and push sequence

1. Staged and committed this intent's 40 changed/new files
   (`4a9426bb` → later `ecd8a1dd` after rebase): `fix(catalog): eliminate
N+1 cover-URL fan-out with batch endpoint and CDN thumbnails`.
   - First commit attempt (`b24cnej7g`) was blocked by the pre-commit GGA
     hook with 4 real findings across `dependencies.py` (missing return
     type on `get_cdn_invalidator()`), `product_router.py` (8 parameters
     using the old `= File(...)/Query(...)/Form(...)` default pattern
     instead of `Annotated[...]`, FastAPI's B008-safe form), and
     `purge_cdn_cache_task.py` (`dict[str, Any]` return type narrowed to
     `dict[str, str]`). A 5th flagged finding (docstring indentation in
     `i_task_dispatcher.py`) was verified as a GGA hallucination
     (`ruff format --check` confirmed the file was already correctly
     formatted) and left unchanged. All 4 real findings fixed; re-ran the
     full backend unit suite (1410 tests) to confirm no regression from
     the fixes.
   - Second attempt passed all 5 GGA batches; blocked only by the
     `trailing-whitespace` auto-fix hook modifying the audit shard file —
     re-staged and committed successfully.
2. A small trailing audit-log append (7 lines, generated by the commit
   process itself) required one follow-up commit
   (`chore(aidlc): commit audit trail from deployment-execution commit
attempt`) before the working tree was clean enough to rebase.
3. `git pull --rebase origin main` — succeeded cleanly. Git automatically
   detected and skipped the duplicate `chore(ci): remove temporary prod
diagnostic workflow` commit (`d10d2855`, identical effect to the
   already-local `23be72f3`/`9781f1b3`) via `skipped previously applied
commit`; the real remote fix (`639ca18e`) rebased in with no conflicts.
4. `git push origin main` — succeeded (`639ca18e..6771dfed`). Pre-push
   hooks (prettier, ruff, pyright, full backend pytest suite) all passed.

## Pipeline execution (automatic)

| Step                             | Trigger                     | Result                                                                                                          |
| -------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------------------------- |
| CI (run 35993948835)             | `push` to `main`            | Success — all 7 jobs (Lint Python/Node, Test Python/Node, Validate Spec Status, Validate Code Standards, Build) |
| Deploy Staging (run 35994273387) | `workflow_run` (CI success) | Success — `deploy-staging` job                                                                                  |

Both ran automatically per the existing pipeline documented in
`operation/deployment-pipeline/cd-config.md` — no manual intervention
beyond the commit/push above.

## Production promotion

**Not executed.** `promote-prod.yml` requires a separate, explicit
`workflow_dispatch` with the typed `"deploy"` confirmation input — this is
the project's permanent, human-owned production gate (`project.md` §
Mandated) and is never triggered automatically by this stage or any
AI-DLC automation. Recommendation: promote once the human has reviewed this
deployment's results, including the two accepted-risk gaps (FR4.1, FR3.3)
and the structured-logging observability gap found during smoke testing.

## Database migrations

`20260922_0001_add_thumbnail_image_key_to_product.py` — confirmed applied
automatically on staging container startup (see `smoke-test-results.md`).
Nullable column, no backfill, reversible — matches
`operation/deployment-pipeline/rollback-runbook.md`.
