**Collaborator:** aidlc-quality-agent

## Contribution

El piso de equipo ya afirmado (test-after, sin backfill de cobertura no tocada, 40% frontend/sin piso backend, suite completa verde en CI) alcanza como marco general para este intent, pero — igual que en `260903-catalog-client-export` — hace falta un piso mínimo específico afirmado en la entrevista, porque toda la superficie nueva de este intent es 100% frontend y 100% sin cobertura previa. Sin ese piso puntual, "test-after" por sí solo no obliga a cubrir el caso de seguridad más importante del feature (el gating por permiso).

Piso mínimo que recomiendo confirmar en Step 4:

1. **Regresión explícita de gating negativo, no solo camino feliz**: un caso de test donde el usuario NO tiene `ORG_ADMIN_VIEW_ALL` debe demostrar que (a) el selector no se renderiza / no está disponible en el flujo de export, y (b) `exportCatalogClientFormat()` se invoca sin `organization_id` (o con el valor por defecto del propio tenant), nunca con un `organization_id` ajeno. Esto es un caso límite de seguridad de UI, análogo al patrón ya establecido en `team.md` de exigir casos límite explícitos (no alcanza con un solo camino feliz) — y particularmente sensible acá porque el propio hallazgo #68 de `code-quality-assessment.md` ya mostró en este mismo dominio (cross-org) que un test mal diseñado puede terminar afirmando como "correcto" exactamente el comportamiento que un intent busca cambiar. Un test de wiring que solo cubre el camino feliz (usuario con permiso, selector visible, export con `organization_id` correcto) no sería suficiente piso para este intent.
2. **Test del wiring nuevo en sí** (`exportCatalogClientFormat` recibiendo el `organization_id` correcto cuando cambia la selección) — es superficie de test completamente nueva, no hay nada pre-existente que "mantener en verde" acá, consistente con cómo `team.md` ya trató la superficie de test nueva de `handleExportCsv`/`exportCatalogCsv` en el intent anterior.
3. Si Requirements/Functional Design resuelve la bifurcación (a)/(b) por la opción (a) — reutilizar `organizationStore.viewingOrgId` como su primer consumidor real de datos —, agregar al menos un caso que confirme que ese consumo nuevo no rompe el comportamiento actual de `OrganizationPicker` en el header (mismo store, dos consumidores ahora). No hace falta un test de integración cross-componente completo; alcanza con verificar que el selector de export lee/escribe el mismo store sin introducir un segundo mecanismo de estado paralelo (esto último ya lo señala `team-practices.md` como punto de Code Style, pero tiene una consecuencia de testing directa: si aparece un mecanismo de estado paralelo, el punto 3 no se puede satisfacer con un solo test compartido).

Sobre `OrganizationPicker.test.tsx` como precedente: **sí es directamente reusable**, no solo transferible como patrón de diseño (a diferencia del precedente de ZIP de `260903`, que sí requería una suite de casos nueva). El hallazgo #75 de `code-quality-assessment.md` confirma que ese archivo ya mockea `useAuth`, `useOrganizations` y `useOrganizationStore` — exactamente las tres dependencias que cualquier componente/hook nuevo de este intent (vía (a) o (b)) va a necesitar mockear. Recomiendo que la entrevista confirme que Build and Test debe reusar ese mismo patrón de mocks en vez de inventar uno nuevo, sea cual sea la vía de diseño elegida.

Un gap adyacente que la entrevista NO debería absorber en el piso de este intent: `team.md` ya documenta que `handleExportCsv`/`exportCatalogCsv` en `catalog/page.tsx` tienen cero cobertura hoy, señalado como gap a resolver en Build and Test del intent `260903` (no confirmado si ya se resolvió). Ese es código pre-existente no tocado por este intent — el piso de "test-after sin backfill" aplica: no corresponde exigir cobertura retroactiva de ese handler como parte de este intent, solo del wiring nuevo de selección de organización. Vale la pena que Build and Test verifique el estado actual antes de asumir qué ya está cubierto, para no duplicar ni omitir sin querer.

Finalmente, reconfirmo el gap ya señalado en `team.md` (no resuelto, que yo pueda ver): `catalog/page.tsx` no calza limpio en ninguno de los dos patrones de ubicación de test vigentes (`tests/components/{module}/X.test.tsx` vs. co-located `page.test.tsx`). Este intent va a agregar tests nuevos exactamente en esa zona gris — recomiendo que la entrevista o Build and Test resuelva explícitamente cuál convención aplica antes de escribir los tests del selector/wiring, para no reabrir la ambigüedad por segunda vez sin decisión.

## Positions

- AGREE: la recomendación del lead de que este intent no requiere ninguna especialización nueva de práctica de equipo en Testing Posture (metodología, ordering, piso de cobertura, gates de CI) — es correcto que la asimetría de cobertura y el resto del marco general siguen vigentes sin cambios.
- AGREE: que la bifurcación de diseño (a)/(b) es una decisión de producto/arquitectura del feature, no una práctica de equipo, y que no bloquea a Practices Discovery — pero sí bloquea fijar el piso de test EXACTO (ya lo señala el propio draft), por eso el piso que propongo arriba está formulado para ser válido bajo cualquiera de las dos vías.
- AGREE: que `OrganizationPicker.test.tsx` es un precedente relevante — con la precisión adicional (no una objeción) de que acá es reusable directamente como patrón de mocks, un nivel más fuerte que el precedente "transferible pero con suite nueva" de `csv_export.py` en `260903`.
- OBJECT: None.
