<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->

> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations

<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->

## Deviations

<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->

- 2026-08-27T05:00:00Z — `aidlc-testing-posture.ts fingerprint --unit code-generation` computes `stageDir` as `construction/<unit>/code-generation/`, doubling the path for this zero-Unit stage whose real artifacts live at `construction/code-generation/` (no unit segment) — same root cause as the already-learned `plan-approval-guard`/`knownUnits()` bug in project.md, but in a different tool. Workaround: temporarily duplicated `code-generation-plan.md` + `unit-test-instructions.md` at the doubled path, ran fingerprint there, deleted the duplicate immediately after. No dispatch-guard was actually hit this time since code generation ran inline (developer role) rather than via Task dispatch, per the same established workaround.
- 2026-08-27T05:10:00Z — `unit-test-instructions.md` committed to "create a minimal happy-path test if a touched file lacks one" — overstated the actual obligation. `requirements.md` FR3 only requires the EXISTING suite stay green; the scope's own testing-posture floor ("refactor: adds no extra new-test floor") does not require backfilling tests for files that had zero coverage before this refactor touched them. For `onboarding/page.tsx` and `BulkUploadCSV.tsx` (no prior test file, behavior-preserving try/finally→promise-chain rewrite only): verified via lint + typecheck + the same pattern already validated with tests on 7+ other files this session, did not create new test files. Correcting `unit-test-instructions.md`'s wording to match FR3 rather than backfilling test debt this intent didn't scope in.

## Tradeoffs

<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->

- 2026-08-27T06:00:00Z — Adversarial review iteration 1 (aidlc-architecture-reviewer-agent) returned NOT-READY with 3 findings. Fixed all 3 directly (inline, same workaround as original generation — no separate lead re-dispatch): (1) CRITICAL — `fb-accounts/page.tsx`'s FR2.6 fix silently defaulted `formatDate()` to `en-US`, a real observable behavior change on a Spanish-UI page; fixed by passing explicit `"es-AR"`, matching the existing convention already used in `(seller)/publications/page.tsx`. (2) MAJOR — `traceability.json` FR2.4 status corrected from `OK` to `N/A` to match the FR2.7 pattern for fully-rejected findings. (3) MINOR — `unit-test-instructions.md`'s stated test floor corrected to match what was actually applied (no backfill for previously-untested touched files). Verified the critical fix: lint+typecheck clean, rescan confirms 0 remaining diagnostics on that file, score unchanged at 54.

## Open questions

<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->

- 2026-08-27T05:50:00Z — FR2.2 manual verification (3 of 60 unused-export/unused-file candidates, per plan — no deletion this pass): `src/components/admin/marketplace-access/ConfirmActionDialog.tsx` and `src/components/admin/marketplace-access/hooks.ts` (`useFilteredGrants`) — CONFIRMED unreachable, zero importers anywhere in `src` (grepped both the file path and the exported symbol name). Surprising: `hooks.ts` is the same file touched in this session's earlier marketplace-access cleanup commit (`588f7550`) — it was extracted for reuse but never actually wired into `MarketplaceAccessManager.tsx`, which keeps its own inline filtering logic instead. Worth flagging to the human: either wire the hook in (dedupe the inline logic) or delete it, but that's a product/architecture call beyond this intent's "delete confirmed-dead code" scope. `check-pages.ts` — INCONCLUSIVE: no importer and no package.json script entry, but could be a deliberately-standalone dev utility meant to be run ad-hoc (`npx tsx check-pages.ts`); different risk profile than a truly-dead component, needs a human call rather than a confident classification.
- 2026-08-27T05:40:00Z — FR2.6b (`no-fetch-response-used-without-status-check`, `app/api/v1/auth/2fa/disable/route.ts:23`): REJECTED, not fixed. This route is a byte/status passthrough proxy — it deliberately forwards the backend's status AND body verbatim (`NextResponse.json(await response.json(), { status: response.status, ... })`) for both success and error responses, which is the correct behavior for a proxy (a 401/400 from the backend must reach the client with its original status and message, not get swallowed). The rule's generic fix (`if (!response.ok) throw`) would be a regression here — it would convert a legitimate backend error response into a generic 502 "Proxy error", losing the real status/message. The only genuine edge case (backend returns a non-JSON body, `response.json()` throws, falls into the catch-all and loses the real status) is a different, narrower defect than what this rule targets — same class as the already-known BFF proxy content-type bug documented in project.md, but not what's flagged here.
- 2026-08-27T05:30:00Z — FR2.4 (`js-combine-iterations`, review-queue/page.tsx:100/117 + organizations/[id]/page.tsx:581): checked both sampled occurrences against the rule's own evidence boundary ("confirm a production profile shows the traversal or allocation is material... for ordinary collections, the readable chain is often the better tradeoff"). Both are `.filter().map()` chains over small, UI-bounded arrays (a human's batch-selected product IDs; an org's FB account list) — no production profiling available, no reason to believe traversal cost is material, and one of the two runs directly in JSX render where a manual loop would be strictly less readable for zero measurable gain. REJECTED for this sample — hypothesis not confirmed, not applying a rewrite. Not evaluating the remaining ~16 occurrences in this pass; flagging the category as likely low-value across the board given the codebase's typical array sizes (admin/seller UI lists, not high-throughput data pipelines), pending someone actually profiling a specific page if they suspect otherwise.
- 2026-08-27T05:20:00Z — FR2.7 (`tenant-static-proxy-risk` x3, organizations.ts:187/userApi.ts:156/verticals.ts:138): all 3 are REJECTED as false positives, not fixed. The rule's own scope is "server route source files only" (server-context dirs, middleware._/route._ entrypoints) — these 3 files are browser-side React Query hooks under `lib/api/` calling this app's OWN `/api/v1/...` REST endpoints with an org ID in the path, not a server-side asset-fetch/object-store call. No file/S3/disk access happens in this code at all; tenant authorization is enforced server-side in the FastAPI backend from the JWT (project convention, confirmed in codekb). None of the rule's trigger primitives (`getObject*`, `getSignedUrl`, `createReadStream`, `path.join` for asset paths) are present — only a same-origin `fetch()` to a REST resource URL, which is the rule's own documented suppress case ("tenant value already bound to the authenticated org... only namespaces the path"). Flagging for requirements-analysis/future-intent review: react-doctor's directory-scope claim for this rule may not match its actual detection behavior in this codebase (it fired outside its stated `server route source files only` scope) — worth reporting upstream or configuring an ignore-tag if it keeps false-positiving on `lib/api/*` client hooks.
