# Business Rules — U1 (`u1-vehicle-catalog-api`)

```yaml
rules:
  - id: BR1.1
    statement: Reconciliar un valor normalizado de NHTSA contra el catálogo canónico de un campo, devolviendo el valor exacto o ausencia de match.
    category: calculation
    applies_to: FacebookVehicleValueCatalog
    trigger: Decode de VIN exitoso (nhtsa_normalizer.py ya produjo un valor normalizado para un campo select-backed).
    logic: >
      IF el valor normalizado aparece en accepted_raw_aliases de alguna CanonicalFieldOption de ese field_key
      THEN devolver el canonical_value exacto de esa entrada.
      ELSE devolver None (sin match).
    violation_behaviour: No aplica — es una función pura de lookup, no una validación que pueda "violarse".
    source: FR1.1, AC1.1.1

  - id: BR1.2
    statement: Un campo sin match reconciliado queda vacío en la respuesta del decode de VIN, y su nombre se agrega a unmatched_fields — no se devuelve el valor crudo sin reconciliar. Un campo sin catálogo aplicable (0 entradas para ese field_key) no se toca en absoluto.
    category: validation
    applies_to: VehicleVinDecodeService
    trigger: NHTSA devolvió un valor crudo para un field_key select-backed, ya normalizado por nhtsa_normalizer.py.
    logic: >
      Corrección post-revisión (el IF/ELSE de 2 ramas original no cubría el 3er caso — reescrito a 3 ramas explícitas):
      CASE 1 — BR1.1 devuelve un canonical_value (hubo match): el campo en `vehicle` se devuelve con ese canonical_value.
      CASE 2 — get_options(field_key) tiene al menos una entrada (catálogo aplicable) PERO BR1.1 devuelve None (sin match):
        el campo en `vehicle` se devuelve null, Y el nombre del campo se agrega a `unmatched_fields`.
      CASE 3 — get_options(field_key) no tiene ninguna entrada (0 entradas, catálogo NO aplicable para ese field_key):
        el campo se devuelve con el valor normalizado tal cual (sin reconciliar), SIN agregarlo a `unmatched_fields`
        — no es un mismatch, es un campo fuera del alcance del catálogo canónico.
      La señal para distinguir CASE 2 de CASE 3 es get_options(field_key) is None (0 entradas) vs. get_options(field_key)
      no vacío — NO una referencia a BR1.4 (que solo define el endpoint HTTP, no este flujo de decode de VIN).
    violation_behaviour: No aplica — comportamiento del contrato, no una validación de guardado.
    source: FR1.2, AC1.1.2

  - id: BR1.3
    statement: Un valor ya reconciliado (proveniente de BR1.1 y aceptado tal cual por el usuario) pasa Category.validate_attributes() sin error, igual que hoy.
    category: validation
    applies_to: Category
    trigger: Guardado de un vehículo con un atributo cuyo valor coincide con una option configurada del schema.
    logic: "IF el valor del atributo está en las options configuradas del schema (comportamiento ya existente, sin cambios) THEN pasa la validación."
    violation_behaviour: "N/A — esta regla no cambia; se documenta acá solo para dejar explícito que sigue vigente sin modificación (FR1.3)."
    source: FR1.3, AC1.1.3, AC1.1.4

  - id: BR1.4
    statement: El endpoint de opciones canónicas devuelve el conjunto completo de valores canónicos vigentes para un field_key reconocido; para un field_key sin catálogo, devuelve 404 y registra un warning server-side. Requiere solo autenticación, sin gate de Platform Admin.
    category: validation
    applies_to: FacebookVehicleValueCatalog
    trigger: GET /categories/facebook-values/{field_key} (Contrato 2, contract-summary.md).
    logic: >
      field_key usa el MISMO vocabulario attribute_schema/VIN-decode que BR1.1 (ver entities.md, corrección post-revisión)
      — NO el enum FacebookFieldKey del frontend; la traducción entre ambos es responsabilidad de Functional Design de U2.
      IF field_key tiene al menos una CanonicalFieldOption
      THEN devolver 200 con la lista de canonical_value de todas sus entradas (ni una de más ni de menos).
      ELSE devolver 404, Y loguear un warning server-side con el field_key no reconocido (Q1 de esta etapa, opción A)
           — para distinguir un campo genuinamente sin catálogo de un bug de configuración.
      Distinto de BR1.2: acá "sin catálogo" es el caso completo (0 entradas para el field_key), no "hubo entradas pero ninguna matcheó".
    authorization: >
      Corrección post-revisión (Major #3, decisión de bajo riesgo resuelta directo): requiere únicamente `CurrentUser`
      (autenticación), SIN `_require_platform_admin()` — mismo patrón ya usado por GET /{category_id}/schema
      (`category_router.py`), que también es una lectura sin dato sensible por tenant. El editor de schema que lo
      consume está gateado a Platform Admin del lado de la UI, pero el endpoint en sí no necesita duplicar ese gate
      porque no expone nada que un usuario autenticado cualquiera no pueda ya inferir (es un catálogo estático,
      igual para toda la plataforma, sin segmentación por organización).
    violation_behaviour: 404 con mensaje indicando el field_key no reconocido.
    source: FR1.4, AC1.2.1

  - id: BR3.1
    statement: Migrar ad-hoc los registros de vehículo legacy que no cumplen el catálogo canónico, con guardas propias de este caso (no una copia literal de un precedente) más una guarda anti-drift entre el armado del batch y su aplicación.
    category: policy
    applies_to: migración legacy (script ad-hoc, no un componente persistente)
    trigger: Ejecución manual del script de migración (una sola vez, no un job recurrente).
    logic: >
      IF un producto de vehículo tiene un atributo cuyo valor no calza con ninguna CanonicalFieldOption del field_key correspondiente
      THEN se incluye en el conjunto a migrar.
      Antes de aplicar cualquier cambio, para cada registro candidato: (1) verificar que el producto todavía existe,
      (2) verificar que el nuevo valor calza con el catálogo canónico, (3) verificar que el registro no fue modificado
      por otro proceso desde que se armó el conjunto a migrar (guarda anti-drift/re-ejecución).
      Corrección post-revisión (Major #4): estas 3 guardas son propias de este caso, NO una copia literal de las de
      `20260812_0002_migrate_legacy_sedan_products.py` (esa migración valida existencia de categoría destino y
      re-verifica que los productos legacy siguen coincidiendo con la categoría/condición esperada — un shape
      distinto porque cambia category_id, no atributos). El espíritu que SÍ se toma prestado del precedente es:
      validar antes de mutar, y proteger contra drift de estado entre el armado del batch y su aplicación.
    violation_behaviour: Si cualquiera de las 3 guardas falla para un registro puntual, ese registro se excluye de la migración y se reporta en el log de resumen — no se aborta la migración completa por un registro puntual.
    source: FR3.1

  - id: BR5.1
    statement: Sanitizar contra inyección de fórmulas cualquier valor de celda del CSV cliente que empiece con =, +, -, o @.
    category: validation
    applies_to: build_client_format_row (csv_export.py)
    trigger: Construcción de una fila del CSV de export cliente (export_catalog_client_format.py).
    logic: >
      IF un valor de celda (string) empieza con uno de los caracteres =, +, -, @
      THEN prefijarlo con una comilla simple (') antes de escribirlo — el valor visible para el cliente no cambia
           (Excel/Sheets muestra el valor sin la comilla), pero deja de interpretarse como fórmula.
      ELSE escribir el valor tal cual.
    violation_behaviour: N/A — es una transformación defensiva, no un rechazo de guardado.
    source: FR5.1
```

## Resumen de reglas

| ID    | Categoría   | Aplica a                                | Origen |
| ----- | ----------- | --------------------------------------- | ------ |
| BR1.1 | calculation | FacebookVehicleValueCatalog             | FR1.1  |
| BR1.2 | validation  | VehicleVinDecodeService                 | FR1.2  |
| BR1.3 | validation  | Category (sin cambios)                  | FR1.3  |
| BR1.4 | validation  | FacebookVehicleValueCatalog             | FR1.4  |
| BR3.1 | policy      | Migración legacy (script ad-hoc)        | FR3.1  |
| BR5.1 | validation  | build_client_format_row (csv_export.py) | FR5.1  |

FR4 (documentación de `IPublisherService`) no genera una regla de negocio nueva — no hay comportamiento nuevo que testear, solo un artefacto de documentación (cubierto en `functional-spec.md`).

## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-17T03:34:08Z
**Iteration:** 1

### Hallazgos

| #   | Severidad | Ubicación                                                                                          | Hallazgo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Recomendación                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | --------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Critical  | `entities.md` (`allowed_values` de `field_key`), `rules.md` BR1.4, `functional-spec.md` Workflow 2 | **El diseño reintroduce, dentro de sí mismo, el mismo mismatch de vocabulario que el hallazgo #87 (razón de ser del intent).** `entities.md` fija `allowed_values: ["body_type", "fuel_type", "drivetrain", "make", "transmission", "engine_type"]` para `CanonicalFieldOption.field_key` — este es el vocabulario de `attribute_schema`/`nhtsa_normalizer.py` (confirmado con `normalize_nhtsa_value(field_type)` y `Category.validate_attributes()`, que itera `self.attribute_schema.items()` con esos mismos nombres). Pero el ÚNICO consumidor real de BR1.4/Contrato 2 (`GET /categories/facebook-values/{field_key}`) es `category-schema-editor.tsx`, que resuelve `field_key` a través de `FACEBOOK_FIELD_KEY_MAP` hacia el tipo `FacebookFieldKey` — verificado en `apps/web/src/lib/i18n/facebook-values/index.ts`: `label, category, vehicle_type, body_style, vehicle_condition, fuel_type, transmission, brand, year, exterior_color, interior_color, item_state, platform, device`. Comparando ambos vocabularios: solo `fuel_type` y `transmission` coinciden literalmente; `body_type` ≠ `body_style`, `make` ≠ `brand`, y `drivetrain`/`engine_type` **no existen** en `FacebookFieldKey` (y `engine_type` tampoco existe en `nhtsa_normalizer.py` — no es una clave real de ningún lado, es inventada). Además, el reviewer de Domain Design ya había confirmado contra código real que `vehicle_router.py::decode_vin()` reconcilia también `wheelbase_type`, `bed_type`, `cab_type`, `electrification_level` — ninguno de los 4 aparece en `allowed_values`. `rules.md`/`entities.md` nunca especifican si `field_key` es un namespace único compartido entre BR1.1 (llamado con nombres de `attribute_schema`) y BR1.4 (llamado por el frontend con nombres `FacebookFieldKey`), ni qué componente traduce entre ambos — sin esa definición, un developer no puede implementar correctamente ni la seed del catálogo ni el endpoint del Contrato 2, y el riesgo concreto es que se repita en runtime la misma desalineación silenciosa que este intent existe para cerrar. También compromete NFR1 (100% de campos select-backed cubiertos, sin margen) si el catálogo se siembra según esta lista incompleta/incorrecta. | Antes de Code Generation: (a) confirmar si `CanonicalFieldOption.field_key` usa el vocabulario `attribute_schema`/VIN-decode o el vocabulario `FacebookFieldKey`, o si son dos namespaces distintos con una tabla de traducción explícita (análoga a `FACEBOOK_FIELD_KEY_MAP`, pero ahora en el backend o documentada acá); (b) corregir `allowed_values` contra la lista real confirmada por Domain Design (`make, fuel_type, transmission, body_type, drivetrain, wheelbase_type, bed_type, cab_type, electrification_level`), quitando `engine_type`; (c) decidir si BR1.1 (reconcile, superset de campos VIN-decode) y BR1.4 (endpoint, solo campos con `FacebookFieldKey` mapeado) operan sobre el mismo catálogo o catálogos con cobertura distinta. |
| 2   | Critical  | `rules.md` BR1.2                                                                                   | **El IF/ELSE de BR1.2 no cubre correctamente el tercer caso (campo sin catálogo aplicable durante el decode de VIN) y su cross-referencia a BR1.4 está rota.** BR1.2 dice: "IF BR1.1 devuelve None Y el field_key tiene catálogo aplicable (ver BR1.4 para el caso sin catálogo) THEN null + unmatched_fields. ELSE (hubo match) el campo se devuelve con el canonical_value." La negación lógica de "None Y tiene catálogo" incluye DOS casos, no uno: (i) hubo match, y (ii) no hubo match Y NO tiene catálogo — pero el ELSE etiqueta ambos como "(hubo match)" y ordena devolver "el canonical_value", que en el caso (ii) **no existe** (BR1.1 devolvió `None`). Implementado literalmente, esto produce un `KeyError`/valor incorrecto para cualquier campo sin catálogo aplicable durante el decode de VIN. La cross-referencia "ver BR1.4 para el caso sin catálogo" tampoco resuelve nada: BR1.4 solo define el comportamiento del endpoint `GET /categories/facebook-values/{field_key}` (Contrato 2) — no dice nada sobre el decode de VIN. El comportamiento correcto SÍ está descrito, pero en `functional-spec.md` Workflow 1 paso 7 ("el campo se devuelve con el valor normalizado tal cual, sin agregarlo a `unmatched_fields`") — no en `rules.md`, que es el artefacto primario/autoritativo de reglas de negocio. Un developer que implemente desde `rules.md` sin cruzar `functional-spec.md` implementa mal el caso borde que el propio brief de esta revisión pedía verificar explícitamente.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Reescribir BR1.2 como una lógica de 3 ramas explícitas: (a) match → canonical_value; (b) sin match, con catálogo aplicable → null + unmatched_fields; (c) sin catálogo aplicable (0 entradas para ese field_key) → valor normalizado tal cual, sin tocar unmatched_fields. Quitar la referencia a BR1.4 (no aplica a este flujo) o reemplazarla por una referencia a un mecanismo real (ej. `get_options(field_key) is None` como señal de "sin catálogo", consistente con el Consolidated Summary de `functional-design-questions.md`).                                                                                                                                                                                                                   |
| 3   | Major     | `rules.md` BR1.4 / `functional-spec.md` / `functional-design-questions.md`                         | `contract-summary.md` (ya READY) dejó una Open Question EXPLÍCITAMENTE asignada a esta etapa: "Manejo de error/timeout si `FacebookVehicleValueCatalog` no tiene entrada... — ¿404 silencioso, o log de warning?" (resuelta correctamente por BR1.4/Q1) **y** un hallazgo Minor propio del reviewer de Contract Design: "No se especifica gating de autorización para `GET /categories/facebook-values/{field_key}` ... Confirmar el nivel de auth en Functional Design de U1". Ninguno de los artefactos de esta etapa (`rules.md`, `functional-spec.md`, `functional-design-questions.md`) resuelve esto — no dicen si el endpoint requiere solo autenticación (patrón confirmado en `GET /{category_id}/schema`, que exige `CurrentUser` pero NO `_require_platform_admin()`) o el mismo gate de Platform Admin que las mutaciones de `category_router.py` (`_require_platform_admin()`, línea 115). `functional-spec.md` solo dice que la UI que lo consume "está gateada a Platform Admin" — eso describe el actor del frontend, no la política de autorización del backend. Es un ítem de implementabilidad real y explícitamente asignado a esta etapa que quedó sin resolver.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Agregar una cláusula a BR1.4 (o una regla nueva) fijando el nivel de auth del endpoint — dado que es una lectura de catálogo estático sin dato sensible por tenant, una opción razonable es requerir solo `CurrentUser` (mismo patrón que `GET /{category_id}/schema`), pero la decisión debe quedar explícita, no inferida.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4   | Major     | `rules.md` BR3.1                                                                                   | La afirmación "mismas 3 guardas ya usadas en `20260812_0002_migrate_legacy_sedan_products.py`" no se sostiene contra el código real. Verificado: las 3 guardas de esa migración son (1) `_validate_category()` — la categoría destino existe, (2) existencia de los productos legacy (chequeo previo, con `return` temprano si no hay ninguno), y (3) `_validate_legacy_products()` — los productos legacy TODAVÍA coinciden con la categoría/condición esperada antes de mutar (protección contra re-ejecución o drift de estado desde que se armó la lista de candidatos). BR3.1 en cambio define sus 3 guardas como: (1) el producto existe, (2) su categoría sigue siendo de vehículo, (3) el nuevo valor calza con el catálogo canónico — ésta última es una guarda inventada para este caso (no existe en el precedente, que no valida "nuevo valor" alguno porque no cambia atributos, cambia `category_id`), y NINGUNA de las 3 de BR3.1 reproduce la guarda (3) real del precedente (protección anti-drift/re-ejecución). La cita de fidelidad al precedente es incorrecta y puede llevar a un developer a omitir la protección de re-ejecución que sí tenía el script de referencia, o a copiar funciones de guarda con semántica distinta a la que el nombre sugiere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Corregir la cita: describir las 3 guardas de BR3.1 como propias de este caso (no como "las mismas" del precedente), y agregar explícitamente una guarda de re-ejecución/drift (ej. "el registro no fue modificado por otro proceso desde que se armó el conjunto a migrar") si el riesgo de re-ejecución parcial aplica a este script ad-hoc.                                                                                                                                                                                                                                                                                                                                                                                                              |
| 5   | Minor     | `entities.md`, restricción de unicidad de `accepted_raw_aliases`                                   | La regla "un mismo raw alias no puede aparecer en `accepted_raw_aliases` de más de una entrada del mismo `field_key`" se declara "validado en Code Generation" sin especificar el mecanismo (assertion al cargar el módulo, test unitario dedicado, u otro) — dado el precedente del proyecto de catálogos estáticos sin verificación propia (`CATEGORY_TRANSLATION_TABLE`, hallazgo #88), vale la pena que `unit-test-instructions.md` de Code Generation pida explícitamente un test dedicado, no solo confiar en que "se validará".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Pedir explícitamente en las instrucciones de test de Code Generation un test que recorra todo el catálogo y falle si hay un alias duplicado dentro del mismo `field_key`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

Hallazgos #1 y #2 son mecánicos/objetivos — verificables carácter a carácter contra el código real (`nhtsa_normalizer.py`, `apps/web/src/lib/i18n/facebook-values/index.ts`, `category-schema-editor.tsx`, `category_router.py`) y contra la propia lógica interna de `rules.md`; no requieren una decisión de producto, solo corrección. Hallazgo #3 requiere una decisión (nivel de auth) pero de bajo riesgo/alcance — no es un judgment call de arquitectura. Hallazgo #4 es objetivo (cita de precedente verificable contra el archivo real). Hallazgo #5 es una sugerencia de piso de test, no bloqueante por sí sola.

### Verificación de consistencia con `components.md`/`decisions.md`

`rules.md`/`entities.md` respetan ADR-001 (un solo componente `FacebookVehicleValueCatalog`, sin separar dato/lógica) y ADR-002 (no modifican `nhtsa_normalizer.py`, se insertan aguas abajo) — sin violaciones ahí. El problema no es que el diseño contradiga las ADRs, sino que la ADR-001/ADR-002 nunca tuvieron que resolver el vocabulario de `field_key` (esa pregunta no se planteó en Domain Design, que solo fijó `identifier: field_key + canonical_value` sin fijar el dominio de valores de `field_key`) — la responsabilidad de resolverlo cae en esta etapa (Functional Design), y no se resolvió (Hallazgo #1).

### Cobertura de `traceability.json`

Los 5 AC (AC1.1.1-AC1.1.4, AC1.2.1) mapean a un BR real y la relación es coherente en el papel. Sin embargo, AC1.2.1 → BR1.4 queda comprometido en la práctica por el Hallazgo #1: si `field_key` no tiene un vocabulario resuelto, BR1.4 no puede garantizar "las `options` resultantes son EXACTAMENTE el conjunto vigente" (AC1.2.1) porque no está claro con qué clave se consulta el catálogo. Las entradas `reverse` (BR3.1, BR5.1, FR4 → `N/A`, sin AC/historia propia) son correctas y consistentes con `stories.md` — mismo patrón ya usado en intents previos de este proyecto.

### Summary

El diseño es tecnología-agnóstico y no contradice las ADRs de Domain Design, pero tiene dos defectos Critical verificables: (1) el `field_key` de `CanonicalFieldOption`/BR1.4 usa un vocabulario que no coincide con el `FacebookFieldKey` real que consume `category-schema-editor.tsx` — el mismo tipo de mismatch de catálogos (hallazgo #87) que este intent existe para resolver reaparece dentro del propio diseño; y (2) la lógica de BR1.2 no cubre correctamente el caso "campo sin catálogo aplicable" durante el decode de VIN, con una cross-referencia rota hacia BR1.4. Sumados a dos hallazgos Major (auth del endpoint nuevo sin resolver pese a estar explícitamente asignado a esta etapa por `contract-summary.md`; cita de fidelidad incorrecta de BR3.1 contra el script de migración de referencia), un developer no puede implementar este Unit sin volver a preguntarle al arquitecto. NOT-READY.

## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-17T04:12:00Z
**Iteration:** 2

### Verificación punto por punto de los 5 hallazgos de la iteración 1

| #   | Severidad original                       | Estado       | Evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ---------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Critical (vocabulario `field_key`)       | **Resuelto** | `entities.md` fija `allowed_values: ["make", "fuel_type", "transmission", "body_type", "drivetrain", "wheelbase_type", "bed_type", "cab_type", "electrification_level"]` con una nota explícita de que es el vocabulario `attribute_schema`/VIN-decode, no `FacebookFieldKey`, y que la traducción es responsabilidad de U2. Re-verifiqué los 9 nombres carácter a carácter contra el código real: `nhtsa_normalizer.py::normalize_nhtsa_value()` usa exactamente `field_type in {"make", "body_type", "drivetrain", "transmission", "fuel_type", "electrification", "boolean", "wheelbase_type", "bed_type", "cab_type"}` como ramas del IF/ELIF, y `vehicle_router.py` (`VINDecodeResponse`, líneas 39-69 y el armado del response líneas 216-242) expone exactamente esos mismos 9 campos como atributos del response (`electrification_level` es el nombre del campo de salida para la rama `field_type == "electrification"` del normalizador — consistente, no una discrepancia). `engine_type` (clave inventada) ya no aparece en ningún lado. Los 9 nombres son reales y completos — no falta ninguno de los confirmados por Domain Design, y no sobra ninguno. |
| 2   | Critical (lógica de 3 ramas de BR1.2)    | **Resuelto** | BR1.2 ahora define explícitamente 3 casos mutuamente excluyentes y colectivamente exhaustivos: CASE 1 (match → canonical_value), CASE 2 (`get_options(field_key)` no vacío Y sin match → `null` + `unmatched_fields`), CASE 3 (`get_options(field_key)` vacío → valor normalizado tal cual, sin tocar `unmatched_fields`). La señal de distinción (`get_options(field_key) is None`/vacío) es un mecanismo real, no una referencia rota a BR1.4. Verifiqué que `functional-spec.md` Workflow 1 (pasos 5-7) describe la MISMA lógica de 3 ramas palabra por palabra — sin contradicción entre ambos artefactos. La lógica es correcta y completa; no queda ningún caso sin cubrir.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3   | Major (auth de BR1.4 sin resolver)       | **Resuelto** | BR1.4 fija explícitamente "requiere solo `CurrentUser`, sin `_require_platform_admin()`", citando el patrón de `GET /{category_id}/schema`. Verifiqué el código real de `category_router.py`: `get_category_schema` (línea 286, `GET /{category_id}/schema`) recibe `current_user: CurrentUser` y efectivamente NO llama a `_require_platform_admin()` — a diferencia de los endpoints de mutación (PATCH línea 307/326, clone línea 401/415) que sí la llaman. La cita del patrón es exacta. Es una decisión de bajo riesgo (lectura de catálogo estático sin dato sensible por tenant) resuelta directo, consistente con el criterio ya aplicado repetidas veces en este proyecto para decisiones sin trade-off arquitectónico genuino — resolución aceptable.                                                                                                                                                                                                                                                                                                                                                                                                        |
| 4   | Major (cita de fidelidad de BR3.1)       | **Resuelto** | BR3.1 ahora describe sus 3 guardas (existencia del producto, match con catálogo canónico, anti-drift/re-ejecución) como propias de este caso, y explícitamente aclara que NO son una copia literal de `20260812_0002_migrate_legacy_sedan_products.py` (que valida existencia de categoría destino y que los productos legacy siguen coincidiendo con categoría/condición — un shape distinto porque esa migración cambia `category_id`, no atributos). El espíritu tomado prestado del precedente (validar antes de mutar + proteger contra drift de estado) queda explícito sin sobre-afirmar identidad literal. `functional-spec.md` Workflow 3 refleja la misma redacción corregida. Sin sobre-afirmación.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 5   | Minor (mecanismo de unicidad de aliases) | **Resuelto** | `entities.md`, restricción de unicidad de `accepted_raw_aliases`, ahora pide explícitamente: "Code Generation DEBE incluir un test dedicado que recorra todo el catálogo sembrado y falle si encuentra un alias duplicado dentro del mismo `field_key`" — deja de ser una afirmación vaga de "se va a validar". Alcanza para un hallazgo Minor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### Consistencia de `functional-spec.md` (Workflows 1, 2, 3)

Los tres workflows quedaron alineados con las correcciones de `rules.md`/`entities.md`, sin texto viejo contradictorio: Workflow 1 refleja la lógica de 3 ramas de BR1.2 y aclara el vocabulario de `field_key`; Workflow 2 refleja la resolución de auth de BR1.4 (`CurrentUser` sin `_require_platform_admin()`) y el mismo vocabulario de `field_key`; Workflow 3 refleja las 3 guardas propias (no copiadas) de BR3.1. No encontré ninguna referencia residual a `engine_type`, a la cita de "mismas guardas" sin matizar, ni al IF/ELSE de 2 ramas original de BR1.2.

### `traceability.json`

Sin cambios respecto a la iteración 1, y sigue siendo correcto: AC1.2.1 → BR1.4 ya no está comprometido — con el vocabulario de `field_key` resuelto, BR1.4 sí puede garantizar que las `options` devueltas son exactamente el conjunto vigente para ese campo (AC1.2.1). El resto de las entradas (AC1.1.1-AC1.1.4 → BR1.1/BR1.2/BR1.3, y las entradas `reverse` para BR3.1/BR5.1/FR4) no se ven afectadas por las correcciones y siguen siendo coherentes.

### Hallazgos nuevos

Ninguno. Las correcciones son precisas, verificables carácter a carácter contra el código real, y no introdujeron inconsistencias nuevas entre `rules.md`, `entities.md` y `functional-spec.md`.

### Summary

Los 5 hallazgos de la iteración 1 (2 Critical, 2 Major, 1 Minor) quedaron resueltos con evidencia verificable contra el código real (`nhtsa_normalizer.py`, `vehicle_router.py`, `category_router.py`), sin sobre-afirmaciones nuevas ni contradicciones entre los artefactos del Unit. Un developer puede implementar este Unit sin necesitar volver a preguntarle al arquitecto. READY.
