## Plan Approval

**Resumen del plan**: extensión de `SchemaFieldRenderer.tsx`/`VinDecodeField.tsx` con el mecanismo de `unmatched_fields` (canal RHF virtual `_unmatchedFields`), extensión de `category-schema-editor.tsx` para consumir el catálogo canónico de U1 vía `row.key` directo, y `ProductLocationFields` nuevo (ciudad/provincia + badge de herencia). Metodología: test-after, Test Strategy Standard (5-8 tests/componente).

**Testing Contract:** ver `code-generation-plan.md` (bloque JSON completo, idéntico al output de `aidlc-testing-posture.ts render`).

[Approval Fingerprint]: sha256:7205159f359208ee87559c9f2a78852d22951a773f80dfef332e89c1acea4a76

- Approve Plan
- Request Changes

[Answer]: Approve Plan
