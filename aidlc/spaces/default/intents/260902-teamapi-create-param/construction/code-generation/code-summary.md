# Code Summary — Fix teamApi.create mismatch parameter

## Files Modified

- `apps/web/src/lib/api/teamApi.ts` — `CreateTeamRequest.organization_id` → `org_id` (FR1.1).
- `apps/web/src/lib/api/schemas/teamApi.ts` — `TeamSchema.organization_id` → `org_id` (FR2.1).
- `apps/web/src/components/forms/TeamForm.tsx` — `onSubmit` payload field `organization_id` → `org_id` (FR1.2).
- `apps/web/src/hooks/useTeams.ts` — `UseTeamsReturn.createTeam`'s locally-redeclared parameter type field `organization_id` → `org_id` (FR2.2; mechanical consequence of the `CreateTeamRequest` rename, not named by file path in `requirements.md` but is exactly the "code that consumes the field" FR2.2 describes).
- `apps/web/src/hooks/useTeams.test.ts` — mock `Team` fixtures and the `createTeam` call assertion updated to `org_id` (mechanical consequence, same behavior asserted against the corrected field name).
- `apps/web/src/components/teams/TeamSwitcher.test.tsx` — mock `Team` fixtures updated to `org_id` (mechanical consequence — these objects are typed against `TeamListResponse`/`Team`, which now requires `org_id`).
- `apps/web/tests/components/forms/TeamForm.test.tsx` — `createTeam` call assertion updated to expect `org_id` (mechanical consequence).

## Files Deleted

- `apps/web/src/app/api/v1/teams/route.ts` (mock `POST`/`GET /api/v1/teams`) — FR3.1.
- `apps/web/src/app/api/v1/teams/[id]/route.ts` (mock `GET /api/v1/teams/{id}`, no `PATCH`) — FR3.2. Its removal also resolves FR4.1 (`teamApi.update()`'s probable 405): `PATCH /api/v1/teams/{id}` now falls through the existing `fallback` rewrite in `next.config.ts` (unchanged, already correctly configured) straight to the real backend endpoint (`team_router.py`, `PATCH "/{team_id}"`), so no separate code change was needed for FR4.
- `apps/web/src/app/api/v1/teams/org/[orgId]/route.ts` (mock `GET /api/v1/teams/org/{orgId}`) — FR3.2.

No backend DTO/router changes were needed — `CreateTeamRequest.org_id` and `TeamResponse.org_id` (`apps/api/src/prosell/application/dto/team/{create,response}.py`) were already correct; only the frontend and its mocks moved to match, per `requirements.md` Constraint C1.

## Files Added

- `apps/api/tests/integration/api/test_team_api.py` — targeted regression (FR3.3): `AsyncClient`/`ASGITransport` integration test against the real `POST /api/v1/teams` endpoint (mocked repository, no DB — same pattern as the existing `test_team_invitation_api.py`), proving the corrected wire shape (`{name, org_id}`) is accepted (201) and that the stale shape (`organization_id` only) is now rejected (422, no alias).
- `apps/api/tests/contract/schema_matching/test_team_schema_drift.py` — Layer 3 schema-matching test (FR5.1, FR5.2), per `.skills/contract-testing/SKILL.md`: introspects `CreateTeamRequest`/`TeamResponse` Pydantic `model_fields` and parses the corresponding TS field names out of `teamApi.ts`/`schemas/teamApi.ts` via a brace-balanced regex scan (documented in-file as a deliberate simplification — sufficient for these two flat object literals), asserting `org_id` (not `organization_id`) on both sides for both the request and response shapes. This is the permanent structural regression against this bug class recurring on either side of the contract.

## Key Implementation Decisions

- Target field name is `org_id` (the backend's existing name) per `requirements.md` Constraint C1 — the frontend aligned to the backend, not the reverse, consistent with the project's established Zod-mirror convention (backend is the source of truth for wire shape).
- FR3's mock removal fully absorbs FR4 (no separate `teamApi.update()` fix needed) — deleting `[id]/route.ts` entirely, rather than adding a `PATCH` handler to the mock, is the more consistent fix: it makes ALL of `GET`/`PATCH /api/v1/teams/{id}` reach the real backend, not just `PATCH`.
- The Layer 3 test's TS field extraction is a deliberately simple brace-balanced regex scan (ponytail-style, documented in-file), not a real TypeScript parser — appropriate for two flat object literals; flagged in-file as needing a real parser if either type grows nested objects.

## Test Coverage Summary

- Frontend: 28 tests passing across the 3 updated files (`useTeams.test.ts`, `TeamSwitcher.test.tsx`, `TeamForm.test.tsx`) — no new frontend test files, only field-name updates to existing assertions.
- Backend: 6 new Layer 3 schema-matching tests, all passing locally (no DB required). 2 new integration tests (`test_team_api.py`) collected successfully and correctly SKIPPED in this sandbox — this environment has no Docker available to run the temporary Postgres 17 container the project's established convention uses to execute `tests/integration/**` locally (all other pre-existing integration tests skip identically here, for the same reason; this is not a regression introduced by this change). These 2 tests will execute for the first time in CI (`test-python` job, which does provision `postgres-test`) or in a future session with Docker available — their logic was verified by close inspection against the exact same fixture/dependency-override pattern already proven in `test_team_invitation_api.py`.
- `ruff check`, `ruff format --check`, and `pyright` all pass clean on both new/modified backend files.
- `eslint --max-warnings=0` and `tsc --noEmit` both pass clean on all touched frontend files (after clearing two stale, gitignored `.next/types` Next.js route-type validators that referenced the deleted mock route files — a local build-cache artifact that regenerates automatically on the next `next dev`/`next build`, not a real type error).

## Deviations from the Plan

None beyond the two documented in `code-generation-plan.md` (the `useTeams.ts` local type edit and the 3 mechanical test-file updates), both already noted there as consequences of already-approved FRs rather than new decisions.

## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-02T22:50:56Z
**Iteration:** 1 (ADVISORY)

### Findings

| #   | Severity | Location                                                                                                 | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Recommendation                                                                                                                                                                                     |
| --- | -------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Minor    | `apps/web/src/lib/api/schemas/teamApi.ts` (`TeamSchema`) vs. `apps/api/.../response.py` (`TeamResponse`) | The two shapes have diverged on a field the FR/NFR set does not mention: backend exposes `manager_count`/`vendor_count`/`description`/`parent_team_id`, frontend schema exposes `member_count` (backend has no such field) and both use `.optional()`/`.passthrough()` so nothing breaks today — but the new Layer-3 drift test (`test_team_schema_drift.py`) only asserts the 6 `CORE_FIELDS_BOTH_SIDES_MUST_SHARE`, so it will never catch this specific drift even though it exists in the exact two files the test was built to guard. Pre-existing, out of this bugfix's scope (NFR2 explicitly excludes non-`org_id` field changes), but worth flagging since the artifact's stated purpose ("permanent regression against this bug class") reads as broader than what the test actually enforces. | Non-blocking for this intent; note as known limitation, or widen the drift test's assertions in a follow-up if `member_count`/`manager_count`+`vendor_count` divergence ever matters functionally. |
| 2   | Minor    | `traceability.json`                                                                                      | `upstream_ids` lists parent IDs (`FR1`, `FR2`, `FR3`, `FR4`, `FR5`) but `coverage[]` only enumerates their child sub-IDs (`FR1.1`, `FR1.2`, …) — consistent with the project's established pattern elsewhere (parent = container, children = pass/fail units) and every child is covered, so this is not a gap, just worth naming explicitly for a future auditor who greps for a bare `FR3` row and finds none.                                                                                                                                                                                                                                                                                                                                                                                         | No action required; cosmetic.                                                                                                                                                                      |

### Verification Performed

- Read the stage definition, Q&A/plan approval, approved plan (with embedded Testing Contract, fingerprint `sha256:1bd28...` matching the approved answer), `code-summary.md`, `traceability.json`, and upstream `requirements.md` (FR1-FR5, NFR1-NFR2, C1-C3) — all self-consistent, no contradictions found.
- Read every modified/added file end-to-end: `teamApi.ts`, `schemas/teamApi.ts`, `TeamForm.tsx`, `useTeams.ts`, `useTeams.test.ts`, `TeamSwitcher.test.tsx`, `TeamForm.test.tsx`, `test_team_api.py`, `test_team_schema_drift.py`, plus the two backend DTO files (`create.py`, `response.py`).
- `rg -n "organization_id"` across every team-domain frontend file (prod + test) returned zero matches — the field rename is complete and has no stray leftover.
- Confirmed via `ls`/`git status --short` that all three mock BFF route files (`route.ts`, `[id]/route.ts`, `org/[orgId]/route.ts`) are deleted (`D` in git status), and `rg -n "app/api/v1/teams"` across `apps/web/src` (excluding test files) found no remaining reference to the deleted paths — no dangling import.
- Confirmed `git status --short` shows zero changes under `apps/api/src/prosell/application/dto/team/` — the code-summary's claim that no backend DTO changes were needed is accurate; both DTOs already used `org_id`.
- Ran the frontend test files live: `pnpm exec vitest run src/hooks/useTeams.test.ts src/components/teams/TeamSwitcher.test.tsx tests/components/forms/TeamForm.test.tsx` → **28/28 passed**, matching the code-summary's claim exactly.
- Ran `pnpm exec tsc --noEmit` → clean, no errors, corroborating the code-summary's typecheck claim.
- Ran the two new backend test files live: `uv run pytest tests/contract/schema_matching/test_team_schema_drift.py` → **6/6 passed**; `uv run pytest tests/integration/api/test_team_api.py` → **2/2 SKIPPED** with the explicit reason "Integration test DB (localhost:5433) not available" — this exactly matches and substantiates code-summary.md's disclosure that these 2 tests were skipped in this sandbox due to no local Postgres/Docker, not silently claimed as passing. The disclosure is honest, not glossed over.
- Ran `uv run ruff check` on both new backend test files → clean, corroborating the lint claim.
- Read both new backend test files fully (not skimmed): `test_team_api.py` correctly exercises the real FastAPI app via `ASGITransport`/`AsyncClient` with the repository mocked out (no DB dependency for logic, only for the transport-level fixture check), asserts 201 + `org_id` present + `organization_id` absent for the corrected payload, and asserts 422 for the stale payload with no DB call made — this is a real regression test, not a tautology. `test_team_schema_drift.py`'s brace-balanced regex field extraction was manually traced against the two real TS files and correctly resolves `CreateTeamRequest`'s and `TeamSchema`'s top-level field sets for these two flat literals; the in-file caveat about needing a real parser if nested objects appear is accurate and appropriately scoped.
- Verified the "mechanical consequence" edits (`useTeams.ts`'s local `createTeam` param type, and the three test-file field-name updates) are genuinely mechanical: each is a 1:1 rename of `organization_id` → `org_id` in a type declaration or a fixture/assertion literal, with no new test scenario, no new branch, no altered assertion semantics beyond the field name. No hidden behavior change found.
- Confirmed the DTO/response backend contract fields (`org_id: UUID` on both `CreateTeamRequest` and `TeamResponse`) match what FR1.1/FR2.1/requirements.md cite by file and line.
- Every FR/NFR in `traceability.json`'s `coverage[]` array points to a file that actually exists and actually contains the claimed change; no broken or invented target found.

### Summary

Code Generation output for this bugfix is accurate, complete, and traceable end-to-end: every FR/NFR claim in `code-summary.md` and `traceability.json` was independently verified against the real code and by re-running the actual test commands, all of which reproduced exactly the results claimed (28/28 frontend tests, 6/6 new backend schema-drift tests, 2/2 backend integration tests correctly skipped for a disclosed, non-regression environment reason). No stray `organization_id` reference survives in the `team` frontend domain, no dangling import from the three deleted mock routes, and no backend DTO was touched (confirmed unnecessary, confirmed untouched). The two findings above are informational only — a pre-existing, out-of-scope field-naming loose end between `TeamResponse` and `TeamSchema` that the new drift test does not (and was never asked to) catch, and a cosmetic traceability convention already established elsewhere in the project. Neither blocks approval.
